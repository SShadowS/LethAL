node_modules/
dist/
# Compiled standalone binaries (`bun run build:binary`) — ~100 MB each, never committed.
build/
*.tsbuildinfo
.DS_Store
.bun-cache/
coverage/
*.log
.env
.env.local
launch.local.json
lethal.sqlite
lethal.config.local.json
lethal.config.envtool.json
*.app

# Local MCP server config — contains credentials
.mcp.json

# AL symbol packages (large, machine-local)
.alpackages/

# Serena's project memories — machine-local agent scratch, not project source
.serena/
lethal.sqlite-shm
lethal.sqlite-wal

# Model-panel review artifacts — agent scratch, not project source
.panel/

# Agent worktrees — machine-local checkouts, not project source
.claude/worktrees/

# AL Test Runner VS Code extension scratch — machine-local, not project source
.altestrunner/
.stryker-tmp/
reports/mutation/

# R178: `lethal export` output embeds full source; never commit it (guarded in
# scripts/redact-campaign-report.test.ts).
mutation-report.json
mutation-report.html
