# LethAL — project instructions

Write in plain English.
Use common words and short sentences. Avoid jargon when a simpler term exists. If a technical term is necessary, define it the first time you use it.
Assume I am intelligent but unfamiliar with the terminology. Be concise, but do not remove details needed for correctness.

Mutation-testing tool for Microsoft Dynamics 365 Business Central **AL** code. Bun + TypeScript monorepo. Authoritative architecture is `design.md` (repo root); per-layer specs/plans under `docs/superpowers/`.

## The roadmap — `docs/roadmap/` is the record, `ROADMAP.md` is a generated index
Open work, measured-but-unclosed correctness risks, and known product gaps live **one item per file** in `docs/roadmap/R<nnn>.md` (zero-padded), each with a stable `R<n>` id, a `status` and an evidence pointer. Repo-root `ROADMAP.md` is a GENERATED index of titles, statuses and links — never hand-edit it; `bun scripts/roadmap-index.ts` rebuilds it and `scripts/roadmap-index.test.ts` fails if the two disagree.
- **Reading:** the index answers "is this already filed?" for ~6 KT; reading one item is reading its file. The single-table form is gone because a field-wise read on `|` silently returned a fraction of a row and looked complete (R118) — one file per row makes that impossible.
- **Adding:** **file an item the moment you discover one** (a limitation you hit, a gap a real project would trip on) — write `docs/roadmap/R<nnn>.md`, then regenerate. Two sessions filing at once cannot collide.
- **Closing:** mark `status` `done (<commit>)` as it lands, then regenerate. A row that closes with a RULING rather than a code landing may say `closed <date> — <the ruling>` instead; both count as closed, and a "how many are open" count must accept either. (R77 and R101 are the two, and they were written that way deliberately — a ruling has no commit to name.) Session ledgers under `.superpowers/` are scratch and get archived; the roadmap is the durable record.
- **Re-check the next free id immediately before writing** (`ls docs/roadmap/`). Other sessions commit here concurrently and HEAD can move mid-session: R174 was overwritten that way and had to be restored from HEAD.
- Check it before starting new work: what you are about to build may already be filed, or blocked on something that is.

## Packages (workspaces under `packages/`)
- `engine` — AST (tree-sitter-al), `MutationSpec`, semantic layer, `astSubtreeHash`.
- `operator-sdk` — operator interfaces.
- `builtin-tier1` — Tier-1 mutation operators.
- `schemata` — compiler: instrument a project with all mutations behind runtime guards, one artifact (`selector.ts`, `project.ts`, `compile.ts`).
- `runner` — orchestration + execution: `orchestrator.ts`, backends (`bcdev-backend.ts`, `al-runner-backend.ts`), `store.ts` (bun:sqlite), `deployment-verifier.ts`, `run-mutant-transport.ts`, `harness.ts`.

AL extension: `extensions/lethal-control` (the `LethAL Control` BC extension, runtime 16).

## Build / test loop (order matters — the dist trap bites every session)
1. `bun run typecheck` — `tsc --build --force`. SEPARATE from tests; run explicitly.
2. `rm -rf packages/*/dist` — **AFTER typecheck, BEFORE any `bun test`**. `tsc --build` regenerates `packages/*/dist`, whose stale compiled `*.test.js` get picked up by `bun test` and cause ~21 phantom failures. (A PostToolUse hook auto-cleans dist after a typecheck — see `.claude/settings.json`.)
3. `bun test` (or `bun test packages/<pkg>`) — full unit suite; does NOT type-check.
4. `bun run compile:fixtures` — offline `alc` compile of every `fixtures/*` AL project. Run it after
   touching ANY `.al` under `fixtures/`. Nothing else compiles them: LethAL publishes the target on
   every run but treats publishing the TEST APP as the user's own workflow, so a broken test fixture
   leaves the live gate happily measuring the previously published build. That is not hypothetical —
   a docs-only commit deleted a procedure's body and `itest:tables` stayed green for days (R56).
- `bun run mutate` — StrykerJS over LethAL's OWN TypeScript (currently `selection.ts` + `line-map.ts`, ~20 s). Optional, never gates. **Read `docs/mutation-testing-ourselves.md` before reading a score**: it would NOT have caught R175, because that was a wrong belief about BC encoded in the code AND in the test pinning it, and mutating such a gate kills the mutant. It catches the OTHER hazard, a test that would pass either way, and found one in hour-old code on its first run.
- Lint: `biome check .` is noisy (pre-existing organizeImports/format debt in `engine`/`builtin-tier1`). Run biome **only on files you touched**: `bunx biome check <paths>`. (A PostToolUse hook auto-formats touched `*.ts` — see settings.)

## Integration tests (env-gated, live BC, minutes each — run foreground, never poll)
- `LETHAL_ITEST_BCDEV=1 bun run itest:bcdev` — authoritative backend. Frozen: killed **3** / survived **12** / no-coverage **4**, and since R132 it also pins `assertionScreen.discrimination` **`vacuous`** — its suite raises through bare `Error(...)` only, so R121's screen flags every kill and separates nothing there, which is the half the tables gate gave up when its fixture gained an assertion arm.
- `LETHAL_ITEST_ALRUNNER=1 LETHAL_ALRUNNER_PATH="C:/Users/SShadowS/.dotnet/tools/al-runner.exe" bun run itest:alrunner` — frozen: **3 / 16 / 0**, and since R147 it also pins `executionContexts[].platformAppsDir` — a session that did not pin al-runner's platform-app directory fails the gate, and emits an `al-runner-platform-apps-unpinned` warning naming which check refused. Measured against **al-runner v2.3.1.0** (re-measured 2026-08-20 after bumping 2.1.2.0 -> 2.3.1.0, a minor jump that skips 2.2 entirely — NuGet publishes no 2.2.x — and makes the parity-verified BUNDLED mode the default, demoting `--per-suite` to legacy. VERDICT-IDENTICAL: all 17 per-mutant verdicts unchanged, R129's BC-runtime announcement still parses, R147's platform-app pin still parses, and R149's pinned contract re-measurement ran without refusing, so all five wire facts still hold under `--package-cache`. Previously re-measured 2026-08-12 after 2.1.1.0 -> 2.1.2.0, whose parser moved onto BC's own syntax tree; unchanged then too, and unchanged the same day under the grammar 4.0.0 bump). al-runner ships several times a day and is a global dotnet tool, so the gate PRINTS the build it ran against as its first line — read it before calling a difference a regression. The measured v2 CLI/wire contract is in `docs/measurements/README.md` §"al-runner v2"; R123 is the probe that would check it per session instead of by habit. **Do not pin the tool to an older release to make this gate pass** — 2.1.0.0 needed `--auto-provision` in the argv, not a downgrade (R125).
- `LETHAL_ITEST_ENVTOOL=1 bun run itest:envtool` — bcdev reached through a config-declared external environment tool (Layer 6C; see `fixtures/README.md` §"Running against an external environment tool") instead of a directly-configured container. Frozen: **3 / 12 / 4** over 19, and **MEASURED 2026-08-28** on a restored environment after two days in which these figures were INFERRED from `itest:bcdev` and must not have been quoted beside the others (R177). The number is not the point: the per-mutant table is IDENTICAL to `itest:bcdev`'s on the same day, diffed mechanically, which is what says the indirection changes no verdict — a matching 3/12/4 would look the same even if the indirection had changed WHICH mutants those were. The restoration was pre-committed by identity key before it ran (`docs/superpowers/specs/2026-08-28-r177-envtool-restoration.md`) and returned exactly the two predicted additions, zero removed, zero changed. **Provisioning a replacement environment has two hard requirements, both found by hitting them: BC 27+ (`lethal-control` is runtime 16.0; BC 26 caps at 15.2 and refuses the publish) and DK localization (the fixture's company is `CRONUS Danmark A/S`, which W1/BASE does not have, and the harness 404s AFTER the control app publishes fine).** BC 28 DK matches `itest:bcdev`'s container, so prefer it. A brand-new environment also needs the fixture bootstrapped once by hand — publish `sandbox-app`'s built `.app`, then `sandbox-tests`' — or the tests app fails with `AL0185: Codeunit 'Sandbox Logic' is missing`. The gate still REFUSES to self-record a baseline while `UNVERIFIED_MOVES` (in the itest) is non-empty: confirm each listed mutant by name against `itest:bcdev` on the same day and clear the list in the same commit that records the baseline. Needs a gitignored `fixtures/sandbox-app/lethal.config.envtool.json` naming a reachable environment.
- `LETHAL_ITEST_TABLES=1 bun run itest:tables` — the TABLE fixture (`fixtures/sandbox-data` + `-tests`), where Tier-2 operators and table-trigger mutation live. Frozen: killed **267** / survived **63** / no-coverage **15** over 345 deployed mutants (365 raw specs), plus `untargetedTriggerCount` **0**, and **exactly ONE expected baseline failure** — `Data Tests.PageActionComputesNonZero`, which opens a `TestPage` the fenced session refuses. (R30 grew it from 64/9/2 over 75: a `tableextension` — 5 new mutants, all killed — and a `page`/`pageextension` pair whose 4 mutants are `no-coverage`. R78 took it to 69/9/9 over 87: a minimal `codeunit`/`page` pair reachable ONLY through a TestPage test, so its 3 mutants are the measured statement of the R69 gap. **R69 is now CLOSED — recovery measured at 2.30% of a real app's mutants and the routed path DELETED (`c1da575`)** — so those three are permanent, not provisional: they are what "we do not recover this, and we say so" looks like in a gate. R70 then took it to 71/9/10 over 90: `table 79309` / `page 79324 "Data Scope Probe"`, a cross-kind NAME COLLISION — the one shape all four gates were blind to. Its `void-method-call` at the `SetRange` site is the detector: under the R70 bug Tier 2 claims that site instead and §3.2 precedence DELETES the Tier-1 mutant, so the regression appears as an OPERATOR NAME change the per-mutant baseline catches.) NOTE the gate no longer asserts a blanket green baseline: it asserts that exactly that one test fails, BY NAME, and that the refusal is NAMED in the report — flipping the old assertion to `false` would have gutted it. R82 then took it to 109/17/10 over 136: `codeunit 79311 "Data Swap Ops"`, six arms measuring `lethal.swap-call-arguments` live, with all 30 per-mutant verdicts PRE-COMMITTED before the run and all 30 matching. Four of its sites carry BOTH a swap and a `void-method-call` mutant — the operator adds mutants rather than displacing them. Arm E is the one to know about: a swap killed by a BC length overflow under a test that asserts NOTHING, i.e. a false kill, and it exposed R86 — no kill records why it died.) R72 then took it to 113/18/10 over 141: `Data Commit Ops.CommitThenRunValueForm`, the SAME shape as `CommitThenRun` with `Codeunit.Run`'s return value consumed, which is the one factor a 2x2x2 on Cronus281 measured as deciding BC's write-transaction refusal. Its `remove-commit` mutant is the first anywhere scored `killed` AND screened as a platform artifact (`SessionReport.platformArtifactKills`); the verdict deliberately does not move. All five new verdicts were pre-committed in `docs/superpowers/specs/2026-08-08-r72-value-form-arm-precommitment.md` before the run and all five matched. R136 then took it to 157/25/10 over 192: three operator changes (`swap-modify-flag` extended from `Modify` alone to `Insert`/`Delete`, plus the new `swap-find-direction` and `validate-to-assign`), eleven fixture arms across two new tables, with all 51 per-mutant verdicts PRE-COMMITTED in `docs/superpowers/specs/2026-08-12-r136-trio-precommitment.md` before the run and all 51 matching. Arm K's three untagged kills — a duplicate primary key from a blank `Code[20]` key on a second insert — were the live statement of `docs/roadmap/R138.md`, which is now CLOSED: `swap-modify-flag`'s `Insert` mutants declare `run-trigger-skipped-insert`, so `platformArtifactKills.killedCount` is **3** (the `remove-commit` kill plus the fixture's two KILLED `Insert` sites, arms A and K) and the gate pins BOTH mechanisms by name and the Insert group BY MUTANT. Only ONE of arm K's three kills was this operator's; the other two reach the same duplicate key through Tier-1 operators mutating the trigger itself and stay untagged, so the blind spot shrank rather than closed. **R143 then NARROWED that tag and `platformArtifactKills.killedCount` is now 2:** `insertSkipCanRaise` resolves the receiver's table, reads its `OnInsert` and drops the tag where that trigger provably does not assign the primary key, so arm A (an honest assertion kill on `Data Trigger Probe`, whose `OnInsert` sets a Boolean) LEFT the screen and arm K stayed. A receiver the project cannot resolve KEEPS the tag: for a screen the unsafe direction is under-tagging. No verdict moved and the baseline needed no re-recording. R134 then took it to 183/31/10 over 224 (243 raw): `codeunit 79317 "Data Filter Ops"`, seven arms measuring the new `lethal.flip-filter-literal`, the first operator anywhere in the product that mutates a filter STRING that BC re-parses at RUNTIME rather than AL that `alc` compiles, with all 32 per-mutant verdicts PRE-COMMITTED in `docs/superpowers/specs/2026-08-12-r134-filter-precommitment.md` before the run and all 32 matching. Six spans carry BOTH a flip and a `void-method-call` mutant, and at two of them the verdicts DISAGREE in opposite directions (arm D's flip survives while the deletion is killed, arm G's flip is killed while the deletion survives), which is discrimination evidence no aggregate count can fake; `assertFilterLiteralEvidence` pins it, along with arm H's refusal (a closed range classifies fine, then exhausts the ladder, so it emits no flip mutant). All four flip kills carry the arm's own bare `Error(...)` text naming the exact predicted count in `killingTestFailure`, so none of them is a BC filter rejection scoring `killed` without an assertion. The gate also asserts R121's assertion screen reports itself as **`vacuous`** here: all 52 tests raise via bare `Error(...)`, so the screen flags every kill and separates nothing, and pinning the DISCRIMINATION rather than a count is what stops the same number reading as a finding on this suite and on a suite that uses an assertion library. R141 then took it to 187/31/10 over 228 (248 raw): ONE more arm in the same codeunit, `CountTaggedInBand`, whose `SetFilter` literal carries an inner quote (`<>''`, not blank) and so hits the operator's CHARACTER refusal — `REFUSED_CHARACTERS` declines the string before anything is classified — which is a different code path from arm H's ladder exhaustion and had never run against a real server. The load-bearing result is an ABSENCE: `flip-filter-literal` still emits exactly SIX mutants, never seven, which `assertFilterLiteralEvidence` now asserts by procedure name for both refusal kinds. Its four collateral mutants are all killed, each carrying the arm's own `Error(...)` text, and both counts behind those kills were MEASURED against Cronus283 before the arm was written (`scripts/r141-filter-probe/`) rather than reasoned out. R132 then took it to 191/31/10 over 232 (252 raw) and, more importantly, flipped this gate's `assertionScreen.discrimination` from **`vacuous`** to **`partial`**: `codeunit 79318 "Data Assert Ops"` is a TWIN PAIR whose two procedures are identical in shape and whose covering tests differ only in how they raise, one through Microsoft's `Library Assert` and one through bare `Error(...)`. All four of its mutants are killed, so the verdicts are a control and the only difference is which side of R121's screen each kill lands on, which `assertAssertionScreenTwinPair` pins BY MUTANT rather than by a count. The `vacuous` case did not disappear: `itest:bcdev`, whose suite is still bare `Error(...)` only, now asserts it. Both states are pinned live, by two gates. This is a PIPELINE proof and says nothing about the rule's precision (26.1% on the one hand-classified corpus), which is the confusion R132 exists to prevent. The test app now declares a dependency on Microsoft's `Library Assert` (installed Global on both fixture containers; symbols come from the container's own dev endpoint). R144 then added `declarativeSites`, pinned here at **1 site in 1 file** (`src/DataMainList.Page.al`, an `Enabled = …` page property): the mutant totals are UNCHANGED by it, which is the point — a declarative site produces no mutant, and the gate asserts the report SAYS the refusal happened. R161 then took it to 194/32/10 over 236 (256 raw), and the four new mutants are all ONE shape: `if <bad condition> then Error(...)`, the ordinary BC guard clause, at which the tool previously emitted NOTHING because the `Error` call sits in an un-braced `then_branch` rather than a statement list. Three are killed by the `asserterror` that exists for that guard and the fourth SURVIVES (`Data Ops.InsertWithoutTrigger`'s `DataMain.Delete(false)`, whose only covering test deletes the row first, so the branch is never taken: `covered-but-unreached`, not `no-coverage`). All four verdicts and all three killing-test names were pre-committed in `docs/superpowers/specs/2026-08-19-r161-branch-slot-precommitment.md` before the run and all four matched. `itest:bcdev` re-run and UNCHANGED at 3/10/3; `sandbox-app` and `sandbox-hang` gain no sites, measured, so those gates could not move. Three later operators then took it to the current **201/34/11 over 246** (266 raw) without a fixture change, purely by finding sites already there: `swap-additive` (R159) adds 8, six killed and two survived, both survivors in `Data Main`; `remove-not` (R163) adds 1 killed; and `swap-modify-flag`'s forward direction (R165) adds 1 no-coverage. `swap-enum-member` (R162) finds NO site on this fixture, which is why it is measured elsewhere. R159's `toggle-blank-string` later took it to **259/60/15 over 334** (354 raw), and its arm `codeunit 79320 "Data Blank Ops"` is the point rather than the ten mutants: that operator kills two sites through a DUPLICATE PRIMARY KEY with nothing asserted, declares no `PlatformKillMechanism` on the ruling that changing a written value is ordinary changed behaviour, and leaves R121's screen to say so. Its spike could not test that — every kill was flagged, because this suite raised only through bare `Error(...)`. `ClassifyCode` is killed through `Library Assert` beside those two, so the screen SEPARATES, and `assertBlankStringScreenSeparates` pins both directions BY MUTANT. Before that, R159's `remove-assignment` took it to **252/57/15 over 324** (344 raw) — 52 mutants in one wave, the largest this fixture has taken, 33 killed and 16 survived and 3 no-coverage, and the sixteen survivors are the expected shape: an assignment whose target is never read again is an equivalent mutant no source-derived layer can see. The score falls 0.8423 -> 0.8155, the right direction. R171 then took it to **213/34/12 over 259** (279 raw), the only one of these waves to add a FIXTURE: `negate-guard` claims four guards that were already here (three killed, one in the uncovered pageextension), and `codeunit 79319 "Data Set Ops"` adds nine more, all killed. That arm exists because `remove-not`'s corrected cession adds ZERO sites on every other fixture, so landing the fix alone would have been a change no gate exercises. All thirteen verdicts were pre-committed in `docs/superpowers/specs/2026-08-20-r171-build-precommitment.md` and all thirteen matched, including the CONTROL at `DataSetOps` line 38 staying `negate-conditional`: an over-broad seam fix would have replaced it with a `remove-not` mutant, which is an operator-NAME change the per-mutant baseline catches and a matching total would not. R159's `flip-boolean-literal` then took it to **219/41/12 over 272** (292 raw): thirteen more mutants, SIX killed and SEVEN survived, and the survivors are the point rather than a regression. Four of the seven are behaviours this suite does not assert — `CommitBeforeCodeunitRunSucceeds` checks the row exists and `Flagged` is set but never reads `Amount`, which three of them double through `OnInsert`, and `DeleteRunTriggerLeavesTombstone` checks a RETURN VALUE rather than the tombstone's own field. They also answer R159's own point 2, the strongest argument against building that operator: `empty-block` KILLS those procedures while the fine-grained flip SURVIVES, so coarse and fine disagree at the same sites. The score falls 0.8623 -> 0.8423, which is the right direction for a wave that adds seven deliberate survivors. R159's `shift-integer` then took it to **267/63/15 over 345** (365 raw). Three of its eleven are the `Data Commit Ops` survivors every value-mutating operator finds there, and the other eight are `codeunit 79325 "Data Shift Ops"`, a TWIN PAIR that turned out to control FOUR operators rather than one: the shape `if Amount = 10 then exit(1); exit(0);` yields an `empty-block`, a `negate-conditional`, a `shift-integer` and a `return-value` mutant per half, and all eight kill for the same reason (the mutated procedure returns 0 where 1 is expected), so the VERDICTS are a control and the only variable across the pair is which side of R121's screen each kill lands on. `assertShiftScreenTwinPair` pins all eight BY MUTANT. All eleven verdicts were pre-committed in `docs/superpowers/specs/2026-08-26-r159-shift-integer-build-precommitment.md` and all eleven matched, with zero existing mutants moving. Incidentally it is also the first live proof that R166 was load-bearing: the two halves are byte-identical in shape, so all four operator pairs share an `astSubtreeHash` and only the enclosing procedure name separates them. The operator's OTHER cession, an integer literal in a loop's exit condition, has no arm on any fixture and never will: R164 rules that a hang-capable site must not enter a scored gate, and there is no safe version of one, since a counter-driven loop is made non-terminating by `remove-assignment`, by `swap-additive` and by `shift-integer` itself. A refusal emits no mutant for a server to run, so it is proven offline and positionally in `packages/builtin-tier1/tests/shift-integer.test.ts` instead. Needs a gitignored `fixtures/sandbox-data/lethal.config.local.json` — **not** sandbox-app's; the two fixtures target different containers.
- `lethal run` writes its JSON with `--out`; `--report` is `campaign`'s flag and is SILENTLY IGNORED by `run` (one shared strict `parseArgs` table serves every subcommand). `--progress-out` writes the events NDJSON.
- A differing verdict is a BLOCK (a real regression), never "close enough". Live execution is the authority — unit tests are structurally blind to AL that can't compile or to real BC behavior. Use `/live-gate`.

## AL has no unit-test harness
Verify AL edits by an offline `alc` compile (use `/al-compile` or the `al-compiler` subagent), plus the live gate. `alc.exe` and `altool.exe` live under `~/.vscode/extensions/ms-dynamics-smb.al-*/bin/` on the per-platform VSIX (18.0.2668733, the one installed) and under `bin/win32/` on the multi-platform one: probe both, newest first (R167).

**Changed a fixture's AL? Symbols and the test app do NOT update themselves.** Bump the target's `app.json` version, `alc` the target INTO the tests project's `.alpackages` (deleting the stale `<name>_<oldver>.app` there), then compile and `altool publishapp` the tests app. Skipping it gives `AL0185: Codeunit 'X' is missing` at compile, or `StaleTestAppError` at run, which is R56's guard refusing rather than measuring a stale build.
A dev-endpoint publish can be refused as a DOWNGRADE even when `app.json` is newer: LethAL mints `<major>.<minor>.<days>.<halfSeconds>` for every instrumented publish, so the container's resident version outranks it. Unpublish (tests app FIRST, since unpublishing the target cascades to its dependents), then republish target, then tests. See `.claude/skills/control-app`.

## Conventions (enforced — CI will fail otherwise)
- No `!` non-null assertions (biome `noNonNullAssertion: error`). Destructure then check `undefined`.
- `exactOptionalPropertyTypes`: build optional props with `...(v !== undefined ? { k: v } : {})`.
- Typed error classes extend `Error` **directly**, never each other — `AlcCompileError` (deterministic alc rejection) vs `ArtifactPrepareError` (spawn/IO/hash/manifest) vs `DeploymentError`. Bisection reads ONLY `AlcCompileError` as "subset does not compile"; anything else aborts. Preserve this separation.
- Fail loudly on caller-contract violations (bad artifact-id, corrupt manifest, echo mismatch) — throw, never return a plausible empty default. Empty-vs-empty "matches" is this project's signature bug.
- Generated AL: web-service `ObjectType` exactly `CodeUnit` (capital U). `emitMutationSelector` and `emitStaticSelector` must expose the identical procedure set. Artifact id: 32 lowercase hex, random per artifact.
- Assert phase ordering with call counters on stateful fakes, never wall-clock timing.
- Adding a `SessionReport` field ripples, and several guards fail far from the change: `events.ts`, then `report-fold.ts`'s accumulator, then `report.ts`'s type/builder/banner, then `bun scripts/generate-schemas.ts`, then `tests/schemas.test.ts` (the pinned root-required list AND the older-reports expectation), then `bun test <file> --update-snapshots` for `report-equality`, then every committed sample report REGENERATED live, since `schemas.test.ts` asserts gift-card's validates. A new `Caveat` also needs `CAVEAT_INTERPRETATIONS` plus the counts in `interpretation.test.ts` and `report.test.ts`.

## The recurring hazard: "test passes for the wrong reason"
A test asserts the right thing but passes whether or not the code is correct. Reading the diff does not catch it; only mutation does. **Red-check every fix**: revert the specific fix, confirm the specific test goes red, restore — report both. Use the `mutation-red-checker` subagent. Gate on per-mutant equality, not aggregate counts.

## Committing a campaign report — this repo is PUBLIC
A `SessionReport` from a real project carries that project's AL source in every mutant's
`originalText`/`mutatedText`. Two Continia Document Output reports were committed with 2,550 such
fields before anyone noticed (redacted 2026-08-09). **Run `bun scripts/redact-campaign-report.ts
<report.json>` BEFORE committing one**; `--check` exits 1 on an unredacted file and
`scripts/redact-campaign-report.test.ts` asserts both committed reports stay clean.
The ruling it implements (2026-08-09): **filenames, paths, procedure names and test names are fine
to publish; source code is not.** `killingTestFailure` is deliberately KEPT — its callstacks are
names and line numbers, and its message text is the corpus `scripts/r121-classify-eval.ts` scores
false-kill rules against. Redaction after a push changes what a fresh clone sees and nothing else.
Re-freezing a stage: `campaign compare` REFUSES an uncommitted baseline (the pre-commitment rule in code, not a bug), and `freeze` will not overwrite `<stage>.report.json` on a per-mutant mismatch; it archives to `<stage>.mismatch.report.json`. To re-freeze deliberately: delete `<stage>.baseline.json`, re-run `freeze`, review the diff, commit.

## Environment
Git bash on Windows; use bash syntax with Windows paths. **Never `2>nul`** (creates undeletable files) — use `2>/dev/null`. The `! <cmd>` prefix is only for interactive logins the tools can't complete (e.g. device-code auth). BC container publish/unpublish/restart are NOT user-only: `bccontainerhelper` is reachable from the PowerShell tool — the Cronus BC servers are Windows Docker containers on this machine (switch `docker context use desktop-windows` first, since the session default is the Linux engine). `altool publishapp` publishes over HTTP (dev endpoint); removal needs `UnPublish-BcContainerApp` under the Windows context.
