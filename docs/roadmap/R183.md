---
id: "R183"
title: "`authoritative: false` rests on a justification that is too broad: al-runner rolls back for `asserterror` but not for `Codeunit.Run`"
section: "correctness-risks"
order: 895
status: "done — the justification now separates what LethAL MEASURED from what it INHERITED from upstream docs, and `runAlRunnerCanary` gained a third probe that re-measures the residual every session. Mechanism pinned exactly: `asserterror` rolls back on al-runner 2.7.0.0, `Codeunit.Run` does not, which is one cause for both live symptoms. The flag stays `false`"
---

## What is wrong

`AlRunnerBackend.capabilities()` returns `authoritative: false`, and its doc comment justifies that
with, among other things:

> there is no BC service tier, so there are no transactions — `Commit()` and `Rollback()` are
> no-ops … (upstream `docs/limitations.md`)

**That sentence is measurably false on al-runner 2.7.0.0.** LethAL's own `remove-commit` operator
deletes a `Commit()` and asks whether any test notices. On the table fixture:

| procedure | bcdev | al-runner 2.7.0.0 |
| --- | --- | --- |
| `Data Commit Ops.CommitThenFail` | killed | **killed** |
| `Data Commit Ops.CommitThenRun` | survived | survived |
| `Data Commit Ops.CommitThenRunValueForm` | killed | **survived** |

If `Commit()` were a no-op, deleting it would change nothing and all three would survive. Instead
`CommitThenFail` is KILLED, by al-runner's own message:

```
NavNCLDialogException: expected the row committed before the error to survive it, but it is gone
```

Removing the `Commit()` made the row vanish when the error fired, which is BC's behaviour.

## Why the flag must NOT flip, and why this is still worth filing

`authoritative` stays `false`. `coverage: "none"` is unchanged, the other architectural limits stand,
and one of the three commit mutants really does diverge. Nothing here argues the backend is
authoritative.

**The problem is the same one [[R99]] already had to fix once.** That row found `authoritative: false`
resting on R7, discovered R7 was fixed on v2, and re-decided the flag on new grounds rather than
leaving it "correct by accident". The commit clause is now in exactly that state: it is quoted from
upstream's `docs/limitations.md` as current fact, upstream's doc is behind its own binary, and LethAL
copied it. A justification nobody re-measures is how the last one went stale.

## What the residual actually is, now that it has been measured

Narrower and more specific than "no transactions": only `CommitThenRunValueForm` diverges — [[R72]]'s
arm, `Codeunit.Run` with its **return value consumed**, which is the factor a 2x2x2 on Cronus281
measured as deciding BC's write-transaction refusal. The transaction boundary BC puts around
`Codeunit.Run` in that form is what al-runner does not reproduce.

That is a better sentence for the comment than the one there now, and it is one this repository
measured rather than inherited.

## Work

1. Rewrite the `capabilities()` justification to say what was measured: error-rollback IS modelled;
   the residual is `Codeunit.Run`'s value form, plus `coverage: "none"` and the limits no fixture
   here exercises.
2. Say plainly which claims are INHERITED from upstream docs and unverified by LethAL (permissions,
   company context, base-app data, parallel sessions). Today they read as though LethAL measured
   them; it did not, and it has no arm for any of them.
3. Consider a canary for the commit case, the way R7/R8 have one. The value is the same: this claim
   went stale silently, and a probe that re-measures per session would have said so.

Evidence: `docs/superpowers/specs/2026-08-28-alrunner-bc-parity-probe.md`, where both experiments
were pre-committed before running and this one refuted its own prediction.

---

## DONE 2026-08-28, and the fix found the row's own claim was too broad

All three work items landed, and the third one immediately refuted part of what this row said when
it was filed.

### The mechanism, measured rather than inferred

The row was filed saying "error-rollback IS modelled". Building the canary probe showed that is too
broad in the same way the sentence it replaced was. A three-test probe against al-runner 2.7.0.0:

| error caught by | writes rolled back |
| --- | --- |
| `asserterror`, no preceding `Commit()` | **YES** |
| `asserterror`, after an explicit `Commit()` | **YES** |
| `Codeunit.Run` | **NO** |

On BC both mechanisms roll back. **One cause, both live symptoms:** `CommitThenFail` is an
`asserterror` shape, so it agrees with bcdev; `CommitThenRunValueForm` is a `Codeunit.Run` shape, so
it diverges. The residual is the `Codeunit.Run` transaction boundary, full stop — not "no
transactions", and not "rollback works".

### What landed

1. **`capabilities()` rewritten** and now separates its claims by WHO measured them. What LethAL
   measured against a real container: `coverage: "none"`, the `Codeunit.Run` boundary, and the
   default `--isolation codeunit` not resetting the record store. What is INHERITED from upstream
   `docs/limitations.md` and unverified here — permissions, company context, base-app data,
   `StartSession` inline — is now labelled as such, because no fixture in this repo exercises any of
   it and the old comment read as though LethAL had checked.
2. **A third canary probe**, `transactionRollback`, run once per `--backend al-runner` session
   beside R7 and R8. **Its polarity is the opposite of theirs and the code says so:**
   `defect-confirmed` is the CURRENT correct answer on 2.7.0.0, while `defect-not-reproduced` would
   mean the gap has closed and this comment needs revisiting.
3. The R7/R8 justification staleness this row was really about is now structurally prevented for
   this claim too: three probes, all re-measured per session, none asserted.

### Why this was worth building rather than just rewording

The probe was written to prevent a future staleness. It caught a present one instead — the row's own
summary, written the same day, from a real measurement, and still over-general. Rewording alone would
have shipped "error-rollback is modelled" into the comment as the corrected fact.
