---
id: "R202"
title: "The held request of a session ended by R53's stop can answer HTTP 400 (\"Cannot establish a connection to the SQL Server/Database\") instead of the 408 that names the AL StopSession call; the transport then quarantines the tier the stop existed to score"
status: "open — filed 2026-09-03 from the R198 probe (`scripts/r198-group-runner-probe/`, Cronus283): 1 of 33 stops, the one in GROUP mode about 10 s after a fresh publish; the other 32 (20 recorded in `stop-race.measured.txt`) answered 408. NOT to be fixed by widening the kill rule: the stop's own answer is a decision, not a termination (its AL says so). The R198 redraft scores a non-408 after a confirmed stop only when the op's progress row proves the session died (it never advances past the stopped method across two polls); the same rule belongs at the single-method grain"
section: "backends-and-tooling"
order: 914
---

**Measured 2026-09-03.** `scripts/r198-group-runner-probe/drive.ps1` E8 (second run) and
`drive-stop-race.ps1` (10 rounds): a 45 s bounded hang in a test method, ended from a second
session by `StopSession`. Twelve times the held request came back **HTTP 408** with "The session was
stopped by an AL StopSession call", which `isAlStopResponse` recognises and
`run-mutant-transport.ts` scores `timeout`. Once it came back **HTTP 400** with "Cannot establish a
connection to the SQL Server/Database", 3.9 s after the stop, and the session was just as dead
(its progress row never advanced, its inserted row was rolled back). Which error the stopped
thread hits first is a race inside the NST.

**Today's consequence.** `run-mutant-transport.ts` scores `timeout` only for `stopFired && 408 &&
isAlStopResponse`; any other non-2xx after the stop falls to `in-flight-unknown`. The orchestrator
reconciles: the stop tombstoned the op, so rule 1 says `completed` and retries ONCE as a fresh
attempt, which re-runs the hang, burns another budget, and ends in `result-lost` or a quarantine.
`itest:hang` pins the 408 path and has never seen the other.

**What would fix it, and what must not.** The first idea, "a confirmed stop plus ANY non-2xx in
the grace window scores `timeout`", was refused by the R198 v2 review: `stopped: true` means
`TryStopHungRun` reached `StopSession`, and `TryStopHungRun`'s own comment says that proves
nothing about the session ending; a gateway 502 inside the grace window on a slow-but-passing
method would become a false kill. The evidence that IS in hand is the op's progress row
(`LC Op Progress` in the R198 redraft, which the single-method `RunMutant` writes too): a session
that really died inside method k never advances past `(k, token_k, running)`. So: after a
confirmed stop, a non-408 non-2xx on the held request is `timeout` only if the progress row still
reads the stopped method across two status polls one interval apart; otherwise `in-flight-unknown`
as today. The 408 path stays as it is. Measured frequency: 1 in 33, GROUP mode, ~10 s after a
publish; not measured whether the publish is the cause.
