---
id: "R11"
title: "`tierRank` has no tier-3 rank"
status: "**done (`d2b3236`) — by deciding it, not by adding the rank.** R13 measured that tier 3 does not become real, so the two-tier ordering is CORRECT rather than incomplete and the speculative fix would have been wrong: a third rank with no operator behind it is an ordering nobody has reasoned about, sitting ready to resolve a collision silently. The collision it guards is concrete — the one Tier-3 candidate that fits the existing emit path (`IsolationLevelSwap` deleting `LockTable()`) emits a byte-identical identity to the `void-method-call` mutant already shipped at **25** sites of Continia Document Output, so registering it as tier 3 aborts those sessions loudly, which is the designed behaviour. Closed the way R70 says a premise must be closed: as an EXECUTABLE assertion, not a comment — `packages/schemata/tests/dedup.test.ts` \"refuses to order a REGISTERED tier-3 operator against a tier-1 one\", red-checked (adding `tier === 3 ? 3` to `tierRank` fails exactly that test and nothing else), plus the reason in `tierRank`'s own comment. Adding the rank is only correct together with the first tier-3 operator that ships — never before it."
section: "next-up"
order: 30
---

**`tierRank` has no tier-3 rank.** A tier-3 operator colliding with a tier-1 one hits "cannot order" and throws instead of resolving by precedence. Fix when tier 3 becomes real.
