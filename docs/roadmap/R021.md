---
id: "R21"
title: "Env-tool mode still hard-requires `altool.exe` to exist"
status: "done (f616885)"
section: "backends-and-tooling"
order: 80
---

**Env-tool mode still hard-requires `altool.exe` to exist**, even though the env-tool publish path never constructs a `ContainerDeployer`. Harmless where the AL extension is installed; a confusing gate otherwise. Evidence: `packages/runner/src/cli.ts` `defaultAlToolPaths()` check.
