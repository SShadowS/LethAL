---
id: "R69"
title: "`TestPage` is UNSUPPORTED on the fenced path, so every test that opens one fails there — and the original…"
status: "done (`c1da575`, measured unprofitable 2026-08-02) — routed path and control-app objects 71011-71014 DELETED, verified per-mutant on all four gates afterwards (`itest:bcdev` 3/10/3 · `itest:alrunner` 3/13/0 · `itest:tables` 69/9/9 over 87 · `itest:envtool` 3/10/3, on LethAL Control 1.0.0.14); `testpage-unsupported.ts`, its report wiring, and the fixture pair with its three `no-coverage` mutants are KEPT as the permanent measured statement of what is not recovered"
section: "backends-and-tooling"
order: 540
---

**`TestPage` is UNSUPPORTED on the fenced path, so every test that opens one fails there — and the original framing of this row ("hangs") was wrong.** Measured 2026-07-31 on Cronus283 while building R30's fixture: a `[Test]` whose body is `DataMainList.OpenView(); DataMainList.Close();` never returned. LethAL saw only its own abort, correctly classified it `in-flight-unknown` (BC may still be executing), and latched the tier quarantined at BASELINE — `killed=0 survived=0 noCoverage=0 baselineGreen=true`, plus the usual §8 recovery (`force-reset-lease` → serverGeneration rotated, epoch → 91). **This is much larger than the fixture that found it.** Every verdict runs in the fenced `GuiAllowed=No` / `ClientType=ODataV4` session (R57/R60), real BC suites use `TestPage` heavily, and R58 made the fenced path the DEFAULT for the baseline as well as the mutants — so a customer suite with TestPage tests may be unable to complete a baseline at all, which reads as "LethAL hung" rather than as a session-type limitation. Direction is safe (a quarantine, never a wrong verdict) and R53's `--stop-hung-sessions` may already make it survivable, but that is UNTESTED here — R53's stop path was proven on mutant runs, and this hang is at baseline. **What is not yet known**, and each is one probe: whether it is TestPage as such or this page's shape; whether it hangs or waits on something answerable; whether `--stop-hung-sessions` converts it into a scored/failed baseline test rather than a quarantine; and whether Document Output's 1,246-test suite (which completes on the fence) contains TestPage tests at all — if it does, the failure is narrower than this row assumes and the row must be corrected. Evidence: `fixtures/sandbox-data-tests/src/DataTests.Codeunit.al` (the removed test and why), `fixtures/README.md` §"Extension objects". **CORRECTED 2026-07-31 by a dedicated probe — `TestPage` does not hang, it is REFUSED, fast.** `fixtures/sandbox-probes/src/{ProbeList.Page.al,TestPageProbe.Codeunit.al}` — a code-free list page and a test that does `OpenView(); Close();` — run on Cronus281 through both paths: **hub** (`GuiAllowed=Yes`/`ClientType=Web`): opens and closes fine (`MEASURED testpage-open=OK`, 1766 ms). **fenced** (`GuiAllowed=No`/`ClientType=ODataV4`): fails in **87 ms** with `Unexpected CLR exception thrown.: System.NotSupportedException: Specified method is not supported. at Microsoft.Dynamics.Nav.Runtime.NavSession.CreateNavTestService()`. The run COMPLETED — no hang, no quarantine, no stranded operation; score `n/a` with all 16 mutants `no-coverage` because the baseline went red. **So the hazard is real but different from the one filed**, and milder: a suite using `TestPage` does not wedge a run, it loses those tests from the green set at baseline, and every mutant they alone covered becomes `no-coverage` — reported with the `baseline-red` caveat, never as a wrong verdict. **Sized on a real project: 9 of Continia Document Output's 104 test files declare a `TestPage`, 38+ declarations** (`CDO Email Editor` alone 18 times), so a DO run on the R58 fenced default silently drops that slice of the suite. **What remains genuinely unexplained, and must not be attributed to TestPage:** the ORIGINAL observation on Cronus283 (a `TestPage` test that went `in-flight-unknown` at baseline, quarantined the tier and scored nothing). If the platform refuses `CreateNavTestService` in 87 ms, that test should have failed fast too. The difference was the page (over a table carrying `OnModify`/`OnValidate` triggers, with a `pageextension` `OnOpenPage` that WROTE a row) and the container. That strand is its own open question — it was a hang with an unknown cause, not a TestPage property. Direction is safe either way (a red baseline test, or a quarantine — never a wrong verdict). Fix candidates, in cost order: name the `CreateNavTestService` refusal in the report the way R27/R35/R59 name theirs, so a reader sees "these tests cannot run on this path" rather than N unexplained baseline failures; and decide whether a `TestPage`-using suite should be routed to a hub baseline deliberately (which re-opens R55's dual-runner asymmetry, so not free). **SPIKE 2026-08-01 — the client-services (native WebSocket) route is de-risked on transport and session-capability, but NOT yet measured end-to-end for a TestPage test on that exact path.** Probed via `U:/Git/bc-mcp` (native BC client-services protocol) against Cronus281; scripts left at `bc-mcp/scripts/r69-*.ts`. Established, all measured today: (a) **transport works** — bc-mcp opens the AL Test Tool (page 130451), reads its lines, invokes actions, and answers its dialogs over the native WebSocket; the correct "Choose" StrMenu answer is `SaveValue` the option's TEXT label (`"&All"`, not a numeric index) then `InvokeAction Ok(300)`. (b) **the in-session Test Runner FIRES over this path** — invoking `&Run Tests` reset the result column to pending and ran `Test Runner - Isol. Codeunit`, PROVEN because the runner itself raised `You tried to invoke the CodeUnit object with the ID 80002 from the object Test Runner - Isol. Codeunit. An object with that ID does not exist in the current application compiled with emit version 28014` — the runner executed in-session and tried to invoke the suite's (now-deleted) codeunit. (The suite's persisted `Success` rows were STALE from a removed app `80002 "CG-AL-E002"`; freshness was checked, not trusted.) (c) **CORRECTION (Fable review 2026-08-01) — link (c) is measured on the WRONG transport and is correlation, not proof.** The `MEASURED testpage-open=OK | GuiAllowed=Yes | ClientType=Web` reading (79218 on Cronus281) came via `bcdev_test_run`, which is a THIRD transport — a dev-endpoint **SignalR `TestRunnerHub`** (`bc-dev-mcp/src/core/hubs/test-runner-hub.ts`), NOT bc-mcp's client-services WebSocket. TestPage-open has therefore been observed on the SignalR-hub session and REFUSED on the ODataV4 session, and **never observed on a genuine client-services session.** The refusal is `NavSession.CreateNavTestService()` throwing — a **virtual-override question on the concrete `NavSession` subclass**; `(GuiAllowed, ClientType)` is a tuple the session *reports*, not the mechanism that grants the capability, so two different session classes can both report `Web/Yes` yet differ on the override. The bc-mcp client-services session's own `GuiAllowed`/`ClientType` were not captured. **The seam I named was also wrong:** it is NOT "does the isolation runner preserve/​fork the session" — `TestIsolation=Codeunit` is *transaction* isolation, a TestRunner codeunit runs via `CODEUNIT.RUN` in the driving session (the 80002 error surfacing on the driving WebSocket confirms this). The real seam is link (c) itself, on-path: is the client-services `NavSession` subclass one that overrides `CreateNavTestService`. **Practical risk is nonetheless LOW:** bccontainerhelper's `Run-TestsInBcContainer` / `ClientContext.ps1` drives the **Command Line TestTool (page 130455)** over `Microsoft.Dynamics.Framework.UI.Client` — the SAME client-services protocol bc-mcp reimplements — and TestPage-heavy suites pass through it in CI across the BC ecosystem daily; the composite (client-services session + isolation runner + TestPage) is precedent-proven at scale. So close it by measuring, cheaply. **The one unmeasured seam concretely:** running `79218` on the bc-mcp client-services transport. Over the STOCK AL Test Tool (page 130451) this is blocked by seeding — stale DEFAULT suite codeunit gone; "Get Test Codeunits by Range" filter did not match `79218` through this driver; the "Select Tests" modal lookup is NOT drivable via bc-mcp (`openedPages=[]`, form absent from `ctx.forms`). **Corrected next step (was: build a control action first — WRONG ORDER):** drive **page 130455 "Command Line TestTool"** over bc-mcp instead — it ships in the same Microsoft Test Runner app as 130451, exists to be driven programmatically, and seeds via a plain editable range-filter field whose `OnValidate` populates the suite (no modal, no "Get by Range" dialog — neither 130451 failure transfers). Zero new AL, runs the already-published 79218, ~30 lines off `bc-mcp/scripts/r69-run79218.ts`. **Highest-value single measurement:** open 130455 → `SaveValue "79218"` into the range field → confirm the line appears → set/confirm the isolation Test Runner → Run → read 79218's line for `MEASURED …` produced ON the client-services transport — closes link (c) on-path and records the tuple the test actually sees in one reading. **Probe 0 (ILSpy `Microsoft.Dynamics.Nav.Ncl.dll`, enumerate `NavSession` subclasses overriding `CreateNavTestService`)** is the FALLBACK if the 130455 run fails ambiguously — it discriminates capability-failure from driver-failure but proves neither seeding nor readback. Build the dedicated control action AFTER, as the batch-runner's first component, inheriting the measurement. **Design constraints the batch-runner MUST carry (Fable):** (1) one WebSocket session per BATCH + `TestIsolation=Codeunit` means each test-codeunit run's rollback also rolls back any UNCOMMITTED control-action writes since the last commit — the loop must `Commit()` after every result row and work-queue state change, or a later test's rollback silently eats earlier results (this project's empty-vs-empty signature bug); (2) the control table needs a per-invocation NONCE, load-bearing (the stale-suite discovery generalises); (3) the Cronus283 `in-flight-unknown` hang is NOT retired by this route — it can wedge the batch WebSocket mid-queue, so the design needs a hang story distinguishing "not run" from "run, unrecorded" for items after a wedge. Still a product call (R55/R57/R58): the path runs `GuiAllowed=Yes`, so verdicts carry interactive semantics; scope to fence-REFUSED tests with per-test runner provenance. (Sizing caveat: "9 of 104 files" counts FILES, not tests or mutant coverage — the lost verdict slice may differ. **AND THE FIGURE ITSELF IS NOW UNVERIFIED — see R76:** no Document Output checkout on this machine reproduces 104 test files or a `CDO Email Editor`; the largest available has 17 test codeunits / 146 tests, of which 3 files / 27 tests (18%) declare a TestPage. The finding's SHAPE holds; the number does not.) **SEED FOLLOW-UP 2026-08-01 — the corrected zero-AL route (page 130455) is ALSO bc-mcp-seed-blocked; BOTH stock test-tool pages are dead ends for seeding over bc-mcp, so the control action is REQUIRED, not optional.** Drove page 130455 over bc-mcp (`bc-mcp/scripts/r69-cmdline.ts`): it opens fine and IS an editable list (a trailing `DraftRecord…` row, editable `Codeunit ID`), but it does NOT expose the header range-filter field Fable expected — seeding needs a validated repeater-cell edit, which bc-mcp cannot commit. Overwriting an existing codeunit-header row's `Codeunit ID` to `79218` returns `success:true` with `newValue:"79218"` echoed at the control, but the record never changes (re-read still shows `80002`) — `Codeunit ID` is a primary-key/non-editable field on an existing Test Method Line, so the `SaveValue` is swallowed with no `OnValidate` cascade (two `InvokeCompleted`, `durationMs:0`, zero `PropertyChanged`); forcing a commit by moving the current row does not help. Writing to the draft row instead commits a new line with `Codeunit ID = 0` — the value never binds on insert either. So across THREE routes (130451 range-filter no-match; 130451 "Select Tests" modal not drivable; 130455 repeater edit not committing / key field), seeding a stock suite fails at the bc-mcp DRIVER level — the RUN action works, only the stateful edit needed to seed does not. **Net: the empirical result re-justifies the dedicated control-app action** (a fixed-shape action that runs one method in-session and writes to a table — no suite, no repeater edit, no modal), converging back to "build the control action" as MEASURED-necessary. Remaining non-AL alternative if a direct reading is still wanted first: SQL-seed the `Test Method Line` rows out-of-band then invoke RUN over bc-mcp (fragile — method lines are normally reflected, and raw tenant-DB writes are risky), or take the reading via bccontainerhelper's own `ClientContext` (proves Microsoft's client-services protocol per Fable's precedent, but not bc-mcp specifically). **SPIKE RESOLVED 2026-08-01 — PROVEN on-path. Link (c) is closed: a genuine bc-mcp client-services session DOES open a `TestPage`.** The control-app batch runner shipped (LethAL Control **1.0.0.11**, objects 71011 "LC Batch Queue" / 71012 "LC Batch Result" / 71013 "LC Batch Runner" codeunit / 71014 "LC Batch Runner" page, plus Control API OData actions `SeedBatchItem` / `ClearBatch` / `GetBatchResults`; published tenant-installed on Cronus281 + Cronus283). Probe `bc-mcp/scripts/r69-batch-spike.ts`: seeded `79218`/`ReportsTestPageOpen` over OData (derived port 7048, Basic auth, `company`+`tenant` — **verified working**, the assumed auth held), drove page 71014's `Run Batch` action over the native client-services WebSocket, read results back over OData. Result: **`MEASURED testpage-open=OK | GuiAllowed=Yes | ClientType=Web`** — and 79218 raises that string at its LAST line, reachable only after `ProbeList.OpenView()` AND `.Close()` both returned, so it cannot be produced by anything but a real TestPage open. **On-path is proven by the AL stack trace, not by inference:** `Test Page Probe(79218).ReportsTestPageOpen ← Test Runner - Mgt(130454).RunTests ← Test Runner - Isol. Codeunit(130450).OnRun ← Test Suite Mgt.(130456).RunAllTests ← LC Run Method(71007).RunOneMethod ← LC Batch Runner(71013).RunBatch ← LC Batch Runner(Page 71014)."Run Batch"-OnAction` — the chain bottoms out at the page action the WebSocket invoked, so the test executed inside the client-services session, through the platform's own isolation test runner, exactly as a productized batch would. **The tuple did NOT discriminate, and that vindicates Fable's correction rather than the original framing:** the client-services session reports `Yes/Web`, the SAME tuple the SignalR hub reported — so the tuple was never the mechanism, and the real question (does the concrete client-services `NavSession` subclass override `CreateNavTestService`) is now answered **YES empirically**, making **Probe 0 (ILSpy) unnecessary**. `Run Batch` returned `success=true` with **zero dialogs**, and the run took well under a second — no hang, no prompt, no quarantine. **What this does NOT prove, stated plainly:** (a) it ran **ONE** item, so nothing here exercises session reuse across many methods — `design.md` ("never batched, never reused across tests") is still an OPEN design tension, untested; (b) `Attested`/`Mutant Id`/`Target App Id` were INERT (`attested:false` with no mutant in play), so the attestation-leak fix is unmeasured; (c) the commit-per-row guarantee is written but never exercised against a real multi-row rollback; (d) the Cronus283 `in-flight-unknown` hang is NOT retired — it can still wedge the batch WebSocket mid-queue; (e) `GuiAllowed=Yes` means verdicts produced here carry INTERACTIVE semantics (an unhandled `Confirm` raises instead of defaulting) — still a product call (R55/R57/R58), unresolved. Phase 2 (productization) is gated behind a spec + `spec-adversary` pass on exactly these. **REPORT FIX LANDED 2026-08-01 (076b726) — the cheap half of R69 is done and is INDEPENDENT of the client-services route.** `packages/runner/src/testpage-unsupported.ts`: `describeTestPageUnsupported` anchors on the `CreateNavTestService` MECHANISM token rather than the generic "Specified method is not supported" prose (red-checked: broadening the anchor makes an unrelated `NavStream.Seek()` `NotSupportedException` claim a TestPage diagnosis). Surfaces as `SessionReport.testPageUnsupported`, the `tests-testpage-unsupported` caveat, a `TESTPAGE UNSUPPORTED ON THIS PATH` console section at the same prominence as `PERMISSIONS REFUSED`, and a per-mutant note. **Deliberately a SEPARATE category from R35, not another branch of it:** R35's refusal has a one-line fix in the reader's own source (`TestPermissions = Disabled`), this one has NO target-side fix — so `unsupportedCoverageNote` lets the ACTIONABLE permissions cause win when a mutant is covered by one of each (red-checked: flipping that precedence tells a reader whose problem IS fixable that nothing can be done). Diagnosis-only — never decides a verdict, never suppresses a failure. Red-checked FOUR ways (anchor, precedence, session-level set wiring, per-mutant note wiring); each went RED for its predicted reason and GREEN on restore; full suite 1443 pass / 0 fail. **Two honest limits of the fix:** (a) the per-mutant note fires only when the refused test still REPORTED coverage — a TestPage refusal that dies before executing anything reports none, so its mutants land plain `no-coverage` and only the SESSION-level caveat names the cause; attaching the reason to those individually would claim a causation that is not established (the mutant may be genuinely uncovered) and was deliberately not done. (b) The localization question is REASONED, not measured: the anchor is a .NET member name rather than English prose so it should survive a non-English server where R35's regex does not (R66), but no localized server has been probed and it must not be claimed as a property. **TASK 0 MEASURED 2026-08-01 — the gate is UNANSWERED, not failed: the probe measured an UNWIRED INSTRUMENT, not the transport's capability. (First recorded as ABANDONED; corrected the same day on review — the measurement was right, the inference was not.)** Probe `U:/Git/bc-mcp/scripts/r69-coverage-probe.ts` (copied from the proven `r69-batch-spike.ts` wiring — `odataPost`/`odataPostString` against `cfg.bc.odataUrl`, `SessionFactory`/`OpenPageOperation`/`ExecuteActionOperation` over the native client-services WebSocket), run against Cronus281 exactly as `docs/superpowers/plans/2026-08-01-r69-phase2-batch-runner.md` Task 0 specifies: two full `ClearBatch`→`SeedBatchItem`→fresh-WebSocket-session→`Run Batch`→`GetBatchResults` round trips, one method per session. **POSITIVE reading** (79218 `ReportsTestPageOpen`, which opens `TestPage "Probe List"` — an instrumented site) returned `result` = `{"name":"Test Page Probe","codeUnit":79218,"codeunitName":"Test Page Probe",...,"testResults":[{"method":"ReportsTestPageOpen","result":1,"message":"MEASURED testpage-open=OK | GuiAllowed=Yes | ClientType=Web",...}]}` — **no `coverage` key anywhere in the entry.** **NEGATIVE CONTROL reading** (79213 `ReportsSessionCapabilities` — the closest existing analog to an empty `[Test]` body; no literal empty-body test exists in `fixtures/sandbox-probes`, and none was published, per the task's constraint against publishing new AL in this task) returned the equivalent shape with `"message":"MEASURED GuiAllowed=Yes | ClientType=Web | Company=CRONUS Danmark A/S | UserId=SSHADOWS"` — also **no `coverage` key**. **Root cause, confirmed by reading the deployed AL before the live run and then corroborated by it:** `extensions/lethal-control/src/BatchRunner.Codeunit.al` codeunit 71013 `RunBatch` never calls `Codeunit "Code Coverage Mgt."`'s `StartApplicationCoverage`/`StopApplicationCoverage` — that pairing exists only in `ControlApi.Codeunit.al`'s `RunMutantWithCoverage`, a *different* OData action used exclusively by the fenced path. So on the deployed **1.0.0.11** build, `GetBatchResults`' `result` field is built solely from `Test Suite Mgt.TestResultsToJSON` and cannot carry a `coverage` array. **That is a property of THIS BUILD's `RunBatch`, not of the client-services transport** — and the distinction is the whole verdict. `RunMutantWithCoverage` is a THIN, GENERIC WRAPPER: `StartApplicationCoverage()` → run → `StopApplicationCoverage()` → attach `CoverageArray(filter)`. `Code Coverage Mgt.` is a platform codeunit and nothing in that pairing is fenced-path-specific; `RunBatch` simply never asks. **So the probe answered "the instrument was not switched on", which is not evidence about what the instrument would read.** The gate's actual question — does a client-services session RECORD per-procedure coverage, and is it clean — remains open and cannot be asked without first adding the pairing to `RunBatch`, i.e. a slice of the plan's own Task 1. **GATE: UNANSWERED. Phase B is NOT abandoned on this reading**; the plan's Task 0 is defective as written (it assumed `RunBatch` already collected coverage, which no one had checked) and must be re-run after the instrument is wired. Recording the wrong verdict here first, and correcting it, is itself the R69 pattern: a prior that looked measured and was not. Full raw console output and both verbatim JSON entries: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-0-report.md`. **TASK 0b MEASURED 2026-08-01 — the instrument is now WIRED and the gate ANSWERS CLEAN. GATE: PROCEED to Phase B.** `extensions/lethal-control/src/BatchRunner.Codeunit.al` codeunit 71013 `RunBatch` now wraps each queue item's `Runner.Run()` in `Code Coverage Mgt.`'s `StartApplicationCoverage`/`StopApplicationCoverage`, attaches `{coverage, coverageScannedRows, coverageEmittedRows}` per row via `"LC Batch Queue"."Coverage Filter"` → `ControlApi.CoverageArray`, and `GetBatchResults` unpacks those three keys onto each result row (`ControlApi.Codeunit.al`, `SeedBatchItem`/`GetBatchResults`). Published as LethAL Control **1.0.0.12**, verified `IsInstalled=True` on both Cronus281 and Cronus283. **`SeedBatchItem`'s real signature, verified against the deployed AL rather than assumed: THREE arguments — `codeunitId: Integer; method: Text; coverageFilter: Text`** — a trailing `coverageFilter` added to the prior two, not the six-argument surface a draft of this task's brief guessed at; the probe was corrected to match the real AL before calling it. `bc-mcp/scripts/r69-coverage-probe.ts` updated in place (not rewritten) and re-run against Cronus281 with `CoverageObjectIdFilter`/`coverageFilter` = `79200..79299` (`fixtures/sandbox-probes/app.json`'s own idRange — mandatory per `RunMutantWithCoverage`'s doc comment, an empty filter does not return inside 300s). **POSITIVE reading** (79214 "Rec XRec Probe Tests" / `ReportsRecVsXRecOnModify` — inserts then `Modify(true)`s a row of table 79201 "Rec XRec Probe", whose `OnModify` trigger raises, i.e. it executes a DIFFERENT in-range object than its own test codeunit): `"coverage":[{"objectType":1,"objectId":79201,"lineNo":29,"hits":1},{"objectType":5,"objectId":79214,"lineNo":27,"hits":1},{"objectType":5,"objectId":79214,"lineNo":29,"hits":1},{"objectType":5,"objectId":79214,"lineNo":30,"hits":1},{"objectType":5,"objectId":79214,"lineNo":31,"hits":1},{"objectType":5,"objectId":79214,"lineNo":32,"hits":1},{"objectType":5,"objectId":79214,"lineNo":36,"hits":1},{"objectType":5,"objectId":79214,"lineNo":37,"hits":1},{"objectType":5,"objectId":79214,"lineNo":40,"hits":1}],"coverageScannedRows":31,"coverageEmittedRows":9` — coverage correctly NAMES the table (79201, objectType 1) whose trigger the test actually executed, alongside the test codeunit's own lines (79214, objectType 5). **NEGATIVE CONTROL reading** (79213 "Session Capability Probe" / `ReportsSessionCapabilities` — same closest-analog test as Task 0, since `fixtures/sandbox-probes` still has no literal empty-body `[Test]` and none was published, per this task's constraint against publishing new AL): `"coverage":[{"objectType":5,"objectId":79213,"lineNo":34,"hits":1}],"coverageScannedRows":13,"coverageEmittedRows":1` — reproduced identically on two consecutive full round trips. **Judgement on the negative control's single row:** it is the test codeunit's OWN executed line (its `Error(...)` call, line 34 of 79213) — self-attribution of a session that genuinely ran, not leakage of unrelated activity; ZERO objects other than the running test codeunit's own id appear. This is how the filter is INTENDED to be used in Phase B — a design assumption this reading does not verify, not an established fact about an architecture that does not exist yet: mutation targets and the tests that cover them are meant to live in a target app's own idRanges, disjoint from the test codeunit's — this fixture uniquely co-locates test and "target-like" fixture objects in one idRange, so the running test's self-coverage is a fixture artifact, not evidence the mechanism would misattribute a real mutation target. **Also observed, on two consecutive full round trips:** the negative control (run second, moments after the positive) emitted NO rows for the positive reading's objects (79201/79214). **INFERRED, not proven:** consistent with `Code Coverage Mgt.`'s Start/Stop bracket not leaking across separate batch-loop iterations or separate WebSocket sessions on the same NST — but one ordered A-then-B pair is not a test of the mechanism; a leak that only appears under concurrency, under a longer queue, or in the reverse order would not show up here. **`coverageScannedRows` > 0 on BOTH readings proves collection ran** (never the `scanned=0` "instrument recorded nothing" failure mode); **`coverageEmittedRows` on the negative control names no foreign object**, satisfying the brief's actual load-bearing question (does the bracket attribute activity the test never executed) even though the literal count is 1, not 0 — the brief's stated expectation of a literal zero assumed a true empty-body test that this fixture does not have. Exact commands, full console transcripts and the corrected verdict logic (an earlier verdict branch in the probe mis-graded this exact reading as `INCONCLUSIVE` by testing `coverageEmittedRows === 0` literally instead of "no foreign object id"; caught and fixed before this row was written): `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-0b-report.md`. **TASK 7 MEASURED 2026-08-01 (live gate, all three itests) — Phase 2 shipped (Tasks 0a-6, commits 5f4660d..9aed254: batch transport, two-gate router, per-verdict runner provenance) and validated against real containers, with one structural limit found.** `itest:bcdev` and `itest:envtool` (Cronus281) reproduce the frozen **3 killed / 10 survived / 3 no-coverage** per-mutant identical to the committed baselines; queried the results DB directly, and every one of the 32 verdicts across both runs carries `runner: null` (== `"fenced"` by `RunnerKind`'s own contract) — zero `client-services` leakage, as required, since neither fixture declares a TestPage. `itest:tables` (Cronus283) reproduces the frozen **69 killed / 9 survived / 6 no-coverage** over 84 mutants, per-mutant identical to `tables.baseline.json`, also 100% `runner: fenced` — but ONLY with the pageextension unreached, i.e. unchanged from before Phase 2 existed. **Reinstating `PageExtCountsMatchingRelated` (the exact test this row's 2026-07-31 entry named) reproduced the ORIGINAL hang, not the 87 ms fast refusal — twice, deterministically, on a freshly recovered container.** First attempt: the fenced session went `in-flight-unknown` on it at baseline (`quarantined: {"reason":"baseline test in-flight-unknown running PageExtCountsMatchingRelated"}`, `killed=0 survived=0 noCoverage=0` for the WHOLE 84-mutant run), and the container's NST was found genuinely stuck `StopPending` afterward — recovery needed a Docker-level restart (`Restart-BcContainerServiceTier` itself timed out) plus `force-reset-lease`. Second attempt, same test, freshly recovered container: byte-identical outcome (the NST stayed `Running` this time, but the fence still answered `in-flight-unknown` and quarantined), recovered again with `force-reset-lease` alone. **So the code-free probe's 87 ms REFUSED finding does not generalise past a code-free page**: a page whose `SourceTable` carries real triggers/a FlowField, extended by a pageextension that WRITES a row from `OnOpenPage` (exactly `Data Main List` / `Data Main List Ext`, R30's fixture), still wedges the fenced session at baseline — confirming, not resolving, this row's own "what hung on Cronus283 is still unexplained" residual. **This also means Phase 2's router cannot rescue this shape by construction, independent of whether gate 2 would pass it**: `runSession`'s baseline-discovery loop (`packages/runner/src/orchestrator.ts`, the `for (const ref of tests)` loop preceding all mutant scheduling) runs every discovered test once on the fenced path and calls `quarantineInFlight` — latching the WHOLE session unsafe and stopping — the instant ANY one of them comes back `in-flight-unknown`, unconditionally, before gate 1's routing logic (which only pattern-matches a FAST, completed baseline failure's text) is ever reached. A baseline hang takes the entire suite down with it, not just the mutants the hanging test would have covered. The test was therefore NOT committed (reverted to the pre-existing no-test state, comment updated with this measurement) so `itest:tables` stays a working regression gate; the pageextension mutants remain `no-coverage`, unchanged. No `packages/` change was made pursuing this — closing it needs one (e.g. isolating TestPage-suspected baseline tests behind their own short deadline, run separately from the rest of baseline discovery, so one hang does not quarantine tests that never needed rescuing), which was out of Task 7's scope and is filed here rather than attempted. Full transcripts, recovery commands and the per-mutant join: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-7-report.md`.

## Superseded status

open — **capability PROVEN 2026-08-01** (client-services route measured end-to-end: seed → in-session batch loop → TestPage open → readback, `GuiAllowed=Yes`/`ClientType=Web`); **Phase 2 Task 0 gate PASSED 2026-08-01** — 1.0.0.12 wires `Code Coverage Mgt.` into `RunBatch`, and a re-measured positive/negative pair shows collection running clean (scanned>0 both readings; negative control names zero objects other than its own running test codeunit). **Phase B (control-app changes) is UNBLOCKED** per the task-0b brief's gate; the shipped Phase-3 report-naming fix (`describeTestPageUnsupported`, 076b726) remains delivered value independent of Phase B's outcome. **Phase 2 (Tasks 0a-6) SHIPPED and reviewed** (batch transport, two-gate router, per-verdict runner provenance — commits 5f4660d..9aed254). **Task 7 (live validation) measured 2026-08-01: router correctness CONFIRMED on the fenced/client-services boundary it targets** (bcdev+envtool unchanged at 3/10/3, 100% fenced, no leak) **but it structurally cannot rescue a baseline HANG (only a fast refusal)**, so the pageextension mutants this row opened stay `no-coverage` and this row stays open. **HONESTY QUALIFIER (final whole-branch review, 2026-08-01): the client-services transport is BUILT AND UNIT-TESTED BUT NOT WIRED.** `SessionConfig.routedTransport` is declared and consumed in `orchestrator.ts`, but grep across production code (everything outside `packages/runner/tests/orchestrator.test.ts`) finds NOTHING that constructs it — so `runSession` always takes the `routedTransport === undefined` branch, in every one of the live gates measured above. Task 7's clean bcdev/envtool/tables figures therefore demonstrate **NO REGRESSION** from Phase 2 landing on the fenced path that already shipped — they are NOT evidence of a working routed path in production, and must not be read as Phase 2 having been live-validated end-to-end. Two blockers must be fixed before any production code is allowed to construct `routedTransport`: R74 (routed survivors discard the attestation) and R75 (routed verdicts discard `coverageAttribution`) — see below. **GO/NO-GO THRESHOLD, WRITTEN 2026-08-02 BEFORE THE NUMBER WAS KNOWN** (this row has produced five retracted over-generalisations in two days; pre-commitment is the countermeasure, and it is honoured either way). The measurement: on the Document Output corpus at `/u/Git/DO`, under `coverageMode: "procedure"`, **the share of deployed mutants whose covering-test set is NON-EMPTY and consists ONLY of tests that declare a `TestPage`** — the exact set the whole R69 apparatus would recover, and the first R69 figure stated in the unit that matters (every earlier one was test-denominated or unreproducible; see R76). Denominator: mutants that received a verdict (killed + survived + no-coverage). **≥ 5% → R69 CONTINUES** in the order R74/R75 → R78 → wire → four-gate per-mutant join. **< 5% → R69 CLOSES** as "named, not recovered; recovery measured unprofitable", the routed path and control-app objects 71011-71014 are DELETED (a dead OData surface on customer containers is liability, not insurance; the spec plus `47fbd19` is a complete re-derivation kit), and `testpage-unsupported.ts` plus the fixture pair are KEPT. Why 5%: what remains to build is not the ~30 lines of R74/R75 but a permanent client-services surface on every customer container plus a second verdict path to keep honest across four gates; below 5% the recovered mutants move the mutation score by less than the gaps this project already accepts and names (R40 alone concedes 41% of a real app's sites), so the surface would cost more than it buys. The absolute count is recorded alongside the share for the record, but **the share is the decision variable** — one number, applied without re-argument. **MEASURED 2026-08-02: 439 of 19,081 deployable mutants = 2.30%. BELOW THRESHOLD. R69 CLOSES — named, not recovered; recovery measured unprofitable.** Full method, bounds and reproduction: `docs/measurements/README.md` §"R69's go/no-go". Corpus `U:/Git/do-rel2` (Cloud 28.4.0.0, the version the environment actually runs) through the hub on Continia environment `f19aca88`; 19 TestPage tests (18 executable), 1,197 other tests. Generous bound was 980 (5.14%); subtracting sites any other test also covers leaves 439. Every variant stays below: own-source only (excluding the 137 `.dependencies` files whose objects belong to other apps) **370 of 15,331 = 2.41%**; plus every mutant of the one page whose TestPage test is absent from the deployed test app, **2.37%**. The one test codeunit that died mid-run can only make the exclusive set look BIGGER, so the missing direction is the safe one. **Two earlier framings of this row's size were wrong and are corrected here:** R76's "9 of 104 cannot be reproduced against any checkout on this machine" is false — it reproduces exactly against `do-rel2` (R76 was reading `U:/Git/DO`, a different, 38-file checkout) — and the figure was a `grep -l` artifact regardless: 9 files MENTION `TestPage`, **5** contain a TestPage test, and per test it is 19 of 1,287 = **1.5%**.
