---
id: "R18"
title: "`envTool` + `--backend al-runner` is silently ignored"
status: "done (f616885)"
section: "backends-and-tooling"
order: 50
---

**`envTool` + `--backend al-runner` is silently ignored.** `--keep-env` and `--allow-expiring-env` are refused loudly for al-runner on the reasoning that a silent no-op is wrong; an entire configured `envTool` section being ignored deserves at least the same warning. Evidence: `packages/runner/src/cli.ts` al-runner branch.
