---
id: "R30"
title: "Mutation sites written inside `tableextension`/`pageextension` are refused"
status: "**partly done 2026-07-28 — the mechanism is in and tested; the measured value on a real project is +2 mutants.** `OBJECT_KINDS` now admits `tableextension`/`pageextension`, and an implicit `Rec`/`xRec` inside a `tableextension` — both the QUALIFIED (`Rec.TestField(...)`) and the RECEIVERLESS (`TestField(...)`) forms — resolves to the EXTENDED table via the header's `base_object` field, so rule 3's `projectDeclaresProcedureOnTable` guard applies to that table exactly as for a site written in the table itself. Red-checked three ways: removing the kinds, mis-resolving to the extension's OWN name (which looks successful and silently bypasses the shadowing guard), and removing the receiverless branch. `itest:tables` unchanged at 64/9/2 per-mutant. **The honest result: on Continia Document Output this gained 77 -> 79 `remove-testfield` and nothing else.** Handling only the qualified form gained EXACTLY ZERO — byte-identical operator totals — because DO's 31 tableextensions contain no `Rec.`-qualified calls at all; that is why the receiverless form was measured for and then added. **Second half done 2026-07-28, and it was worth 9x the first.** `buildSymbolTable` now indexes a `tableextension`'s own members (globals, procedures with their locals and parameters) for VARIABLE SCOPE, so a call on a variable DECLARED inside an extension resolves instead of being refused as unresolvable — the shape DO's extension code overwhelmingly uses. Indexed under a NAMESPACED key (`extensionScopeKey`), never the bare extension name, so `resolveProcedure(\"My Ext\", ...)` keeps answering `null`: scope and callability are different questions, and the engine test asserting the second is deliberately unchanged rather than overruled. **Measured on Document Output, Tier-2 totals 961 -> 979 (+18):** `remove-setrange` 672->682, `remove-testfield` 77->82, `remove-calcfields` 202->205 — against +2 for the implicit-`Rec` half alone. Red-checked; `itest:tables` 64/9/2 and `itest:bcdev` 3/10/3 both unchanged per-mutant after an engine-level change. `pageextension` stays refused by design: its implicit record is the extended PAGE's `SourceTable`, declared in an object the project usually cannot see, and guessing would CLAIM sites wrongly. **CLOSED 2026-07-31 — the third half shipped and the mechanism is now LIVE-GATED.** (a) `buildSymbolTable` indexed a `pageextension`'s members NOWHERE (the node matched neither the tableextension branch nor the object-kind map and fell through the loop), so every call on a variable declared inside one was refused as unresolvable. Both extension kinds are now indexed for variable scope under a KIND-namespaced key (`extensionScopeKey(kind, name)` — AL lets a `tableextension` and a `pageextension` share a name, and one namespace for both lets each resolve the other's variables). `tableExtensions` deliberately still holds table extensions only: it is the rule-3 shadowing guard's input, keyed on the extended TABLE, and a page name compared to a table name can only match by coincidence. **Measured before it was built, not after** (`scripts/probe-r30-pageext.ts`, Continia Document Output Cloud, 554 files): **18** Tier-2-shaped calls on a record declared inside a `pageextension`, against **ZERO** on a pageextension's implicit `Rec`, and **zero of 93** pageextensions extend a page the project declares — so the `SourceTable` needed to resolve `Rec` is not available even in principle, and that refusal is now measured rather than argued. The probe is calibrated: its `tableextension` count (17) reproduces the +18 the second half actually gained. (b) **The fixture grew its first extension objects, so extension support is no longer unit-only.** `fixtures/sandbox-data` gains `tableextension \"Data Main Ext\"` (implicit-`Rec` `TestField` + a `SetRange` on an extension-scoped local, both reached from a test because a tableextension's public procedures are callable on a variable of the extended table's type) and a code-free `page` + `pageextension` pair. `itest:tables` moved **64/9/2 over 75 deployed → 69/9/6 over 84** (93 raw specs): **+5 killed, all five tableextension mutants** — the first Tier-2 extension mutants any gate has executed — and **+4 no-coverage** for the pageextension. Per-mutant join against the previous baseline: **0 changed verdicts, 0 missing keys, 9 new keys**, so the whole delta is new sites. `itest:bcdev` unchanged at 3/10/3. Red-checked three ways (drop the pageextension parse branch, drop the consumer's scope branch, collapse the kind namespace) — and the first namespacing test PASSED under its mutation for the wrong reason (the collision destroys the procedure entry wholesale, so the receiver became unresolvable, which is also `false`); it was rewritten to assert `true` with the declaration order reversed, and then went red. **What is NOT proven: the pageextension half is claimed, deployed and never executed.** A pageextension's code is reachable only through a `TestPage`, and a TestPage HANGS the fenced session (R69) — measured, not assumed: the test was written, published and run, and the session quarantined at baseline scoring nothing. Also unproven for either kind: nothing here exercises a `pageextension` on a real customer project end to end."
section: "backends-and-tooling"
order: 170
---

**Mutation sites written inside `tableextension`/`pageextension` are refused.** `OBJECT_KINDS` (`packages/builtin-tier2/src/receiver.ts`) omits extension objects, so no Tier-2 operator claims anything declared in one. Safe direction — a missed site costs one operator's signal — but a great deal of real BC code lives in extensions, so the practical coverage loss on a customer project could be large. (The *shadowing* half of this was the unsafe direction and is fixed: `resolveTable` now scans `tableextension` objects targeting the resolved table.)
