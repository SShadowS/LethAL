---
id: "R6"
title: "A file declaring two AL objects is refused outright"
status: "done (Wave 1 Stream C — per-object attribution for all-injectable files)"
section: "product-gaps"
order: 40
---

**A file declaring two AL objects is refused outright.** Legal AL, rare in practice. Every layer below `objectHeaderOf` assumes one object per file — the real fix is per-object attribution, not a looser check. Refusal is correct until then. Evidence: `packages/schemata/src/project.ts`, commit `81c1e96`.
