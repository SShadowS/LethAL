---
id: "R12"
title: "Live proof of a dedup collision"
status: "done (branch `tier2-phase1`) — the Phase-1 fixture fires it for real: **6** Tier-1 `lethal.void-method-call` specs lose the §3.2 precedence to a Tier-2 deletion at the same site (81 raw specs → 75 deployed), measured offline and confirmed by the live gate's own 63+10+2=75. No throwaway operator involved."
section: "next-up"
order: 20
---

**Live proof of a dedup collision.** `dedupeSpecs` is unit-proven and runs on the live path, but no two Tier-1 operators claim the same site, so the collision branch has never fired against a real server. Do it at Phase 1's gate, with a real Tier-1/Tier-2 overlap — do not fake one with a throwaway duplicate operator.
