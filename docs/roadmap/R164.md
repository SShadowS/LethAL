---
id: "R164"
title: "290 loop-exit negations score only with an opt-in flag that ends a session on the user's server"
section: "product-gaps"
order: 875
status: "done (`12b57b2`) — `lethal.loop-truncate` (Tier 1, 1.0.0) ships WITH the cession it needs: `negate-conditional` refuses a `repeat` exit condition. 334 sites on `do-rel2/Cloud`, exact-span collisions 331 -> 5, net +8 mutants and 326 potential strandings removed. Both stages pre-committed and every verdict matched; `itest:hang` re-frozen at 22 mutants with 4 non-terminating, gift-card re-frozen at 60 with the operator swapped in place"
---

`negate-conditional` claims the `until` condition of a `repeat` loop. On the canonical BC shape the
mutant does not terminate:

```al
repeat Process(Rec) until Rec.Next() = 0;    // before
repeat Process(Rec) until Rec.Next() <> 0;   // after: on a ONE-ROW set, Next() returns 0 forever
```

**Measured on `do-rel2/Cloud`:** 334 `repeat` loops, of which **290 are exactly `<rec>.Next() = 0`**
and every one of them is claimed by `negate-conditional` (326 of the 334 conditions are claimed in
total: 303 comparisons plus 23 logical expressions). A test fixture with a single row is the common
case, so a large share of those 290 hang rather than finish.

**This is not new and it is not a wrong verdict.** R53 is exactly this mutant, found as M0013 on
Document Output, and it is CLOSED: with `--stop-hung-sessions` the run scores it `timeout-killed`,
proven live on `fixtures/sandbox-hang` and gated by `itest:hang`. Design §6.7 is explicit that a
timeout counts as killed, since the mutation caused observable misbehaviour. So the mutant is
neither a false kill nor unhandled.

**What IS unfiled is the cost on the default path.** `--stop-hung-sessions` is off by default,
deliberately, because it ends a BC session on the user's server, and
`docs/using-lethal-from-an-agent.md` tells a caller not to turn it on unasked. Without it each of
those mutants strands its tier and blocks every mutant behind it. R53's own limits note adds a
second case: on a hosted topology the proxy ceiling (R44 measured 362 s) can kill the held request
before BC answers, so the feature may be structurally unable to score them there.

**The candidate.** A terminating mutant at the same site, `until <cond>` rewritten to `until true`,
so the body runs exactly once. A SURVIVOR is then a precise statement: no test drives this loop over
more than one row, which is the most common weakness in a BC suite. It scores on the default path,
needs no session kill, and cannot hang.

**It is a precedence question, not just a new operator.** `negate-conditional` claims the site first,
so taking it needs a §3.2-style cession at `until` position, and that has to be designed rather than
bolted on. First measurement before any of it: how many `negate-conditional` mutants sit at `until`
position in the tables fixture, and how each one actually died.

Proposed by the Fable subagent 2026-08-19. Its framing, "a false kill with no screen", is wrong on
both halves; the argument survives restated as a default-path cost.

---

**First measurement done 2026-08-19, and it changes what building this would cost.**

This row asked for one thing before any code: "how many `negate-conditional` mutants sit at `until`
position in the tables fixture, and how each one died." Censused across every fixture:

| fixture | `repeat` loops | until-condition claimed by `negate-conditional` |
| --- | ---: | --- |
| `fixtures/sandbox-data` (`itest:tables`) | **0** | none |
| `fixtures/sandbox-app` (`itest:bcdev`, `itest:envtool`, `itest:alrunner`) | **0** | none |
| `fixtures/sandbox-hang` (`itest:hang`) | 1 | **no** — `until Counter >= Limit` is a boundary, which `conditional-boundary` claims |
| `examples/gift-card` (the demo) | 1 | **yes** — `until GiftCard.Next() = 0`, the canonical shape |

So exactly ONE canonical site exists in everything this project runs, and it is in
`BlockExpiredCards` — the nightly job no test calls. Its mutant is recorded **`no-coverage`** in the
frozen demo campaign, alongside the other six there.

**Therefore no gate has ever EXECUTED an until-position `negate-conditional` mutant.** The
non-termination this row is about is measured on a real corpus (290 canonical sites on
`do-rel2/Cloud`) and has never once run in this repository through this operator. `itest:hang`
exercises non-termination through DIFFERENT operators by design — `void-method-call` on the
advancing call and `empty-block` on the advancing procedure's body — which R53 chose deliberately;
it does not cover the negated-exit shape at all.

**What that means for building `loop-truncate`.** The alternative operator cannot be measured on any
existing fixture: there is no COVERED `repeat` loop anywhere to claim. An arm would have to be added,
and adding one to `sandbox-data` would mean deliberately introducing a hang-capable site into a
SCORED gate — which is precisely the situation `itest:hang` was created to isolate, so that the other
gates never pay for it. The arm therefore belongs in `fixtures/sandbox-hang`, whose gate already runs
both legs (with and without `--stop-hung-sessions`) and already asserts the stranded-versus-scored
distinction this row turns on.

That is a bigger and better-shaped job than "add an operator", and it is now specified rather than
guessed. The precedence question against `negate-conditional` is unchanged and still has to be
designed §3.2-style before any of it.

---

**BUILT 2026-08-26.** Design:
`docs/superpowers/specs/2026-08-26-r164-loop-truncate-design.md`. Verdicts:
`docs/superpowers/specs/2026-08-26-r164-loop-truncate-precommitment.md`.

**The design found something that changed it, before any code.** This row assumed the precedence
question was a §3.2-style cession to be designed. It is worse than that: §3.2 **cannot arbitrate
here at all**. `dedupeSpecs` keys on the replacement TEXT as well as the span, so `Rec.Next() <> 0`
and `true` at one span are two identities and BOTH ship. Making the new operator Tier 2 would have
changed nothing, and the operator alone would have ADDED a mutant beside the hanging one and fixed
nothing. The cession had to be coded into `negate-conditional`, which also makes it consistent with
`shift-integer` and `negate-guard`, both of which already refuse loop conditions for this reason.

**The hazard is now MEASURED in this repository, which it never had been.** This row's own first
measurement recorded that no gate had ever EXECUTED an until-position `negate-conditional` mutant.
The build ran in two stages deliberately so that it would: stage 1 registered `loop-truncate` and
left `negate-conditional` alone, and the arm's mutant came back **`timeout-killed`**. Stage 2 landed
the cession and that row is gone. Ceding 326 corpus mutants on reasoning alone would have been the
refusal condition the design named.

**The arm is `NextRow` / `WalkOneRow` in `fixtures/sandbox-hang`**, exactly where this row said it
belonged. It reproduces BC's `Rec.Next()` CONTRACT (1 while rows remain, 0 once exhausted, 0 forever
after) with no table behind it, so the canonical hang is deterministic. `HangLogic`'s original arm
avoided a real recordset for the same reason, and the reasoning applied unchanged.

**Two live cession witnesses, and the second is the better one.** gift-card's `BlockExpiredCards`
carries the canonical shape on a real recordset. Its re-run is identical in every way a total could
measure (60 mutants, 69.4%) and the per-mutant diff shows one identity swapping operator at the same
`astSubtreeHash`. A gate comparing counts would have called that "no change".

**Scope, stated rather than implied.** `repeat` only: `while <cond>` has no run-once rewrite
(`while false` runs the body zero times, a different mutation), and `for`/`foreach` carry no boolean
exit condition. 92 corpus loops are deliberately untouched.

**What is left is filed as [[R173]]**, with numbers: 19 claims at `repeat` exit conditions from three
other operators, 37 at `while` conditions, and 1 nested comparison. The cession is narrow and
exact-span on purpose, because widening it would delete working, terminating, killed mutants,
`itest:hang`'s M0005 among them.
