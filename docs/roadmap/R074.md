---
id: "R74"
title: "BLOCKS WIRING `routedTransport`: routed survivors discard the attestation, and the identity gate is vacuous…"
status: "**MOOT (2026-08-02) — R69 closed, the routed path is DELETED.** This blocker described a false-survive hazard on code that no longer exists: `batch-transport.ts`, `testpage-router.ts`, the routed orchestrator helpers and the R74/R75 tripwire were removed once the recovery was measured at 2.30% (see R69). Kept as a row rather than deleted because the UNDERLYING trap is general and will be met again by anything that scores a verdict from a second execution path: `LC Control State` sets `ObservedIdentityMismatch` only INSIDE its `ObservedAny := true` branch, so a fail-closed `identityMismatch !== false` check passes TRIVIALLY when no instrumented site ever executed. Nothing compared to nothing — this project's signature bug."
section: "backends-and-tooling"
order: 570
---

**BLOCKS WIRING `routedTransport`: routed survivors discard the attestation, and the identity gate is vacuous without it.** `runOneBatchMethod` returns `attested` (`batch-transport.ts`); `callRouted` (`orchestrator.ts`) never reads it; the routed `record(...)` passes `guardObserved: undefined` (`orchestrator.ts`). `report.ts`'s `guardObserved` field documents `guardObserved === false` as "DECISIVE and damning for a survivor" while ABSENT means "not measured". Worse, `ControlState.Codeunit.al` sets `ObservedIdentityMismatch` ONLY inside the `ObservedAny := true` branch, so `batch-transport.ts`'s fail-closed `identityMismatch !== false` check passes trivially when no guard ever fired — nothing compared to nothing, this project's signature bug. Failing sequence: mutant M is object-attributed to routed test T; T passes on client-services without ever reaching M's site; `attested=false`, `identityMismatch=false` → M recorded **`survived`**, counted in `mutationScore`, and EXCLUDED from the `UNEXERCISED SURVIVORS` callout (`report.ts`) because `guardObserved` is absent rather than false. A false survive. Filed, not fixed — do not implement without a design pass; blocks any production code constructing `routedTransport`.
