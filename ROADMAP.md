# LethAL Roadmap — open work and known limitations

Living list of everything known-but-not-done: planned work, correctness risks we have measured and
not yet closed, and product gaps a real user would hit. Session ledgers under `.superpowers/` are
scratch and get archived; **this file is the durable record.**

## How to use this file

- **Add an item the moment it is discovered**, even mid-task — one line of "what breaks, for whom"
  beats a perfect write-up later. Items get a stable `R<n>` id; never renumber, never reuse an id.
- **Every item names its evidence** — a file, a commit, a measured result. An item with no evidence
  pointer is a rumour, not a roadmap entry.
- **Status:** `open` · `in progress` · `blocked (<on what>)` · `done (<commit>)`.
- **Closing an item:** mark it `done` with the commit, leave it in place for one release cycle, then
  delete it. Deleted items live on in git history.
- **Do not** duplicate what the code or `design.md` already says. This file records what is *missing*
  or *wrong*, not how the system works.

Priority is deliberately not a column: the ordering inside each section is the priority.

---

## Next up

| id | item | status |
|---|---|---|
| **R10** | **Tier-2 Phase 1 — the first real Tier-2 operators.** Spec: `docs/superpowers/specs/2026-07-25-tier2-mutation-operators-design.md`. Phase 0 (merged 2026-07-25) proved table triggers mutate, execute and kill on a live server; Phase 1 writes the operators that target them. | done (branch `tier2-phase1`, final fixes `d0070db`) — four operators shipped (`RemoveTestField`, `RemoveSetRange`, `RemoveCalcFields`, `SwapModifyFlag`) over the shared `claimsRecordMethod` receiver predicate; `itest:tables` frozen at **63 killed / 10 survived / 2 no-coverage** across 75 deployed mutants (81 raw specs), `untargetedTriggerCount` 0. `tables.baseline.json` re-recorded and committed (`7005495`), then PROVEN to compare on a subsequent run — the previous file could never match itself (see R29). |
| **R12** | **Live proof of a dedup collision.** `dedupeSpecs` is unit-proven and runs on the live path, but no two Tier-1 operators claim the same site, so the collision branch has never fired against a real server. Do it at Phase 1's gate, with a real Tier-1/Tier-2 overlap — do not fake one with a throwaway duplicate operator. | done (branch `tier2-phase1`) — the Phase-1 fixture fires it for real: **6** Tier-1 `lethal.void-method-call` specs lose the §3.2 precedence to a Tier-2 deletion at the same site (81 raw specs → 75 deployed), measured offline and confirmed by the live gate's own 63+10+2=75. No throwaway operator involved. |
| **R11** | **`tierRank` has no tier-3 rank.** A tier-3 operator colliding with a tier-1 one hits "cannot order" and throws instead of resolving by precedence. Fix when tier 3 becomes real. | **done (`d2b3236`) — by deciding it, not by adding the rank.** R13 measured that tier 3 does not become real, so the two-tier ordering is CORRECT rather than incomplete and the speculative fix would have been wrong: a third rank with no operator behind it is an ordering nobody has reasoned about, sitting ready to resolve a collision silently. The collision it guards is concrete — the one Tier-3 candidate that fits the existing emit path (`IsolationLevelSwap` deleting `LockTable()`) emits a byte-identical identity to the `void-method-call` mutant already shipped at **25** sites of Continia Document Output, so registering it as tier 3 aborts those sessions loudly, which is the designed behaviour. Closed the way R70 says a premise must be closed: as an EXECUTABLE assertion, not a comment — `packages/schemata/tests/dedup.test.ts` "refuses to order a REGISTERED tier-3 operator against a tier-1 one", red-checked (adding `tier === 3 ? 3` to `tierRank` fails exactly that test and nothing else), plus the reason in `tierRank`'s own comment. Adding the rank is only correct together with the first tier-3 operator that ships — never before it. |
| **R13** | **Tier-3 operators.** **CLOSED 2026-08-02 — designed, measured, refused on COST, and the category itself dissolved.** Decision: `docs/superpowers/specs/2026-08-02-r13-tier3-decision.md`; measurements: `docs/measurements/README.md` §R13; probe: `scripts/r13-probe/`. Thresholds were pre-committed at `349901a` BEFORE any candidate was counted (calibrated on the shipped set on `do-rel2/Cloud`, 19,132 mutants, smallest shipped operator `swap-modify-flag` = 13): (a) fits the existing guarded emit path -> >=13 mutants; (b) needs a new activation mechanism -> >=5% (957); (c) measured unkillable -> never. **The premise did not survive.** `design.md` called all three operators "object metadata" needing "a distinct emit path"; measured against the grammar, only ONE is declarative. `IsolationLevelSwap` targets `call_expression`/`assignment_statement` in statement position — the SAME path Tier 2 uses — and 25 of its 36 sites ALREADY carry a shipped `lethal.void-method-call` mutant, so its marginal footprint is **11**, below bar (a): refused on the cost bar alone. `EventPublisherSignature`’s signature form is emittable only as a duplicate publisher that no subscriber binds to (equivalent by construction); the observable version is an argument swap at the RAISE site, which is a different, non-event-specific operator — re-filed as **R82**. `PermissionReduce` is the only true metadata operator: 38 `Permissions` properties / **423** `tabledata` grants (2.21%) against bar (b)’s 957 — refused on cost. **It is NOT refused as worthless: it is KILLABLE, measured.** The first draft of the decision claimed "unkillable in both of the two modes a suite can be in"; an adversarial review found a third mode in the censused project itself (a test declaring `TestPermissions = Disabled` that then calls `LibraryLowerPermissions.SetO365Basic()`), and arm A8 measured that reducing `rm`->`r` on a callee turns a succeeding write into `Sorry, the current permissions prevented the action.` — with a direct-write control proving the lowering took effect. Reachability bounds the value: **10 of 1,290 tests (0.78%), in 2 of 104 test files**. Reopen if a project shows >=957 grant sites or permission-lowering stops being <1% of a suite. | done (`d2b3236`) |
| **R15** | **Custom environment tool support** — run LethAL against environments owned by an external CLI (first case: Continia's `continia.exe`), described purely in config: tool path plus command templates for create / resolve / symbols / publish / delete. The tool provisions; LethAL's fenced `RunMutant` path still decides every verdict. Spec: `docs/superpowers/specs/2026-07-26-custom-env-tool-design.md`. **Implementation is complete and reviewed**: `packages/runner/src/{env-tool,env-tool-session,env-tool-publisher}.ts`, the `cli.ts` wiring (`resolveEnvToolSession`, `--keep-env`, `--allow-expiring-env`), and the live-gate script `packages/runner/itest/envtool.itest.ts` are all committed. Task 1's live probe against a real Continia environment measured `coverage: "procedure"` (full fidelity — see `fixtures/README.md` §"Running against an external environment tool"). **The live gate itself has not been run** — see R16. | done (gate PASSED 2026-07-26, baseline frozen) |
| **R16** | **Run the `envtool` live gate and record its baseline.** `bun run itest:envtool` (env-gated `LETHAL_ITEST_ENVTOOL=1`) is committed and wired through the real CLI seams (`resolveEnvToolSession`, `buildBackend`, `leaseSessionFor`, `resourceIdentityFor`) but has never actually been executed against a real environment — no `packages/runner/itest/envtool.baseline.json` exists yet. Needs a human decision on which Continia environment to point it at (config: gitignored `fixtures/sandbox-app/lethal.config.envtool.json`, `.env` with `CONTINIA_API_TOKEN`/`CONTINIA_ENV_ID`) and one live run to record the per-mutant baseline, the way `itest:bcdev` and `itest:tables` already did. Expected 3 killed / 10 survived / 3 no-coverage — carried over from the bcdev gate on the identical fixture, on the strength of Task 1's coverage-mode probe, but not yet confirmed by an actual `itest:envtool` run. | done (2026-07-26 — 3 killed / 10 survived / 3 no-coverage, matching the container gate) |

## Correctness risks (measured, not closed)

| id | item | status |
|---|---|---|
| **R1** | ~~Fenced-path writes fail~~ — **REFUTED 2026-07-26, twice over.** The entry first blamed a missing user permission (wrong: the OData user is SUPER), then Microsoft's Permissions Mock on the fenced path (also wrong). Measured by A/B on one property — two probe codeunits identical except `TestPermissions`, same app, same tables, same server, mock running in both: **omitted (→ Restrictive) = writes refused; `TestPermissions = Disabled` = writes succeed.** It is not the invocation path either — `continia test run` reaches `Test Runner - Mgt` 130454 with the mock started exactly as `RunMutant` does, and a restrictive codeunit is refused there too. The real rule: **a test codeunit that does not declare `TestPermissions = Disabled` cannot write, on any runner.** Real suites declare it (Continia Document Output: **76 of 76** test codeunits, and `InherentPermissions` on zero tables — this row said "77 of 77" until R13 recounted: the 77th object matching `Subtype = Test` is a `TestRunner`, which declares no `TestPermissions` at all). Our fixture did not, and the `InherentPermissions = RIMD` workaround was hiding that. Fixed in `769f667`: property declared, workaround removed from both tables, `itest:tables` passes per-mutant at the frozen 3/2/2 with the `OnInsert` mutant scoring. **Nothing blocks Tier-2 trigger operators.** Credit: the user challenged this from their own suite. | done (refuted — the limitation did not exist) |
| **R2** | **Single-tenant containers only, unenforced.** The harness reports `tenantCountReachable:false` — AL cannot enumerate tenants from an extension — so the 5C-B1 lease cannot fence a second tenant, and app publication is service-instance-wide regardless. Verify single-tenancy out of band (`Get-BcContainerTenants`). Evidence: `fixtures/README.md` §"Single-tenant containers only". | done (b2339d3 — warns once per process; limitation stated in fixtures/README.md) |
| **R94** | **al-runner v2 turns a hung mutant into a KILL.** v1 reported a runner-enforced timeout as `status: "fail"` with the text `Test exceeded <n>s timeout`, which `RUNNER_TIMEOUT_MESSAGE` (`packages/runner/src/al-runner-backend.ts:24`) matches to produce `outcome: "timeout"`. v2 reports it as `status: "error"` with `TIMEOUT after <n>s` (upstream `AlRunner/TestExecutor.cs:292-299`). The regex misses, the status is not `pass`, so `run()` returns `fail` — and a mutant that merely hung is recorded **killed**. A false kill, the direction this project treats as unacceptable, and it is invisible in aggregate counts. Also note the budget is no longer a flag: `--test-timeout` is gone and the timeout is `AL_RUNNER_TEST_TIMEOUT_SEC` (env), default 60 s (`TestExecutor.cs:30,336`), so the deliberate "halve the client deadline so the runner's timer fires first" reasoning at `al-runner-backend.ts:257-267` has nothing to set. Measured 2026-08-05 against a local build of upstream `main` + PR #1657. | open — blocked on R93 |
| **R95** | **v2 re-meaned exit codes 2 and 3, and our decode swallows the dangerous one.** `OneShotTransport` maps exit 2 to `kind:"skip"` and exit 3 to `kind:"error"` (`packages/runner/src/al-runner-transport.ts:74-76`). In v2, **2 = a bundle could not execute (process-level error)** and **3 = a bundle could not compile** (`al-runner --help`, EXECUTION section). So a genuine execution failure now becomes a silently skipped mutant with no verdict and no error, while a compile failure — the ordinary "this mutant does not build" case — is escalated to a hard error. The whole-session case is contained by the baseline-green gate (measured: every mutant came back `error note=no green baseline tests` rather than a plausible score), but a mid-run exec failure on SOME mutants is not: those would be skipped under a green baseline. Fix the mapping with the decode work in R93, and red-check that an exit-2 run cannot produce a scored mutant. | open — blocked on R93 |
| **R96** | **`--test-isolation method` silently stopped meaning per-method isolation.** We pass `method` and comment that it "forces the advertised full-reset capability" (`packages/runner/src/al-runner-transport.ts:51-54`), and `capabilities()` advertises `isolation: "full-reset"` (`al-runner-backend.ts:117`). v2 accepts `method` only as a **v1 alias for `codeunit`** — state shared inside a codeunit, reset between codeunits (upstream `AlRunner/Program.cs:296-299`). Real per-test reset is `--isolation test`. On v2 the flag is accepted, nothing warns, and one test's residue can decide the next test's verdict inside the same codeunit while the report still claims full reset. Server mode inherits the daemon's own CLI flags (one shared `TestExecutor`, `Program.cs:620`), so `--server --isolation test` is the fix there too. | open — blocked on R93 |
| **R8** | **al-runner drops a table global var written by a trigger.** After `Validate("No.", 'B2')` the fixture's `TouchCount()` returns 0 there, though the trigger body demonstrably ran. Root-caused 2026-07-26 (three throwaway diagnostic probes against al-runner.exe directly): the write is real — a nested `TouchCount()` call from INSIDE the trigger's own frame reads 1 — and the field write (`"No."` itself) demonstrably survives the same call boundary; only the non-field global does not survive the return from `Validate()` back into a later, separate top-level dispatch on the same record variable. Third-party, upstream — documented rather than fixed. Detectable: al-runner is now startup-canaried for it (`packages/runner/src/al-runner-canary.ts`, `runFromCli`'s al-runner branch), so every session states whether the defect reproduces on the binary actually configured, rather than repeating a static 2026-07-25 claim. Any mutant whose only observable effect is table-global state may still be misjudged on that backend when the canary confirms it. Evidence: `fixtures/README.md` §Tier-2 Phase 0. | done (Wave 1 Stream D — root-caused, upstream, detectable by canary) |

## Product gaps a real project hits

| id | item | status |
|---|---|---|
| **R3** | **Selector object ids are hardcoded.** `DEFAULT_SELECTOR_IDS` in `packages/runner/src/cli.ts` pins 79197–79199 with no flag and no config key. A project whose `idRanges` exclude those three cannot be instrumented at all (`alc` rejects with AL0297). `--selector-id`/`--control-id`/`--table-id` flags and a `lethal.config.json` `selectorIds` section now override each id independently (CLI > config file > `DEFAULT_SELECTOR_IDS`, unchanged default). `@lethal/schemata`'s `parseIdRanges`/`validateSelectorIds` (`id-ranges.ts`) validate the resolved ids against the target app.json's declared `idRanges`, against each other (pairwise distinct), and against codeunit ids the target already declares — before any `alc` compile is attempted, naming the offending id and range. | done (Wave 1 Stream B — CLI flags + config, validated against app.json idRanges) |
| **R4** | **Two instrumented projects cannot share one BC container** — a direct consequence of R3: publishing a second one fails with *"The application object of type 'CodeUnit' with the ID '79197' is defined in multiple apps"*. Multi-project users hit this immediately. Closed by fixing R3. Live-verified on Cronus281: `fixtures/sandbox-app` instrumented+published with default ids (score unchanged: 3 killed/10 survived/3 no-coverage) and `fixtures/sandbox-data` instrumented+published with distinct custom ids (79309/79308/79307, score 3 killed/2 survived/2 no-coverage) — both apps simultaneously `IsInstalled: True` on the same container, no AL0297/id-collision error. | done (Wave 1 Stream B — proven live on Cronus281, two instrumented projects side by side) |
| **R5** | **The report does not say how much of the project was skipped.** Object kinds the selector var cannot be injected into (page, report, query, xmlport) are dropped at spec generation with a stderr warning only, so a page-heavy project can get a confident-looking score computed over a small fraction of its code. Wants an explicit "N files not instrumented" field on the report itself. | done (Wave 1 Stream C — SessionReport.notInstrumented, into --out and --dry-run) |
| **R6** | **A file declaring two AL objects is refused outright.** Legal AL, rare in practice. Every layer below `objectHeaderOf` assumes one object per file — the real fix is per-object attribution, not a looser check. Refusal is correct until then. Evidence: `packages/schemata/src/project.ts`, commit `81c1e96`. | done (Wave 1 Stream C — per-object attribution for all-injectable files) |
| **R49** | **`lethal --help` does not exist, and it is the first command a new user runs.** `parseCliConfig` (`packages/runner/src/cli.ts`) drives `node:util`'s `parseArgs` in strict mode with no `help` or `version` option, so `--help` exits 1 with a raw `TypeError: Unknown option '--help'` and a stack trace into the bundled binary. A bare `lethal` is better (`unknown subcommand: got none, expected one of: run, clear-quarantine, force-reset-lease`) but names no flags. Now user-visible rather than internal: `v0.1.0-alpha.1` ships a downloadable binary whose only discovery surface is `fixtures/README.md`, which a downloader does not have. There is no `--version` either, so a bug report cannot state which build it came from — the version exists only in the release filename. Evidence: measured 2026-07-27 against `build/lethal-0.1.0-alpha.1-windows-x64.exe`; `docs/releasing.md` §"Known gaps in the distribution path". | done — `--help`/`-h`, `--version`/`-V`, and a bare `lethal`, all intercepted BEFORE `parseArgs` (strict mode is what turned `--help` into a `TypeError`). The version is a STATIC `import ... with { type: "json" }` of the root `package.json`, not a runtime read — R50 measured that a runtime-computed path resolves against Bun's virtual root under `--compile` and fails, so the same rule applies to any bundled asset. Usage is written for someone holding only the binary: it names the quarantine exit code (3, not a crash) and marks `--tests-only` as the one narrowing that CAN change a verdict. A test pins the help text against the flag set `parseCliConfig` accepts, so a new flag cannot ship undocumented. Verified on the compiled 102.7 MiB Windows binary run from a foreign cwd: `--version` prints 0.1.0-alpha.1, `--help` exits 0, and `run --dry-run` still parses AL (16 sites on `fixtures/sandbox-app`). |
| **R50** | **Neither wasm asset survived `bun build --compile`, so the standalone binary could not parse a single line of AL.** `parser.ts` resolved the vendored grammar relative to `import.meta.url`, which under `--compile` is Bun's virtual root — and web-tree-sitter's own emscripten runtime resolved the same way, failing FIRST: `failed to asynchronously prepare wasm: Error: ENOENT ... open 'B:\~BUN\root\tree-sitter.wasm'`. The unit suite is structurally blind to this (it runs interpreted, where the relative resolve is correct), so nothing would have caught it before a user did. Note it is TWO assets, not one — `tree-sitter.wasm` (web-tree-sitter's runtime, 205 KB) and `tree-sitter-al.wasm` (our grammar, 7.9 MB, R14). | done — both now reached via Bun's `file` loader (`import ... with { type: "file" }`), which is what makes `bun build` embed them at all; a runtime-computed path is invisible to the bundler. `Parser.init`'s emscripten `locateFile` hook redirects the runtime wasm. Verified by running the compiled binary from a foreign cwd: `lethal run --project fixtures/sandbox-app --dry-run` output is byte-identical (md5 `dad395b2…`) to the same command under `bun`. **Any new non-TS runtime asset must be imported the same way** — see `docs/releasing.md` §"Runtime assets". |
| **R100** | **al-runner v2 makes BC artifacts a hard local prerequisite, and rejects a symbols-only `.alpackages`.** v1 compiled against symbols and stubbed the rest, so `--backend al-runner` needed nothing but the binary. v2 links real MS DLLs: it needs a service-tier artifact set locally (measured: 500 DLLs / 335 MB for 28.1.49838.50794) and **code-bearing** MS platform apps for any project that actually depends on them. It never downloads silently — `al-runner provision` or `--auto-provision` is an explicit step. Measured 2026-08-05: pointing `--package-cache` at `fixtures/sandbox-app/.alpackages` aborts with a provisioning-gap message and exit 2, because those MS apps are symbols-only; the same is true of a bccontainerhelper `Applications.<country>` folder, since v2 pulls platform apps from the w1 artifact's `Extensions/`. Our own fixture happens to resolve 0 dependencies and runs without any cache, so the fixture gates will NOT surface this — a real project will, on its first run. Whatever we ship must say which artifacts are missing and name the provisioning command, not fail as "al-runner not runnable". | open — blocked on R93 |
| **R66** | **The `TestPermissions` refusal detector is English-only.** `PERMISSIONS_REFUSAL_RE` (`packages/runner/src/permission-canary.ts`) anchors on BC's English clause "current permissions prevented the action", so on a non-English server every permissions refusal is a silent miss: the tests appear only under `unsupportedTests`, `SessionReport.permissionsRefused` is absent, and the reader is sent to debug tests whose fix is one property. Direction is safe (a miss, never a wrong answer) and the limitation is now TESTED rather than assumed — `packages/runner/tests/permissions-refused.test.ts` pins both the English hit and the Danish miss. Closing it needs a language-independent signal MEASURED against a localized server, not reasoned about: candidates are AL-side `GetLastErrorCode()` inside `RunMutant` (needs an AL change, and a protocol bump if it becomes a new field) and the structural `(TableData <id> <name> <op>: <suite>)` parenthetical, whose tokens are AL keywords rather than prose — the second is cheap but unproven, and a false positive here would tell a user to add a property they already have. Discovered while closing R35. | **UNBLOCKED 2026-07-31 — the premise was wrong and BOTH candidate signals are now measured.** The row said closing this needs "a language-independent signal MEASURED against a localized BC server". No localized server is needed: BC selects error-message resources by SESSION LANGUAGE, and AL sets that from inside a test body with `GlobalLanguage(1030)`. The existing DK containers already carry the Danish resources. Measured on Cronus281 through the fenced path, one session, ONLY the language differing (`fixtures/sandbox-probes/src/LangRefusalProbe.Codeunit.al` + `LangRefusalRunner.Codeunit.al`, a codeunit that deliberately OMITS `TestPermissions = Disabled` so its write is genuinely refused): **English (1033)** — `code=[DB:ClientInsertDenied]`, text `Sorry, the current permissions prevented the action. (TableData 79201 Rec XRec Probe Insert: LethAL Sandbox Probes)`. **Danish (1030)** — `code=[DB:ClientInsertDenied]`, text `De aktuelle rettigheder forhindrede handlingen. (TableData 79201 Rec XRec Probe Insert: LethAL Sandbox Probes)`. So (a) **`GetLastErrorCode()` returns the SAME `DB:ClientInsertDenied` in both languages** — the language-independent signal this row asked for, now a measurement rather than a candidate; and (b) **the structural `(TableData <id> <name> <op>: <suite>)` parenthetical survives translation byte-identically**, which is the cheap route: it is already inside the text the client receives, so it needs no AL change and no protocol bump, unlike surfacing the error code (which would mean adding a field to `RunMutant`'s result and moving `MIN_CONTROL_VERSION`). **What is still NOT proven:** one locale on one container. `TableData` and the operation names are AL keywords and are very unlikely to localize, but that is an expectation, not a measurement, for the other ~25 languages BC ships. The English regex's MISS is already pinned by `packages/runner/tests/permissions-refused.test.ts`; a structural matcher needs its own red-check plus a test that a NON-refusal message carrying a similar parenthetical is not matched, because a false positive here tells a user to declare a property they already have. **Method note worth keeping:** the first two versions of this probe measured THEMSELVES — the platform refuses `INSERT` (and `COMMIT`) inside a `[TryFunction]` under `RunTests`, so the captured text described the wrapper, not permissions. `Codeunit.Run` is the catch idiom that works. That is R26's mistake, hit twice more in one session. | done (this commit) — `PERMISSIONS_REFUSAL_STRUCTURAL_RE` matches the `(TableData <id> <name> <op>: <suite>)` parenthetical, which the 2026-07-31 measurement showed survives translation byte-identically while the prose around it does not. English is tried FIRST so an English server's quote keeps exactly the shape it always had; the structural matcher carries every other language. No AL change and no protocol bump, unlike surfacing `GetLastErrorCode()`. **Matched strictly on purpose** — `TableData`, a numeric id, a name, one of the five AL permission operations, then `: <suite>`, all of it or no match, and case-SENSITIVE because these are keywords rather than prose: a loose matcher would tell a user to declare a property they already have, and a miss costs a diagnosis where a false hit costs trust. The old test that PINNED the Danish miss is inverted rather than deleted, which is what its own comment asked for, and it now uses the MEASURED Danish text instead of the guessed one it carried. **The red-check earned its keep twice:** it confirmed the matcher and the strictness are load-bearing, and it found a HOLE — the "never quotes a following stack frame" test passes even with the left anchor defeated, because the pattern's trailing character class already forbids a newline there. The anchor's real job is stopping a PRECEDING line bleeding into the quote, and that had no test; it does now, and it goes red without the anchor. Still unproven and stated as such: one locale on one container. The other ~25 BC languages are an expectation. |

## Backends and tooling

| id | item | status |
|---|---|---|
| **R7** | **al-runner's `asserterror` never fails a test** — `asserterror I := 1;` is reported `pass`, so any mutant killable only by an `asserterror` assertion comes back SURVIVED there while bcdev kills it (measured: 0/7 vs 3/2/2 on the table fixture; re-confirmed 2026-07-26 against a fresh `AsserterrorNeverRaises` probe). Under-reporting only, never a false kill. Loud-warn, not hard-refuse (see `packages/runner/src/al-runner-canary.ts`'s doc comment): the backend already declares `authoritative: false`, under-reporting is safe-direction, and refusing outright would block legitimate offline smoke-testing for a third-party-binary defect that a future al-runner build could fix on its own. The CLI now runs a startup canary — a known-failing `asserterror` executed against the ACTUAL configured binary every `--backend al-runner` session (`runFromCli`'s `announceAlRunnerCanary`) — and prints a measured, this-build verdict instead of a static 2026-07-25 claim; if a future al-runner build fixes it, the canary says so instead of continuing to warn about a defect that's gone. Evidence: `fixtures/README.md` §Tier-2 Phase 0, `scripts/probe-alrunner-tables.ts`, `packages/runner/tests/al-runner-canary.test.ts`. | done (Wave 1 Stream D — startup canary measures the real binary each session) |
| **R9** | **`itest:tables` runs its session once**, where `itest:bcdev`/`itest:alrunner` run twice and assert run-to-run equality. Cross-run nondeterminism there surfaces as a confusing per-mutant baseline mismatch rather than an explicit determinism failure. | done (86fb417 — runs twice, asserts verdict-identical; live-verified) |
| **R14** | **Stay on the newest tree-sitter-al.** Policy, not a defect: a grammar bump silently changed node shapes once (v2.5.0 → v3.0.1 inserted container nodes and zeroed `statementCalls` 703,239 → 0). The bump procedure — corpus site counts plus a **per-site** baseline proof, not a multiset signature — is in `packages/engine/vendor/README.md`. Re-check on every upstream release. | recurring |
| **R17** | **The env-tool crash-recovery record has a writer and no reader.** `recordCreatedEnv`/`removeRecordedEnv` maintain `~/.lethal/env-state/<runId>.json`, but nothing ever lists it. The design promises "a later `lethal run` prints a warning naming every stale entry it finds"; that scan was never built, so the whole recovery story for a leaked environment is a file nobody reads. Cheap fix: `readdir` + `console.warn` in `resolveEnvToolSession`. Evidence: `packages/runner/src/env-tool-session.ts`, final review of branch `layer-6c-env-tool`. | done (f616885) |
| **R18** | **`envTool` + `--backend al-runner` is silently ignored.** `--keep-env` and `--allow-expiring-env` are refused loudly for al-runner on the reasoning that a silent no-op is wrong; an entire configured `envTool` section being ignored deserves at least the same warning. Evidence: `packages/runner/src/cli.ts` al-runner branch. | done (f616885) |
| **R19** | **The prepublish step and the control-app republish both happen BEFORE the lease is acquired.** (Reworded 2026-07-28 — the original framing was wrong in three ways, corrected by external review and verified here.) **What is NOT the problem:** publishing `publishApps` in reuse mode. `fixtures/README.md` states the contract as "Reuse-mode ignores it **if absent**", so publishing when PRESENT is documented behaviour, and a reuse-mode config that lists apps is the user explicitly delegating test-app publication to LethAL. Skipping it would re-open the R31/R56 staleness class — observed TWICE, badly disguised, two lost debugging sessions — to close a race never once observed. Trading a measured cost for a theoretical one is the wrong direction, and a `publishAppsOnReuse` knob would restate what the existing key already says while silently breaking configs that work today. **Also not available:** verify-before-publish per app. The control app earns it via `HarnessInfo`'s protocol/version surface (R28); a test app has none, `EnvToolPublisher.publishFile` only hashes the bytes it READS with no server-side digest to compare against, and `.app` files are gitignored local builds — so version-equal/content-different is routine and a version check would skip the publish exactly when the app is stale. **The real residual hazard**, narrower than the original text: both publishes happen outside the lease window, so a concurrent session's test app can change mid-run (invisible to the attestation fence, which covers only the TARGET artifact), and repeated interrupted publishes are what R44 measured driving a tenant to `503`. Note the original entry's rationale was borrowed from the control app — republishing the TEST app does not run `LethAL Control`'s install/upgrade codeunits and does not touch the lease or `serverGeneration`. **Fix**: move both under the lease. Real plumbing — `startEnvToolSession` returns before `leaseSessionFor` runs, so prepublish must become a post-lease step or the session needs a post-lease hook. Low priority: never observed, and the common concurrent case publishes identical bytes. | **done for the half that is possible; the other half is impossible by construction.** The entry says "move both under the lease". The CONTROL-APP publish cannot move: `AcquireLease` is an action ON the control app and the lease row lives in its own `LC Lease` table, so there is no lease to hold until that app is published and answering. Same shape as R53's stated fix — a prescribed remedy that the architecture forbids — and it is recorded at the call site (`env-tool-session.ts` step 6) so nobody re-derives it. The `publishApps` half DID move: `startEnvToolSession` now returns `publishTestApps()` instead of publishing inline, `runSession` gains an `afterLeaseAcquired` hook invoked immediately after the lease is held and inside the same try/finally that releases it, and `cli.ts` wires the two through `afterLeaseAcquiredFor`. That closes the stated hazard for LethAL sessions: a concurrent one can no longer republish a test app mid-run behind the attestation fence (which covers the TARGET artifact only). The wiring is an exported seam rather than an inline spread deliberately — a red-check showed that removing the inline version changed nothing any test could see, and a silently-absent publish is the R31/R56 staleness class this entry explicitly refuses to reopen. Gates: `itest:bcdev` 3/10/3 and `itest:tables` 64/9/2, per-mutant identical to frozen. `itest:envtool` **PASS, 3 killed / 10 survived / 3 no-coverage**, per-mutant identical to frozen — run 2026-07-31 after starting `lethal-gate-6c`. That is the gate that actually exercises this path (the deferred `publishTestApps` runs only on the env-tool path), so the change is proven on the path it modifies, not merely on the container paths. |
| **R20** | **`HarnessVerificationError` conflates "wrong build" with transport failures including 401/403**, so an auth blip can still trigger one needless control-app republish — which runs install/upgrade codeunits under a concurrent session's lease. Needs a distinct `HarnessAuthError` thrown from `fetchHarnessInfo`'s 401/403 branch. Plausible trigger: a freshly-created environment whose admin user 401s transiently right after start. Evidence: `packages/runner/src/harness.ts`. | done — `HarnessAuthError` is thrown from `fetchHarnessInfo`'s 401/403 branch, checked BEFORE the stale-build shape (a 401 says nothing about WHICH build is deployed — the request never reached `HarnessInfo`'s own logic). It extends `Error` DIRECTLY, so `env-tool-session`'s `if (!(err instanceof HarnessVerificationError)) throw err` lets it escape and no republish happens; the fix needed no change there at all. That catch's own comment already listed "auth" among the refusals a republish cannot fix — before this the code could not honour it, because auth failures WERE `HarnessVerificationError`s. The message names what to check and states that no republish will be attempted. Red-checked: neutralising the branch reddens exactly 3 tests, and a session-level test pins that a `HarnessAuthError` propagates rather than triggering a republish. |
| **R21** | **Env-tool mode still hard-requires `altool.exe` to exist**, even though the env-tool publish path never constructs a `ContainerDeployer`. Harmless where the AL extension is installed; a confusing gate otherwise. Evidence: `packages/runner/src/cli.ts` `defaultAlToolPaths()` check. | done (f616885) |
| **R22** | **Deferred test-quality gaps on the env-tool code** (each one-line, safe to fix when the file is next opened): `redact`'s longest-first ordering is never exercised (a substring-of-another-secret leak would not be caught); the publisher test's `readArtifact` fake ignores its `path` argument, so hashing the wrong file would pass; the array dot-path test indexes `"0"` of a single-element array, surviving a constant-fold mutation; a `vars` collision test's regex matches boilerplate rather than the key; two "names whichever block is missing" assertions survive a message merge. Evidence: final review of branch `layer-6c-env-tool`. | done (b629d2a, f616885 — all five now discriminate) |
| **R23** | **Nothing forbids `username`/`password` in `envTool.publish.reads`.** If a config did that, the credential-withholding rule would replace a publish failure's detail with "(output withheld)" and silently break the orchestrator's version-conflict recovery, which parses BC's rejection text out of that message. No plausible tool emits credentials from a publish command, so this is a guardrail, not a live bug. Evidence: `packages/runner/src/env-tool.ts`. | done (b629d2a) |
| **R24** | **The env-tool bcdev-key collision check is opt-in.** `validateEnvToolConfig`'s `bcdevDeclaredKeys` parameter defaults to a no-op when omitted, so a future second caller that forgets it silently loses the "two sources for one value" guard. `resolveEnvToolSession` is the only caller today and does supply it. Make the parameter required. Evidence: `packages/runner/src/env-tool.ts`, commit `967554c`. | done (b629d2a) |
| **R25** | **A stale locally-built `lethal-control.app` fails with a confusing error.** `*.app` is gitignored, so the control artifact is a local build — and a build older than its AL source publishes fine, then fails harness verification with `HTTP 400: the parameter 'clientProtocol' … is not a valid parameter`. Confusing precisely because the endpoint EXISTS and answers; it just rejects an argument added later. Hit live on 2026-07-26 during the first env-tool gate run. Wants either a build step that rebuilds it before a gate, or a version/shape check in `HarnessVerifier` that says "your control app is older than this client" instead of surfacing BC's parameter error. | done (b2339d3 — names the stale build, keeps BC's text) |
| **R26** | **The permission canary measures itself, not the target.** Built to report whether the fenced path strips permissions; measured 2026-07-26 to be answering a question about its own codeunit. It does not declare `TestPermissions`, so it defaults to Restrictive and is refused — reporting `mocked` on the very server where a real suite writes happily on both paths. The mechanism and the fail-safes are sound (every degraded path is `inconclusive`, never a false clean); the predictor is wrong. Fix: declare `TestPermissions = Disabled` on the canary so it measures the platform rather than itself, and have it report the TARGET test codeunit's `TestPermissions`, which is what actually decides whether that suite can write. Evidence: `extensions/lethal-control/src/PermissionCanary*.al`, `packages/runner/src/permission-canary.ts`. | done — canary now declares `TestPermissions = Disabled`, so it measures the platform rather than its own declaration. Proven live on one server with the mock installed: control app 1.0.0.5 reported `mocked` / insert refused, 1.0.0.6 reports `not-mocked` / insert succeeded. |
| **R27** | ~~Trigger mutants whose tests write are unscored~~ — **VOID, follows R1.** The unscored-mutant blast radius was real but its cause was a fixture that omitted `TestPermissions = Disabled`, not a platform limit. With the property declared, writing tests run and trigger mutants score normally. What survives as a genuine product concern: **a target suite whose test codeunits omit `TestPermissions` will have its writing tests refused**, and LethAL currently reports those mutants as `error cause=unstable` rather than naming the cause. Report the target codeunit's `TestPermissions` value — that is the actual predictor — and say so plainly when it is not `Disabled`. | done — a permissions refusal is no longer a bare `unstable`: the failure note names the likely missing `TestPermissions = Disabled`, quotes BC's own refusal text, and says what to declare. See R35 for the two paths it does not cover. |
| **R28** | **Nothing detects a stale `LethAL Control` build.** `HarnessInfo` hardcodes `semver '1.0.0.0'` (`ControlApi.Codeunit.al`), so `HarnessVerifier` cannot tell a current control app from one several versions behind. Consequence seen immediately: until 1.0.0.5 is published on a given container, every bcdev session there prints an inconclusive 404 canary line at startup — correct and actionable, but the harness itself should say "your control app predates this client" instead of leaving each new action to fail its own way. Report the real version and compare it. | done (`see below`) — `ControlApi.CurrentAppVersion` now reports the module's real `AppVersion` via `NavApp.GetCurrentModuleInfo`, and `HarnessVerifier.checkControlVersion` refuses anything below `MIN_CONTROL_VERSION` (**1.0.0.7**, in lockstep with `extensions/lethal-control/app.json` and pinned by a test) with a message naming both versions and the rebuild. Dated BEFORE the capability checks, so the one cause is stated once instead of surfacing as whichever action first needs something the build lacks. Absent / non-string / unparseable versions each throw distinctly rather than defaulting to "probably fine" — a pre-R28 build's hardcoded `1.0.0.0` lands in the below-minimum branch, and a failed `GetCurrentModuleInfo` returns `''` (refused) rather than an invented literal. Comparison is `compareAppVersions` (`app-version.ts`), numeric per component: string-comparing reads `1.0.0.10` as older than `1.0.0.7`, i.e. rejects the newest builds first. Red-checked across five mutations — removing the gate reddens all 6 branch tests, string-comparing reddens the multi-digit test, breaking the lockstep reddens exactly the pin. **Consequence: every container must have a rebuilt control app (>= 1.0.0.7) published before its live gate will pass.** Actioned 2026-07-27: `1.0.0.7` built (`alc 18.0.38.8509`, 0 errors) and published `-scope Global -sync -install -upgrade` to Cronus281 and Cronus283, both confirmed `IsInstalled` at 1.0.0.7. **Both live gates then PASSED per-mutant against the R28/R34/R47/R48/R49 branch**: `itest:bcdev` 3 killed / 10 survived / 3 no-coverage, `itest:tables` 63 / 10 / 2 with `untargetedTriggers` 0 — each run twice with verdict-identical output, so the version gate admits a current build rather than merely refusing an old one. |
| **R29** | **False survivors from coverage attribution** — the worst output this tool can produce, and it was live. BC *does* report table-trigger coverage, but `SymbolReference.json` records no trigger, so `AppMethodIndex.lookup` returned `undefined`; the fallback scanned **local** procedures, which is empty for a table whose procedures are public; the observation was then dropped entirely, so the object lost credit along with the member. `byObject` kept only whichever sibling test happened to resolve, `coverageFilter`'s object-level fallback returned that non-empty-but-WRONG set, and the all-green fallback never fired. Measured on the Phase-1 fixture: **10 of 20 survivors were false**, each individually driven through the fenced path against its intended killer and killed (53/20/2 → the honest 63/10/2). Perversely, a table with **public** procedures scored worse than one with none, because the empty case fell through to the correct fallback — every real app is in the losing category. Fixed by emitting an object-level-only `CoverageEntry` (absent `procedure`, never `""`, which would collide with a trigger mutant's own `byMember` key). Evidence: `0a463fd`, `packages/runner/src/{bcdev-backend,selection}.ts`. | done (Tier-2 Phase 1) |
| **R30** | **Mutation sites written inside `tableextension`/`pageextension` are refused.** `OBJECT_KINDS` (`packages/builtin-tier2/src/receiver.ts`) omits extension objects, so no Tier-2 operator claims anything declared in one. Safe direction — a missed site costs one operator's signal — but a great deal of real BC code lives in extensions, so the practical coverage loss on a customer project could be large. (The *shadowing* half of this was the unsafe direction and is fixed: `resolveTable` now scans `tableextension` objects targeting the resolved table.) | **partly done 2026-07-28 — the mechanism is in and tested; the measured value on a real project is +2 mutants.** `OBJECT_KINDS` now admits `tableextension`/`pageextension`, and an implicit `Rec`/`xRec` inside a `tableextension` — both the QUALIFIED (`Rec.TestField(...)`) and the RECEIVERLESS (`TestField(...)`) forms — resolves to the EXTENDED table via the header's `base_object` field, so rule 3's `projectDeclaresProcedureOnTable` guard applies to that table exactly as for a site written in the table itself. Red-checked three ways: removing the kinds, mis-resolving to the extension's OWN name (which looks successful and silently bypasses the shadowing guard), and removing the receiverless branch. `itest:tables` unchanged at 64/9/2 per-mutant. **The honest result: on Continia Document Output this gained 77 -> 79 `remove-testfield` and nothing else.** Handling only the qualified form gained EXACTLY ZERO — byte-identical operator totals — because DO's 31 tableextensions contain no `Rec.`-qualified calls at all; that is why the receiverless form was measured for and then added. **Second half done 2026-07-28, and it was worth 9x the first.** `buildSymbolTable` now indexes a `tableextension`'s own members (globals, procedures with their locals and parameters) for VARIABLE SCOPE, so a call on a variable DECLARED inside an extension resolves instead of being refused as unresolvable — the shape DO's extension code overwhelmingly uses. Indexed under a NAMESPACED key (`extensionScopeKey`), never the bare extension name, so `resolveProcedure("My Ext", ...)` keeps answering `null`: scope and callability are different questions, and the engine test asserting the second is deliberately unchanged rather than overruled. **Measured on Document Output, Tier-2 totals 961 -> 979 (+18):** `remove-setrange` 672->682, `remove-testfield` 77->82, `remove-calcfields` 202->205 — against +2 for the implicit-`Rec` half alone. Red-checked; `itest:tables` 64/9/2 and `itest:bcdev` 3/10/3 both unchanged per-mutant after an engine-level change. `pageextension` stays refused by design: its implicit record is the extended PAGE's `SourceTable`, declared in an object the project usually cannot see, and guessing would CLAIM sites wrongly. **CLOSED 2026-07-31 — the third half shipped and the mechanism is now LIVE-GATED.** (a) `buildSymbolTable` indexed a `pageextension`'s members NOWHERE (the node matched neither the tableextension branch nor the object-kind map and fell through the loop), so every call on a variable declared inside one was refused as unresolvable. Both extension kinds are now indexed for variable scope under a KIND-namespaced key (`extensionScopeKey(kind, name)` — AL lets a `tableextension` and a `pageextension` share a name, and one namespace for both lets each resolve the other's variables). `tableExtensions` deliberately still holds table extensions only: it is the rule-3 shadowing guard's input, keyed on the extended TABLE, and a page name compared to a table name can only match by coincidence. **Measured before it was built, not after** (`scripts/probe-r30-pageext.ts`, Continia Document Output Cloud, 554 files): **18** Tier-2-shaped calls on a record declared inside a `pageextension`, against **ZERO** on a pageextension's implicit `Rec`, and **zero of 93** pageextensions extend a page the project declares — so the `SourceTable` needed to resolve `Rec` is not available even in principle, and that refusal is now measured rather than argued. The probe is calibrated: its `tableextension` count (17) reproduces the +18 the second half actually gained. (b) **The fixture grew its first extension objects, so extension support is no longer unit-only.** `fixtures/sandbox-data` gains `tableextension "Data Main Ext"` (implicit-`Rec` `TestField` + a `SetRange` on an extension-scoped local, both reached from a test because a tableextension's public procedures are callable on a variable of the extended table's type) and a code-free `page` + `pageextension` pair. `itest:tables` moved **64/9/2 over 75 deployed → 69/9/6 over 84** (93 raw specs): **+5 killed, all five tableextension mutants** — the first Tier-2 extension mutants any gate has executed — and **+4 no-coverage** for the pageextension. Per-mutant join against the previous baseline: **0 changed verdicts, 0 missing keys, 9 new keys**, so the whole delta is new sites. `itest:bcdev` unchanged at 3/10/3. Red-checked three ways (drop the pageextension parse branch, drop the consumer's scope branch, collapse the kind namespace) — and the first namespacing test PASSED under its mutation for the wrong reason (the collision destroys the procedure entry wholesale, so the receiver became unresolvable, which is also `false`); it was rewritten to assert `true` with the declaration order reversed, and then went red. **What is NOT proven: the pageextension half is claimed, deployed and never executed.** A pageextension's code is reachable only through a `TestPage`, and a TestPage HANGS the fenced session (R69) — measured, not assumed: the test was written, published and run, and the session quarantined at baseline scoring nothing. Also unproven for either kind: nothing here exercises a `pageextension` on a real customer project end to end. |
| **R31** | **Nothing detects a stale published test app, and it has now cost two debugging sessions.** The gate publishes the instrumented *target* on every run but never the test app — by design, since "publishing the test app is the user's own workflow". The failure mode is badly disguised: the runner discovers tests from source, the server has an older set, the baseline goes red, and dozens of mutants fall to `no-coverage`, which reads as a mutation-scoring problem rather than "your published test app is older than your source". Hit twice on 2026-07-26/27. The harness knows both the discovered test list and what the server actually ran; comparing them and saying so is cheap. | done (`see below`) — the evidence was already present per-test and nothing aggregated it: `BcDevMcpBackend.run` returns `NO_RESULT_FOR_METHOD` when the server has no result for a method it was asked to run, which means the PUBLISHED app does not contain it. `runSession` now tallies those across the baseline and the report carries `staleTestApp.missingTests` plus a `stale-test-app` caveat, and the console says what to DO (republish) and why the symptom is disguised (a missing test cannot kill anything, so its mutants land no-coverage/survived and read as a scoring problem). Producer and detector share one exported constant, pinned by a test, because a reworded literal would make the diagnosis silently stop firing. Deliberately distinct from `baseline-red`: a red baseline has many causes, and collapsing them would send a reader to debug assertions instead of republishing. Red-checked. |
| **R32** | **The 10 surviving Phase-1 mutants were never individually verified as genuine.** The 10 *false* survivors from R29 each got driven through the fenced path against their intended killer; the survivors that remain did not get the same treatment. They are plausible (weak-test sites by design), but "plausible" is exactly what the false ten looked like before they were checked. Drive each against the test that should kill it and record the outcome — a survivor that turns out killable is another R29-class defect. | done (2026-07-27) — **all ten genuine, zero false survivors.** Each driven through the fenced path against ALL 19 tests, twice: 432 fenced calls, both sweeps byte-identical, 0 differing verdicts. Activation was proven per site rather than assumed (`IsActive` is a bare string compare, so an unregistered id behaves exactly like baseline and "ran" proves nothing) by running a sibling mutant from the same emitted guard chain and confirming it kills. Composition: 3 equivalent (M0030/M0050/M0051), 2 boundary-value-starved (M0011/M0025 — killable only by data at exactly 1000 / length 10), 4 deliberate weak-test survivors, 1 weak-assertion hole (M0034 — see R36). |
| **R33** | **Tier-2 Phase 2 operators** — `RemoveCommit`, `RemoveSetLoadFields` (tagged `likely-equivalent`, scored separately), and `SwapRecXRec`. Spec §5. **`SwapRecXRec` must not be built before its go/no-go experiment runs**: when `Modify(true)` is driven from AL rather than a page, `xRec` may carry the same values as `Rec`, and LethAL drives every test headlessly — if the two do not differ on that path the operator is near-worthless in this execution model, and a recorded "we measured it and it does not work here" is the better outcome. `RemoveCommit` also needs the report to distinguish a genuine kill from BC's "cannot run codeunit in a write transaction" platform artifact. | **done 2026-07-31 — ONE operator ships, and the two refusals are each measured rather than argued.** Spec: `docs/superpowers/specs/2026-07-31-r33-tier2-phase2-design.md`, revised after a Fable 5 adversarial review that caught two overreaching claims in its first draft. **(1) `RemoveCommit` SHIPS** (`packages/builtin-tier2/src/remove-commit.ts`, predicate `claimsSystemCall`): receiverless only (the system `Commit` has no qualified form, so `X.Commit()` is by construction a project procedure), refused when the enclosing object — or a `tableextension` of the enclosing table — declares `Commit`, and zero arguments. Registering it changes NO fixture's mutant set: `sandbox-data` stays at 93 specs and `sandbox-app` at 16, with zero `remove-commit` specs, because both fixture `Commit` sites are the shadowed NEGATIVES and both are correctly refused. So the operator ships **proven on its refusals and unproven on its claims** — no gate has ever killed a `remove-commit` mutant, and no fixture positive was added (a positive with no covering test lands `no-coverage`, not `survived` — the review caught that). Filed as R73. **(2) `SwapRecXRec` is DEFERRED with MEASURED SIGNAL, not refused** — and the first version of this row's conclusion was WRONG. The spec's go criterion was framed around `Modify(true)`; the probe answered exactly that (`rec=250 | xrec=250 | differ=No`, identical on the fenced path and the hub) and the write-up then generalised it to every `xRec` site. Two follow-up probes settled it, same container, fenced path: field `OnValidate` driven by `Validate(Amount, 250)` reports **`rec=250 | xrec=100 | differ=YES`**, and `OnRename` driven by `Rename('R2')` reports **`rec.No=R2 | xrec.No=R1 | differ=YES`**. `OnValidate` is where AL's ubiquitous `if F <> xRec.F then` change detection lives — so `xRec` carries real information headlessly in exactly the trigger kinds that read it, and a `SwapRecXRec` scoped to validate/rename sites HAS signal. Building it is R71. **(3) `RemoveSetLoadFields` is REFUSED ON COST, and BOTH earlier justifications were wrong.** v1 said the kill paths were pathological; v2 said the mutant was "unkillable by construction" because omitted fields are fetched JIT and no error can arise. MEASURED: the JIT fetch REREADS THE ROW and rereading a row deleted since the partial read RAISES — `JIT loading of field(s): 'Amount' failed for table: 'Rec XRec Probe' ...`. So it IS killable, via an `asserterror` on that shape. It is refused anyway because the kill needs the row deleted or changed between the partial read and the field access, which no ordinary suite arranges — a judgment someone can argue with using a measurement, rather than a claim about what BC can do. **(4) The write-transaction artifact detector is NOT built — R72.** Nothing in any fixture has ever produced that message, so a detector now would match an ASSUMED string (R31's shape), and the review named a hole it must survive anyway: `if not Codeunit.Run(...) then Error(Err, GetLastErrorText())` re-wraps the artifact and defeats a text match. Probes are committed and permanent (`fixtures/sandbox-probes/src/RecXRecProbe*.al`, `Tier2Phase2Probe.Codeunit.al`); one of them measured ITSELF first (a `[TryFunction]` wrapper around `Commit()` is refused by the platform, R26's mistake) and the corrected version proves `Commit()` executes in the fenced run. Gates: `itest:bcdev` 3/10/3 and `itest:tables` 69/9/6, per-mutant, unchanged — as predicted offline, since no fixture gains a mutant. |
| **R34** | **A reused environment that is `Stopped` is not handled.** `env-tool-session` resolves it, finds a dead endpoint, and aborts. Observed live 2026-07-26: the gate environment had idled to `Stopped` and needed a manual `env start` plus a ~420 s wait. Starting someone else's environment unasked is worse than refusing, so the fix is to refuse *naming the status* — and possibly an opt-in `--start-env`. Predicted by the env-tool spec's own risk list before it happened. | done (uncommitted) — opt-in `envTool.requireStatus.equals` plus a `resolve[].reads.status` entry: a reused environment whose status is not the declared ready value is refused after resolve and before any publish, naming the value the tool actually reported. Both sides come from config — LethAL hardcodes no vendor status vocabulary. Configs that declare no expectation are untouched. The opt-in `--start-env` half is deliberately NOT built — it needs a start-command template plus its own readiness polling, i.e. `startEnv` + `readyWhen` on the reuse path; file it as its own item if it is ever wanted. **Proven live 2026-07-27 against the real failure, not a fake**: the hosted gate environment had idled to `Stopped` again, and with `requireStatus: {equals: "Running"}` added to its (gitignored) config, `itest:envtool` refused in seconds — `reports status "Stopped", not "Running" ... start it with your own environment tool` — instead of resolving a dead endpoint and dying minutes later inside the transport. After starting the environment (145 s to `Running`) the same gate PASSED at 3 killed / 10 survived / 3 no-coverage. | 
| **R35** | **The `TestPermissions` diagnosis had two blind spots.** It fired only on the `unstable` path (`packages/runner/src/orchestrator.ts`, R27's fix). **(1)** A test refused at *baseline discovery* never reaches that path: it is dropped from the green set, and the mutants it alone covered were recorded `error` with the note "unsupported test type" — **a wrong diagnosis, not an absent one** (this entry originally said `no-coverage` with no explanation; the code says otherwise, corrected here on closing). It points the reader at their test's TYPE when the fix is one property in their own source. **(2)** The detector matches BC's English refusal text, so a non-English server gets a silent miss (never a wrong answer). | done — (1) fixed: refusals are collected per batch (batch-local, so a refusal measured in one batch cannot relabel another's note), the mutant note names the permissions cause and **appends BC's words verbatim** rather than asserting the diagnosis — the detector is a hedged English regex and the quote is what lets a reader overrule it — and `SessionReport.permissionsRefused` + a `tests-permission-refused` caveat state it session-wide. The `unstable` path feeds the same set, so one run can no longer emit a note saying "declare `TestPermissions = Disabled`" while the report field is absent. Verdict-neutral by construction: the `error`-vs-`no-coverage` branch is untouched and only wording plus a new report field changed. `unsupportedCoverageNote` is extracted and unit-tested so the mixed case (some covering tests refused, others merely broken) is reachable without a two-batch session. (2) NOT fixed — split out as **R66**, because closing it needs a language-independent signal MEASURED against a localized server, which no unit test can settle. The limitation is now tested rather than assumed (`permissions-refused.test.ts` pins the English hit and the Danish miss). |
| **R36** | **A fixture `asserterror` accepts the wrong error, hiding a real mutant.** `RequireCategoryAFails` asserts only that *an* error occurred, so deleting `DataMain.Get(MainNo)` (M0034) is invisible: with no `Get` the record is blank and `TestField(Category,'A')` still raises, just for a different reason. The mutant is correctly reported as survived — the fixture genuinely does not catch it — but the fixture is supposed to be built so a broken operator fails, and this is the project's signature "test passes for the wrong reason" shape sitting inside the very fixture that exists to catch it. Asserting the error text would make M0034 killable and move the frozen baseline to 64/9/2. Evidence: R32 verification, 2026-07-27. | done — the test now asserts the error message NAMES THE RECORD it loaded (`T-REQ`), which a blank record cannot do. That discriminates on exactly what the deleted `Get` is responsible for; asserting the expected Category would NOT, since both the real and the mutated path mention `'A'`. Live-verified on Cronus283: **63/10/2 -> 64/9/2**, one net verdict, safe direction, and M0034 at `DataOps.Codeunit.al:44` is killed by `RequireCategoryAFails`. The re-recorded baseline shows two changed lines, not one — identical statements share an identity key and `diffMutants` sorts each group canonically, so one member changing re-sorts the group. That is the design working, not instability (`mutant-equality.ts` documents why a within-key ordinal is deliberately not added). |
| **R37** | **Coverage subsets are narrower than the gate's confidence implies.** During R32, seven of the ten survivors had been run against exactly one test, two against seven, one against two. Broadening to all 19 changed no verdict, so the selection was correct here — but the margin is thin, and the pre-`0a463fd` failure looked identical from the outside (one test, wrong one). Worth a periodic all-tests sweep of the survivors as a standing check, since a survivor is exactly where a too-narrow subset hides. | done (2026-07-27) — **answered on a REAL project, and the answer is zero false survivors.** Two runs of one Continia Document Output codeunit, identical in every respect except `coverageMode`: selection ON (the default) and selection OFF (`"none"`, every mutant against all 56 green tests). Compared per-mutant over all **138** mutants — `mutantCode` verified a safe join first (0 identity mismatches at the same code, since both runs generated the same source into one batch), because collapsing on identity keys alone covers only 89 distinct tuples. Transitions: 84 `survived`→`survived`, 16 `killed`→`killed`, 14 `no-coverage`→`survived`, 11 `error`→`survived`, 9 `error`→`killed`, 3 →`error` (R53 skips). **No mutant that selection called `survived` or `no-coverage` was killable by the full suite** — the R29 direction is empty. The selected run also reproduced the original trial exactly (16/86/15/21, score 15.7%) on a rebuilt environment. See R55 for the two accuracy gaps this comparison exposed, both safe-direction. |
| **R46** | **A survivor no instrumented guard fired for is not a finding, and until now nothing distinguished one.** `LC Control State.IsActive` is a bare string compare, so an unactivated mutant behaves byte-identically to baseline — "the test ran and passed" proves nothing about whether the mutation was ever in play. R32 had to establish that by hand, one mutant at a time, after R29 had already produced 10 false survivors out of 20. `RunMutant` already attested `observedAny` per run, but `runSession` folded it into a session-wide artifact ledger and discarded it per mutant. Now surfaced as `MutantOutcome.guardObserved`, OR-ed across a mutant's covering runs, with the asymmetry documented: **`false` is decisive** (no guarded site executed, so the mutant was never given a chance to fail — it belongs with `no-coverage`, not with findings), **`true` is weak** (some selector fired somewhere in the artifact, not necessarily THIS mutant's guard), and **absent means not measured** (al-runner cannot attest; collapsing absent to false would accuse every al-runner survivor). The console names unexercised survivors so they do not read as suite gaps. First measurement on Document Output: **86 of 86 survivors `true`, 0 `false`**, with `no-coverage`/`error` correctly absent — so the artifact was live throughout and the "10 of 12 procedures have zero kills because activation is broken there" hypothesis is ruled out at artifact level. It does NOT individually verify the 86; that still wants driving each against its intended killer. | done (`see commit`) |
| **R47** | **The per-mutant time budget is a hardcoded floor, and one slow (mutant, test) pair costs the WHOLE run.** The budget is `2 x that test's baseline duration`, floored at `MIN_MUTANT_BUDGET_MS = 30_000` (`packages/runner/src/orchestrator.ts`) — a constant with no config or flag reaching it. When a mutated run exceeds it the client aborts, which is correctly indistinguishable from "the server may still be executing it", so the session latches unsafe, stops scheduling, and writes a durable quarantine. **Measured 2026-07-27** attempting R37's all-tests sweep on Continia Document Output with `coverageMode: "none"`: quarantined at mutant **M0013 of 138** on `SendPeriodStatements_WhenCustomerSetToAutomatic_ShouldProcessCustomer`, losing the run after 330 s. The exposure scales with (mutants x tests): an all-tests sweep multiplies the number of pairs that can hit the ceiling, so the very configuration R37 asks for is the one most likely to abort. A slow test is ordinary in a real suite — DO's per-mutant mean under all-tests was 19.5 s with a p95 of 43 s across 44 tests. Wants `--mutant-timeout-ms` (or a multiplier) so a real project can raise it, and is worth pairing with a resume capability so an abort at mutant 13 does not discard the first 12. Evidence: `docs/benchmarks/` timings, the quarantine reason recorded in that run's report. | done — **both halves shipped.** `--mutant-timeout-ms <n>` (`SessionConfig.mutantTimeoutMs`) raises the FLOOR; the effective budget stays `max(2 x baseline, floor)`, because capping is what causes the aborts this exists to prevent. `--resume` / `--resume-run <id>` recover an aborted run: everything still happens — parse, instrument, deploy, baseline — and only the EXECUTION of already-scored mutants is skipped, so coverage attribution and covering-test lists come from the resumed run rather than from the database (a resumed survivor stays actionable). Verdicts are carried by IDENTITY, not by `mutant_code` (which restarts per batch), so an edited site's `astHash` simply stops matching and re-batching is survivable — `maxGuardsPerBatch` is deliberately excluded from the resume fingerprint for exactly that reason, making "re-run with a smaller batch budget" a real recovery path. `error` is not carryable (a non-measurement, not a result) and a colliding identity key is dropped rather than guessed at. The quarantine message now names the budget it used and both flags. **One defect found and closed on the way**: `invalidateBatchVerdicts` (the fail-closed attestation gate) corrected only the in-memory outcomes, on the stated grounds that a quarantined run is never `finishRun`-ed so `priorSurvivorKeys`'s `finished_at IS NOT NULL` filter hides its stale rows — and `--resume` selects on `finished_at IS NULL`, the exact complement, so it would have preferentially read the false survivors that gate exists to destroy (R29, resurrected). `mutants` gained a `batch_index` column and `ResultsStore.invalidateBatch` now applies the correction durably. All four fixes red-checked individually. |
| **R38** | **The codeunit selector var is anchored before the object's PROPERTIES, so any codeunit that declares one does not compile once instrumented.** `injectSelectorVarIntoObject` (`packages/schemata/src/compile.ts`) anchors at `members[0]` for a codeunit. AL requires object properties (`Permissions`, `Access`, `Subtype`, `SingleInstance`, `EventSubscriberInstance`, `TableNo`, …) BEFORE any `var` section, so `{ var MutationSelector … Permissions = …` makes alc read `Permissions` as a variable name and the parse never recovers. The doc comment's "in a codeunit, `var` before the first member compiles clean" was measured against fixture codeunits, which declare no properties — the table branch already skips section-like members for exactly this class of reason; the codeunit branch needs the same treatment. Measured 2026-07-27 on the real Continia Document Output app (551 `.al`, 162 instrumented): **19 of 162 instrumented files, 246 alc errors, whole-app compile fails ⇒ zero mutants runnable.** Root cause proven, not inferred: hand-reordering the injected `var` below the property in ONE file took that file from 41 errors to 0 (246 → 205 total, 19 → 18 affected files) with nothing else changed. | done (`2237d91`) — the codeunit anchor is now the first non-property member; all-properties falls through to the trailing branch the trigger-less table already used. Tables needed no change (properties precede `fields`). Note tree-sitter RECOVERS from the bad ordering with no ERROR node, so `countErrorNodes` is structurally blind to it — the new tests assert position, and `alc` is the authority. |
| **R39** | **The batch project copies only `*.al`, so a real app's resources never reach `alc`.** `prepareBatchProject` (`packages/runner/src/orchestrator.ts`) filters `readdir` to `.al` and copies those alone, but `app.json` still names `logo`, and a real project also carries translations (`.xlf`), control add-in bundles, permission XML and report layouts. alc stops at `app.json(13,3): error AL1001: Source file 'Images\Logo.png' could not be found` — before compiling a single line, so the failure is not even attributable to instrumentation. Every fixture is resource-free, which is why this never surfaced. Note the `.al` copy is also FLATTENED to `basename` with a silent `if (exists) continue`, so two same-named files in different folders would drop one without a word; DO happens to have 551 distinct basenames out of 551 files, so the flattening survived here by luck, not design. Measured 2026-07-27 on Continia Document Output. | done (`d724b62`) — resources copy with their relative paths intact (`.al` stays flat, since that is what `writeInstrumentedProject` writes); tool directories, built `.app` packages and the project's own `app.json` are excluded from the resource sweep, but deliberately NOT from `.al` (DO keeps 137 genuine sources under `.dependencies`). Basename collisions are now detected on the source paths and refused naming both. |
| **R42** | **Instrumentation multiplies ONE existing diagnostic ~18×, and introduces none.** Measured on Continia Document Output, same symbols and compiler, baseline vs instrumented: **201 → 2,995 warnings**, but the composition is the finding — **zero warning IDs appear that the baseline did not already have, and zero are lost**. Exactly one multiplies: `AL0432` (deprecated member) 167 → 2,961 (×17.7). `AL0920`, `AL0729`, `AL0749`, `AL0254`, `AL0685`, `AL0468`, `AL0482`, `AL0523` are all ×1.0. The cause is structural rather than app-specific: `AL0432` fires per OCCURRENCE, and the wrap-lift-duplicate emission repeats each guarded statement once per mutant, so a deprecated call inside a mutated site is re-reported per branch. **Consequence: the extra warnings are provably signal-free** — they say nothing the developer's own baseline build does not already say. A plain build is unaffected (alc does not fail on warnings; the instrumented DO artifact compiled with 0 errors). What remains is (a) noise, and (b) a real break for a project wired to warnings-as-errors or a CI warning budget. Suppressing `AL0432` on the instrumented compile would be SAFE by this measurement but trades away the ability to notice if instrumentation ever did introduce one, so it is deliberately not done unquestioned — the documented rule is **compare warning counts against a baseline compile, never against zero**. Re-measure with `docs/measurements/` style if the emission shape changes. | done (measured; no code change — see rationale) |
| **R43** | **`alc 18` writes OPC part names with single-encoded spaces, producing a package BC 28 cannot load — and LethAL has no way to select a different compiler.** Root cause proven 2026-07-27 by compiling ONE directory with both binaries and the SAME flags, then diffing the packages: identical 614-part lists, but the zip entry names differ — `alc 17.0.29.44223` writes `src/Al/Codeunit/Codeunit%25206175272%2520CDO%2520...al` (space encoded to `%20`, then OPC-escaped to `%2520`), while `alc 18.0.38.8509` writes `Codeunit%206175272%20CDO%20...al`. BC then looks up the properly-escaped name, finds nothing, and reports the generic `Specified part does not exist in the package.` Reproduced minimally: a single-codeunit app publishes fine as `Probe.Codeunit.al` and fails once renamed to `Codeunit 79400 LP Probe With Spaces.al` — so the trigger is a SPACE in a source file name, nothing else. That is why LethAL Control and every fixture publish (no spaces) while Document Output cannot (`Codeunit 6175272 CDO ....al` throughout). **Both compilers exit 0** — this is a packaging defect, not a compile error, and it surfaces only at publish. Earlier hypotheses, each killed by direct test: the `logo` backslash (forward slashes failed, removing the field failed, and `logo/Logo.png` is byte-identical in both packages), translation files, flat-vs-mirrored source layout, missing dependencies, and app size. Fix shipped for the LethAL half: `bcdev.alcPath` config override, so a project can pin the compiler its server accepts. The encoding difference itself is upstream (Microsoft's `alc`). | done (`46a4a46`) — `bcdev.alcPath` pins the compiler; a configured path also satisfies the "no AL extension installed" gate on the env-tool path (an explicit path IS a compiler) but NOT on the ContainerDeployer path, which still needs `altool.exe` from the extension. Red-checked. The encoding difference itself is upstream in Microsoft's `alc` and is not fixed here. |
| **R44** | **Publish cost scales with INJECTED GUARD COUNT, because BC recompiles the extension server-side — and a fully instrumented real app exceeds the hosting proxy's timeout.** Measured 2026-07-27 against a hosted Continia BC 28 environment, same app, same source, same publish path, only the guard count differing: **163 guards → HTTP 200 in 28 s**; **11,777 guards → nginx `504 Gateway Time-out` at 362 s**; Continia's own uninstrumented build publishes fine. The proxy cap is real and unraisable from the client (`curl --max-time 1800` still 504s; the CLI's `--request-timeout 1800000` has no effect, and its own cut-off shows as a 301 s `The operation timed out.`), but the cap is the *constraint*, not the cost — the cost is BC recompiling every injected guard. **Three earlier framings of this entry were wrong and are corrected here**: not app SIZE (8.9 MB transfers in 2.3 s, `time_starttransfer=0.11s`), not SCHEMA work (a schema-identical ForceSync republish hit the same wall), and not the proxy alone. This makes `--only` (R41) load-bearing rather than a convenience: on a hosted environment it is what makes publishing an instrumented real app possible at all, and it caps how large a single batch may be. LethAL should size batches by guard count against a configurable publish budget, not put every mutant in one artifact. Operationally: BC keeps publishing after the proxy severs the connection, holding a tenant-wide lock (~15 s to reject while held); each failed attempt rolls the extension back; repeated interrupted attempts drove the tenant to `503 The tenant 'default' is not accessible`. So the env-tool publish path must poll for the installed version on a timeout, never blindly retry. **The cap also quarantines RUNS, not just publishes** (measured 2026-07-27): a third trial run latched `quarantined: baseline test in-flight-unknown running ContainerUniqueness_Check_FiresDuringAuthReconfiguration` and scored 0 of 138 mutants. A fenced call that outlives the proxy window returns unreadable, which is indistinguishable from "the server may still be executing it" — so the session correctly latched unsafe, refused to score anything, and wrote a durable tier quarantine needing an operator `clear-quarantine`. Two earlier runs of the identical configuration completed, so hosted DO runs are FLAKY rather than merely slow, and the exposure scales with baseline duration (1,246 tests, ~750 s of a ~1,000 s run). Narrowing the TEST set — not just the mutant set — is the lever that reduces both cost and this risk. | done (`dd6c069`) — `--max-guards-per-batch` bounds a batch by guards; splitting stays at FILE granularity, and a file exceeding the budget alone becomes its own oversized batch that REPORTS itself rather than being dropped. Proven live 2026-07-27: 4 codeunits / 167 guards / budget 60 produced 4 batches (40/44/41/40 mutants), **36.8 s per publish** — matching the single-batch ~40 s, so publishing scales linearly with batch count. The proxy timeout itself remains Continia infrastructure and is not fixed here; what is fixed is that LethAL no longer asks it to swallow an unbounded artifact. See R45: baseline is paid PER BATCH, so narrowing the suite is a precondition for this at scale, not an optimisation. |
| **R45** | **The baseline runs the WHOLE test suite, so `--only` narrows cost by far less than it appears to.** Measured on the first live Document Output run: total 1065 s = generate 0.7 s + deploy 40.8 s + **baseline 863.8 s (81%)** + mutants 151.9 s + overhead 8.5 s. `--only` (R41) scoped the run to 163 mutant sites in one codeunit out of 551 files, but the baseline still executed all **1,246** discovered tests once, because narrowing selects MUTANTS, not TESTS. Two consequences. Cost: a narrowed first run on a real project is dominated by a phase the narrowing does not touch, so "run one codeunit to try it out" is nearly as expensive as running far more. Risk: baseline duration is also the exposure window for R44's proxy timeout — the run that quarantined did so during baseline. A `--tests-only <glob>` (or reusing the covering-test set from a prior run) would cut both. Note a subtlety: narrowing the test set CHANGES coverage attribution and therefore verdicts, so it is not a free optimisation and must not be compared against an unnarrowed baseline as though it were. Evidence: `docs/benchmarks/runs.jsonl`, `SessionReport.timings`. | done (`59e603d`) — `--tests-only <glob>` narrows the baseline. Measured on Document Output: baseline **744.8 s → 25.0 s (-97%)**, total **953.8 s → 231.2 s (-76%)**, with verdicts **identical** (16/86/15/21) to the three unnarrowed runs. Modelled deliberately UNLIKE `--only`: that one selects mutants and cannot change a verdict, this one selects TESTS and can — so it carries its own `testsOnly` field, a `tests-narrowed` caveat distinct from `narrowed`, degrades `validity.reliability` on its own, and warns at the console. The identical verdicts show this glob happened to contain every killing test; that is NOT a general safety proof. Confirmed per-batch at 25.3 s in the 4-batch run. |
| **R40** | **The uninstrumentable-object-kind gap costs 41% of a real app's mutation sites — and the reason given for it is WRONG.** On Continia Document Output: **287 of 449 files carrying sites, holding 8,259 of 20,036 sites (41%), are never mutated** — pages, reports, enums, and both `pageextension` and `tableextension`. A DO score is therefore computed over 59% of the app while reading as a whole-app number. `canCarryMutationSelectorVar` (`packages/schemata/src/compile.ts`) refuses every kind but codeunit and table, on the stated grounds that "only a codeunit or a table can carry the injected `var MutationSelector: Codeunit "Mutation Selector";` declaration, so a guard in any other object kind cannot compile (AL0118)". **Measured 2026-07-27 and disproven**: a probe app declaring that exact var inside a `page`, a `pageextension`, a `tableextension` AND a `report` compiles clean — `alc 17.0.29` exit 0, zero errors, artifact produced. The real constraint is the same one R38 turned out to be: WHERE in the object the `var` is anchored, not WHICH object kind. A page needs it after `layout`/`actions`, a report after `dataset`, a tableextension after `fields` — exactly the per-kind anchor reasoning the table branch already encodes. So this is anchor work on `injectSelectorVarIntoObject`, not a language limit, and the 41% is largely recoverable. Note the follow-on: the operators must then also target those bodies (`OnAction`, `OnOpenPage`, `OnAfterGetRecord`), and `notInstrumented` under-reports on a narrowed run (`fileCount: 0` with `totalFiles: 551`), which should be fixed alongside. | done for page/report — **11,777 → 18,115 mutant sites on Document Output**, app coverage 59% → 90%, instrumented artifact compiles clean (0 errors, 9.0 MB). `CARRIER_KINDS` now admits page and report, which take the TRAILING anchor a trigger-less table already used; measured, not assumed. Enabling them immediately exposed a second defect the codeunit/table world could never surface: **204 sites matched by `negate-conditional`/`conditional-boundary` are not inside executable AL at all** — an AL page property is declarative but parses with statement syntax (`SubPageLink` reads `"No." = field("Customer No.")`, a filter reads `"Format" = ''`), so the operators claimed them and nothing could wrap them; one such site aborted the whole session at `buildComponents`. Fixed with `isMutableSite` checked once at spec generation rather than per operator, since it is a precondition of emitting a mutant at all, and the drop is counted and warned rather than silent. **Still open: `pageextension`/`tableextension`** (1,717 sites). They compile with the var, but they do not match `objectHeadersOf`'s header regex, and it is UNMEASURED whether BC attributes an extension's coverage to the extension's own id or the base object's — guessing wrong mis-keys `coverageFilter` and manufactures false survivors, the R29 failure exactly. **Probe run 2026-07-27 (`docs/measurements/tableextension-coverage-probe.al`), and the attribution fear was unfounded**: BC reports a tableextension's code under the EXTENSION's own object id (`…:79481`), not the base table's, so keying extension mutants on the extension id is correct. The real blocker is narrower and now named: **`objectType` arrives as the raw numeric `15`** (BC's TableExtension enum) where LethAL's manifest writes `"tableextension"`, so the `(objectType, objectId)` keys would not match and every extension mutant would silently become `no-coverage`. Remaining work: extend `objectHeadersOf`'s header regex to extensions, and map BC's numeric extension object types in `buildCoverageMap`/`normalizeObjectType` — the mapping must FAIL LOUDLY on an unknown type rather than defaulting, because a silent mismatch is indistinguishable from "nothing covered this". Coverage also came back object-level only (`procedure: undefined`), so extension mutants would lean on `coverageFilter`'s object-level fallback. **Extensions landed 2026-07-27**: `pageextension`/`tableextension` admitted as carriers (trailing anchor, measured), header regexes extended with the extension kinds ordered BEFORE `page`/`table` so a bare alternative cannot shadow them, and BC's numeric object types **14 = PageExtension** and **15 = TableExtension** mapped — both measured live, never read from documentation. `objectTypeName`'s unmapped fallback now warns once per type instead of silently stringifying, since that silence is precisely what hid this defect. **Document Output: 11,777 → 19,832 mutant sites, 162 → 438 instrumented files, nothing skipped**; the fully instrumented artifact compiles clean (0 errors, 9.07 MB). Both gates PASSED after the extension change: **`itest:bcdev` 3/10/3** and **`itest:tables` 63/10/2 with `untargetedTriggers` 0**, per-mutant. `itest:tables` ran on **Cronus283**, freshly prepared, because Cronus282 would not start (`hcsshim::ActivateLayer ... being used by another process`, surviving a host reboot — the user is rebuilding it). That makes the result stronger than a re-run would have been: the frozen numbers reproduced on a container built from scratch, so they describe the fixture and the code rather than accumulated state on one machine. | done |
| **R41** | **There is no way to run a subset of a project's mutants, so a large app has no cheap first run.** `lethal run` takes `--project` and mutates all of it; the only narrowing that exists is `--skip-known-survivors` (needs a prior run) and `--dry-run` (executes nothing). Continia Document Output generates **11,777 mutants in a single batch** — at an assumed 10 s per mutant that is ~33 hours before the first score, and the real per-mutant cost against a hosted environment is not yet measured. The instrumentation itself is not the problem (2.6 s to parse 551 files, 1.6 s to write): the barrier is that a first run on a real app is all-or-nothing. Wants a way to scope the executed set — by file glob, by object, or a bounded sample — so a project can be trialled before it is committed to. Hit 2026-07-27 attempting exactly that trial. | done (`8f24ea9`) — `--only <glob>`, repeatable, patterns union, matched on project-relative forward-slash paths. Narrows **spec generation only**: every file is still parsed into the project-wide semantic context (narrowing the parse set instead would make `--only` change verdicts via the inert Tier-2 shadowing guard — there is a red-checked test for exactly that), still compiled, still published. An unmatched pattern throws rather than selecting nothing. Recorded on `SessionReport.only` and rendered beside the score, since a narrowed score describes the slice, not the project. On DO: `"Al/Codeunit/**"` → 6,572 mutants across 82 files; one named codeunit → 163. |
| **R48** | **Nothing stopped a user pointing `lethal run` at a whole real app, and that path cannot succeed.** `--only` (R41), `--tests-only` (R45) and `--max-guards-per-batch` (R44) all exist, but every one of them is opt-in: the default invocation on Continia Document Output schedules **19,832 mutation sites**, which at that project's measured per-mutant cost (~19.5 s mean, p95 43 s) is days — and it fails long before that, because the artifact carrying every guard is severed by the hosting proxy at 362 s (R44). So the unscoped run is not merely slow; on a real project it usually cannot complete at all, and it fails after burning an hour. Filed 2026-07-27 as the cheapest single fix for first-run experience. | done — `assertRunSizeAcceptable` refuses above `LARGE_RUN_MUTANT_THRESHOLD` (1,000) before anything is published, naming the count, the file count, all three narrowing levers with which of them can change a verdict, and `--allow-large-run` to opt out. **Refusal rather than a warning** is the deliberate part: a warning about a cost that lands hours later scrolls past in the first second. `--dry-run` — whose whole job is answering "how big is this" — is exempt and instead PRINTS the same limit, so the narrowing conversation happens before a publish rather than after one. 1,000 sits above every fixture and above "try one codeunit" (163 on DO) and below `Al/Codeunit/**` (6,572), which is correctly refused. |
| **R51** | **`force-reset-lease` could not reach an env-tool environment at all — the one recovery path a proxy-severed run needs.** `forceResetLeaseFromCli` built its OData URL with `odataBaseUrl(server, instance)`, which injects port 7048. That is right for a container and wrong for a hosted environment behind a path-routed HTTPS proxy (`https://host/{envId}` on 443), and unlike `runFromCli` it ignored an explicit `bcdev.baseUrl`. Same class as R18/R21: the env-tool session works, and a command beside it silently assumes a container. **Hit live 2026-07-27** recovering the DO survivor sweep — a mutant run outlived its budget, BC kept executing it, the next session found the operation stranded (`AcquireLease reported operation-orphaned twice with an UNCHANGED marker`), and the documented §8 recovery answered `HarnessInfo unreachable: Unable to connect`. | done — `recoveryBaseUrl` honours a configured `bcdev.baseUrl` and REFUSES one whose path segments do not include `--instance`, naming both: this command clears safety state (op marker, active-mutant row, every outstanding lease credential), so the operator's flags stay authoritative for which tier is reset rather than a precedence rule nobody recalls under pressure. Matching is on PATH SEGMENTS, not `includes()` — a test caught that a one-character instance is a substring of almost any hostname, so the loose check would have passed exactly the mismatch it exists to catch. Proven live on the same stranded tier: reset OK, serverGeneration rotated, epoch -> 16. |
| **R52** | **`--resume` picked the most recent unfinished run even when it had recorded NOTHING.** `findResumableRun` selected on `finished_at IS NULL` alone, so an attempt that died before scoring a single mutant shadowed the older run holding real verdicts. Measured live 2026-07-27 recovering the DO sweep: a resume aborted at lease acquisition (R51), and the next `--resume` selected that empty run and reported `RESUMED: 0 verdict(s) carried`. Not silently wrong — the report says so, which is `resumedFrom` doing its job — but useless exactly when recovery matters, and an aborted run is precisely the kind most likely to have recorded nothing. | done — the query now also requires at least one row with a CARRYABLE verdict, with `CARRYABLE_VERDICTS` exported from `resume.ts` and passed in so the SQL and the carry rule cannot drift. Falls out for free: a run whose rows `invalidateBatch` rewrote to `error` (the unattested-artifact gate) is now UNRESUMABLE rather than resumable-but-empty, so its false survivors cannot even be reached. Red-checked with the predicate neutralised and parameter arity preserved — exactly 3 tests red, no collateral. |
| **R53** | **A mutant that makes a test hang is indistinguishable from a stranded operation, so it quarantines instead of scoring `timeout-killed`** — and it blocks every mutant after it. Measured on Document Output: **M0013 is `negate-conditional` on `until DOCustSetup.Next() = 0;`**, which becomes `<> 0` and never terminates. This is a mutant the suite arguably DOES catch (design §6.7: a timeout counts as killed, since the mutation caused observable misbehaviour), but LethAL only sees its own client-side abort, which it must treat as `in-flight-unknown` because BC may still be executing. Raising `--mutant-timeout-ms` cannot help: 180 s and 330 s both aborted, and 360 s is the hosting proxy's own ceiling. **This is what blocks R37 on this codeunit** — the sweep dies at mutant 13, so 125 of 138 mutants have never run. The fix is server-side: `RunMutant` must enforce its own time limit inside AL and RETURN a terminal `timed-out` result, which is a completed operation the client can score, rather than leaving the client to guess from a severed connection. Needs an AL change plus a protocol version bump. | **done** — the blocking half was fixed earlier; the scoring half is built, proven, and now GATED (`itest:hang`). The entry's stated fix ("`RunMutant` must enforce its own time limit inside AL") is IMPOSSIBLE — AL cannot preempt a running loop — so the mechanism is a SECOND session ending the first. Measured, not argued (`scripts/r53-probe/`, Cronus281): AL `StopSession` from a web-service session ends another busy in an AL loop; the stopped caller receives **HTTP 408 naming the AL StopSession call**; state committed before the hang survives; and — the measurement that mattered most — **`StopSession` silently no-ops for a nonexistent id, for 0 and for -1**, so it cannot report failure and its return value confirms nothing. Shipped behind opt-in `--stop-hung-sessions` (default off, because it ends a session on the user's server): the request is HELD OPEN at the budget, `StopHungRun` goes out on a second connection, and the run scores `timeout-killed` ONLY if the held request resolves as that 408. Server side, `TryStopHungRun` takes `TryRecoverOp`'s full predicate INCLUDING the tombstone check — without it, a lost ack after a run that already SUCCEEDED matches on attempt id alone and its recorded id names a live pooled session — and refuses `Op Session Id <= 0`. Gating is `MIN_CONTROL_VERSION` 1.0.0.10, not a protocol bump: adding an action breaks no wire contract (R28's purpose). Spec v2 (`docs/superpowers/specs/2026-07-31-r53-server-side-timeout-design.md`) records the three false-kill doors an adversarial review found in v1 and how each is closed. Gates: `itest:bcdev` 3/10/3 and `itest:tables` 64/9/2 (`untargetedTriggerCount` 0), per-mutant identical to frozen — but those prove ABSENCE OF REGRESSION only. **NO frozen gate exercises this feature: no fixture mutant hangs.** **Evidence route decided 2026-07-31: a dedicated hang fixture, NOT a Document Output run.** The DO route was investigated and rejected on cost/repeatability: no `lethal.config` for DO survives (the 2026-07-27 runs' config is gone), so it needs a config authored from scratch, DO's dependency chain installed on an environment, symbols downloaded, and DO's 106-file test app compiled and published — a chain whose own benchmark note already records a pre-existing source/dependency mismatch (`CDOTelemetryTests`) — and it would prove R53 exactly ONCE, unrepeatably, on a product repo. The fixture route is self-contained, runs on the containers already up, and becomes a permanent gate. **Design**: a NEW fixture pair (`sandbox-hang` + `-tests`) with its own app id and id range, so none of the four frozen baselines move — the target carries a `repeat … until X = 0;` whose `negate-conditional` mutant becomes `<> 0` and never terminates (M0013's exact shape), plus a new `itest:hang` asserting that mutant scores `timeout-killed` WITH `--stop-hung-sessions` and quarantines WITHOUT it. Bound every loop: a hang fixture that cannot be stopped wedges a container other gates need. **PROVEN end to end ON THE CONTAINER TOPOLOGY, 2026-07-31** on `fixtures/sandbox-hang` against Cronus281 — the same fixture, the same container, the flag the only difference. WITHOUT `--stop-hung-sessions`: the hanging mutant is `error` (*"could not be confirmed complete — container may be stranded"*) and only **2 of 5** mutants are scored, reproducing the entry's "blocks every mutant behind it" exactly. WITH it: **5 of 5** scored — both hanging mutants (`void-method-call` on the advancing call, `empty-block` on the advancing procedure's body) score `timeout-killed`, the three terminating ones `killed`. The off-run also exercised the whole of design §8 for real: durable tier quarantine, then `AcquireLease` refusing `operation-orphaned` on an unchanged marker, then NST restart -> `force-reset-lease` (generation `48beac82…` -> `091e3d6f…`, epoch -> 885) -> `clear-quarantine`. Note the AL session itself did NOT survive the client abort (container idle at 0.05% CPU afterwards) — but the marker row did, and the CLIENT cannot see either, which is precisely why an abort has to quarantine and why the 408 is the only trustworthy signal. **What the proof does NOT cover, per an adversarial review of the claim:** (a) the HOSTED topology that motivated the entry — R44 measured a 362 s proxy ceiling there, so a budget whose budget+grace approaches it has the HELD request killed at the proxy before BC can answer, the stop is never confirmable, and the run quarantines as before. Safe direction, but the feature may be structurally unable to score M0013 on the very environment R53 was filed from, and nothing warns when the budget nears the ceiling. (b) finish-at-budget+ε, the `already-completed` refusal, and the stop-ack-lost row of spec §6 are unit-covered only, not live. **Gated by `itest:hang`** (`LETHAL_ITEST_HANG=1 bun run itest:hang`, needs the gitignored `fixtures/sandbox-hang/lethal.config.local.json` and its test app published): the ON leg asserts 5/5 scored with both hangs `timeout-killed` AND BC's 408 text persisted in the result rows; the OFF leg asserts the run dies at the hang with `deadline-exceeded`, no `timeout` row, and a quarantined tier; teardown force-resets the stranded marker so the shared container is not left blocking `itest:bcdev`/`itest:envtool`. Which assertion guards which door was MEASURED by mutating the source, not assumed — including one mutation that does NOT fail the gate (routing `deadline-exceeded` to `timeout-killed` in the orchestrator's outcome switch is unreachable, because a hung run takes the stranded path first), recorded in the gate's own header. The review independently verified the claim itself against the run artifacts — BC's 408 text is persisted verbatim in the result rows (*"stopped the session (ID: 33) … by an AL StopSession call"*, 20098 ms against a 20000 ms budget), and `outcome: "timeout"` has exactly one producer, gated on that 408. |
| **R54** | **A resumed run's phase clocks did not add up, and the report hid it.** `mutantsMs` summed EVERY outcome's `durationMs`, including verdicts `--resume` CARRIED from a prior run — time this run never spent. Measured on a resumed Document Output sweep: **2200.4 s of "mutants" reported inside a 2109.7 s run**, with `overhead` clamped to `Math.max(0, ...)` so the contradiction showed as a plausible 0.0 s rather than as the negative number that would have exposed it. These timings exist to extrapolate what a bigger run will COST (R45/R44 both turn on them), so a carried duration is exactly the thing that must not be in them. | done — `carried` is recorded per outcome and surfaced on `MutantOutcome.carried` (provenance a consumer cannot derive: the verdict is real, but a different artifact in an earlier session produced it), and both `mutantsMs` and the per-mutant distribution exclude carried outcomes. **The first red-check FAILED to go red** — the resume fixture's carried durations are ~5 ms, far too small to breach the invariant, so the assertion passed with the fix reverted. Replaced with unit tests driving `buildReport` directly at a scale that discriminates (3 red on revert), and the weak assertion is left in place with a comment naming why it cannot fail. |
| **R55** | **The baseline and the mutants are measured on DIFFERENT RUNNERS, and a test that fails on the baseline's runner silently converts real survivors into `no-coverage`.** The entry first said "coverage collection makes tests fail" — **measured 2026-07-27 and that framing was wrong.** `BcDevMcpBackend` routes by mode (`bcdev-backend.ts`, `status()`'s own doc comment): with `coverageMode: "procedure"` (the DEFAULT) the BASELINE goes through the bc-dev-mcp hub, while every MUTANT goes through the fenced `RunMutant` OData action; with `"none"` both go through `RunMutant`. So the default path establishes the green set on one runner and produces every verdict on another. **A/B, back-to-back on one environment, same 56 tests, same source, only the mode differing, run in BOTH ORDERS:** `none` first -> GREEN 0/56 then `procedure` -> RED 12/56; and `procedure` FIRST -> RED 12/56 then `none` -> GREEN 0/56. Order-independent. (The first pass ran `none` first and the entry claimed that excluded elapsed state — it did not: it excluded drift FAVOURING the `none` arm, not state left BY it. An external review caught the overstatement and the reversed order was run to close it.) The failures are a property of the HUB path, not of coverage collection perturbing anything, and not of drift. Failure texts are ordinary (`Assert.IsTrue failed ...`, `Unhandled UI: Confirm ... has not been activated`), including on PURE-LOGIC tests with no database involvement. **Consequence chain, each step measured:** a test fails on the hub -> it leaves the green set -> its coverage observation leaves with it -> mutants in the procedure it covered fall to `no-coverage`. All **14** such mutants on Document Output sit in `CreateOrSendAutStatements`, and **9 of the 12** baseline failures are the Statement/AutStat tests that cover it. Plus **21** mutants reported `error` for want of a green covering test. Direction is safe — verdicts always come from the fenced path, which passes these tests (R37 confirms no false survivor) — so the damage is confined to WHICH mutants get scored, not to what a scored mutant says. **Fix — NOT yet implemented, and the obvious version is not good enough.** The candidate was: re-check a baseline failure on the fenced runner before dropping it from the green set. An external review (Fable 5, 2026-07-27) found three problems, two confirmed against the code: (a) **truncated coverage** — the observation being preserved comes from the FAILED hub run, which stopped at the failure point, so a test that dies at the activation Confirm BEFORE reaching `CreateOrSendAutStatements` never credits it and the 14 stay `no-coverage`; the fix would not deliver its own motivation, and this is unmeasured. (b) **plumbing** — it makes the baseline take the fenced path under `coverage: "procedure"`, and `orchestrator.ts` (~line 1982) states in terms that a call site without `resyncOpSeq` is "safe only while the backend reports `coverage: "procedure"` (the baseline then never takes the fenced RunMutant path)"; it also needs `pendingMutantId` proven null, and `baselineDuration` must take the FENCED timing since the mutant budget is `2 x baseline` and the runners differ by ~20x. (c) **its own verification would pass for the wrong reason** — "a no-op wherever the baseline is already green" means all four frozen gates pass whether the fix works, is inverted, or is deleted, so any red-check needs a fake whose two runners DISAGREE. See R57 (root cause), R58 (the real fix), R59 (the unsafe direction this leaves open). | done 2026-07-28 — SUBSUMED by R58's rollout: with `coverageMode: "fenced"` the default, baseline and mutants run on ONE session type and the dual-runner asymmetry this entry describes no longer exists on the default path. The candidate fix reviewed and rejected above is dead code, correctly: it was containment for a mechanism that has been removed rather than contained. The entry's mechanism remains true of `coverageMode: "procedure"` while that escape hatch survives (one release) |
| **R56** | **A docs-only commit deleted a fixture test's body, and the tables gate kept passing for days against a stale published test app.** Commit `76dfe48` rewrote a comment inside `InsertDoublesAmountWeak` (`fixtures/sandbox-data-tests`) and removed the procedure's statements and its closing `end;` with it. The project has not compiled since; `alc` reports `AL0104: Syntax error, 'end' expected`. `itest:tables` nevertheless passed every run, because LethAL publishes the TARGET on each run and treats publishing the TEST APP as the user's own workflow — so the gate measured a stale build while the source was broken. **R31 cannot catch this shape**: it fires when the server has no result for a discovered test, and here the server had an OLDER, WORKING build of every test, so nothing it measures diverged. Found 2026-07-27 while doing R36, by compiling the fixture — which nothing in the repo does. The body is restored, the app rebuilt at 1.0.0.1 and republished, and the gate re-verified. **The durable fix is not done**: nothing still compiles the test fixtures, so the next such edit repeats this. Wants an offline `alc` compile of `fixtures/*-tests` in the same place `bun run typecheck` lives, which would have caught it in seconds. | done — `bun run compile:fixtures` (`scripts/compile-fixtures.ts`) compiles all five fixture projects offline and is documented in CLAUDE.md's build loop. Red-checked by reintroducing `76dfe48`'s exact deletion: the check reports `AL0104: Syntax error, 'end' expected` and exits 1. A machine with no AL extension prints SKIPPED and exits 0 — silence would read as success, which is the failure being closed. |
| **R57** | **WHY the hub and the fenced runner disagree is unmeasured.** R55 established THAT they do — 12 of 56 Document Output tests fail through bc-dev-mcp and pass through `RunMutant`, order-independently — but not the mechanism. Candidates, none excluded: a different test-runner codeunit or isolation mode; UI-handler binding (one failure is `Unhandled UI: Confirm ... has not been activated`, which a bound handler would absorb); `TestPermissions` interplay (R1/R35 territory); something in bc-dev-mcp's own invocation. This matters more than it looks: **if the divergence is closable at the source, R55's containment is dead code**, and shipping containment without a root-cause item is precisely how R55 acquired its first, wrong framing. Cheapest next step is a single test run through both paths with the failure text and callstack captured side by side. | done (2026-07-27) — **measured, and it is neither flakiness nor state.** `fixtures/sandbox-probes/src/SessionCapabilityProbe.Codeunit.al` reports session capability from inside a `[Test]` body; run on Cronus281 through both paths, same target, same config, only `coverageMode` differing:<br>`HUB (procedure)`: **GuiAllowed=Yes, ClientType=Web**<br>`FENCE (none)`: **GuiAllowed=No, ClientType=ODataV4**<br>Company (`CRONUS Danmark A/S`) and user are IDENTICAL on both, which excludes the company and permissions hypotheses outright. The mechanism follows: BC treats a handler-less `Confirm` as UI in a GUI-allowed session and raises `Unhandled UI`, while a non-GUI session returns the Confirm's default silently and the caller takes the other branch — which is exactly what Continia Core's `IsAppActiveOrAskToActivate` does, and exactly the failure R55 recorded. Neither runner is wrong: each is correct for its own session type. The defect is that LethAL uses one to define the green set and the other to produce every verdict. **This also answers R59's mechanism** (a test relying on the interactive branch would pass on the hub and fail on the fence — the false-KILL direction) and it raises R60, which is larger than R55. |
| **R58** | **Collect coverage on the FENCED path, and delete the dual-runner asymmetry entirely.** Today the hub exists solely because it is the only source of per-procedure coverage, and that is the whole reason the baseline runs somewhere the mutants never do (R55). AL's Code Coverage Mgt. can be started and stopped from the control app around the test invocation inside `RunMutant` and returned in the payload — one runner, one behaviour, no green-set/verdict split. Costs an AL change plus a protocol version bump (and therefore a control-app republish everywhere, cf. R28). Would subsume R55 and shrink R31/R35's whole disguise class. Raised by external review; feasibility NOT established — "measured infeasible" is an acceptable outcome, silence is not. | **feasibility ESTABLISHED 2026-07-28 — the API is reachable and correctly shaped.** `docs/measurements/r58-coverage-feasibility-probe.al` compiles clean against `LethAL Control`'s EXISTING symbol set (sole dependency: Microsoft `Test Runner`), so **no new dependency is needed**: `Codeunit "Code Coverage Mgt."` with `StartApplicationCoverage()`/`StopApplicationCoverage()` — the exact start/stop shape `RunMutant` would wrap a test invocation in — plus `Record "Code Coverage"` exposing `"Object Type"` (an **Option**, so no `AsInteger()`), `"Object ID"`, `"Line No."` and `"No. of Hits"`, which is what `buildCoverageMap` keys on. **What is NOT established, and must be measured before any protocol work:** whether coverage collected inside a `RunMutant` OData session is COMPLETE — that session is `GuiAllowed=No`/`ClientType=ODataV4` (R57), and nothing yet proves the platform records the same observations there as on the hub. Building on that assumption would repeat R55's first framing. **ANSWERED 2026-07-28 — the fenced session records coverage IDENTICALLY to the hub.** `fixtures/sandbox-coverage-probe` run through both paths on Cronus281, same target, same config, only `coverageMode` differing: `coverageRows=262 totalHits=67 ownObjectRows=44` on BOTH, byte-identical, including the 44 rows for the probe's own codeunit. R57 independently established these are genuinely different sessions (`GuiAllowed=Yes/Web` vs `No/ODataV4`), so this is not one path measured twice — and the caution was warranted, since that same difference is what makes 12 DO tests fail on the hub. **R58 is therefore fully feasible and the remaining work is implementation, not investigation**: `Code Coverage` is LINE-level (`"Line No."`) where the hub returns procedure-level, so lines must be mapped back to procedures using the compiled artifact's symbol data — machinery `app-package.ts` already has for the hub payload. Doing it would delete the dual-runner asymmetry outright, subsuming R55, answering R59, and shrinking R31/R35's disguise class. **SERVER HALF SHIPPED 2026-07-28 (control app 1.0.0.8).** `RunMutantWithCoverage` wraps `RunMutant` in `StartApplicationCoverage`/`StopApplicationCoverage` and attaches a `coverage` array of `{objectType, objectId, lineNo, hits}` (zero-hit rows dropped — a real project's coverage table is large). Deliberately a SEPARATE action, not a parameter on `RunMutant`: adding a parameter changes the OData signature and BC validates the request shape before the action body runs, which is exactly how R25's stale-control-app failure presented. Existing callers are untouched. `MIN_CONTROL_VERSION` moved to 1.0.0.8 in lockstep; both containers republished and both gates re-verified per-mutant (`itest:bcdev` 3/10/3, `itest:tables` 64/9/2). One trap recorded: `ControlApi.Codeunit.al` declares a namespace, so it needs `using System.Tooling;` and `using System.TestTools.CodeCoverage;` — the R58 probes resolved the same names unqualified only because they declare no namespace and sit in the global one. **CLIENT HALF NOT DONE**, and it is the larger part: a `coverageMode` that routes the BASELINE through this action, plus mapping LINE numbers back to procedures. Note the lines refer to the INSTRUMENTED source LethAL itself wrote, so the mapping input is available locally — but that is the piece to get right, and it is where a wrong answer becomes a wrong verdict. **Spec written 2026-07-28: `docs/superpowers/specs/2026-07-28-fenced-coverage-design.md`** — one new opt-in `coverageMode: "fenced"`, the hub path untouched, nothing downstream of `CoverageMap` changed (so `coverageFilter`'s three attribution paths, the part R29 was fought over, are not reopened). Names three unknowns to PROBE before building: what BC's `"Line No."` actually denotes, whether the TARGET app's objects appear at all, and whether the lines refer to the INSTRUMENTED source. Specifies the two safety rules (a line in no known range emits an OBJECT-level entry, never a guess; an object with no line map at all throws) and the differential per-mutant gate that must pass before this can become the default — a unit-test pass proves nothing, since every frozen gate has a green baseline and the mechanism is a no-op for them. **Spec REVISED after external review, which found the first draft would abort every real run.** `CoverageArray` serializes the ENTIRE `Code Coverage` table — Base App, System App, Test Runner, Continia Core — so the draft's "an object with no line map throws" fired on the first Base App row; it is now scoped to objects the ARTIFACT DECLARES, with everything else skipped, which is what the hub path already does (`AppMethodIndex.lookup`'s doc: "callers should skip it"). Also fixed: a straight contradiction between rule 1 (out-of-range line -> object-level entry) and the error-handling section (same input -> throw); the line map must be scoped by `SymbolReference.json` rather than built over the batch dir, since R39 deliberately copies DO's 137 `.dependencies` sources whose published bytes come from their OWN apps (indexing them yields plausible-but-wrong member names — R29 with extra steps); `run()`'s `opts.coverage !== "none"` predicate would route a fenced baseline to the hub; `status()`, `capabilities()` and `orchestrator.ts`'s `resyncOpSeq` call site all need changes the draft omitted. Three unknowns added, one of them probably decisive: **does `StartApplicationCoverage` clear or ACCUMULATE across the baseline's per-test calls** (if it accumulates, test N is credited with tests 1..N-1's coverage, silently), plus payload size against R44's 362 s proxy ceiling, and whether line numbers are file- or object-relative on a multi-object file (unknown #1's probe used a single-object file and cannot distinguish them). A cheaper baseline-only coverage-diff oracle now runs BEFORE the per-mutant gate, and the gate compares covering-test sets and attribution rather than verdicts alone. **CLIENT HALF SHIPPED 2026-07-28.** `coverageMode: "fenced"` is wired end to end and opt-in: `line-map.ts` (`buildLineMap`, scoped by `AppMethodIndex.declaredObjects()`), `RunMutantTransport.runWithCoverage`, `bcdev-backend`'s three routing fixes (`run()` dispatched on `!== "none"` and sent a fenced baseline to the hub; `status()` branched on `=== "none"` and probed bc-dev-mcp, the dependency the mode removes; `capabilities().coverage` widened to a named `CoverageMode`), the `pendingMutantId`-is-null baseline assertion, and `cli.ts`'s config surface. Each of the three routing bugs was SILENT — every one left the mode looking configured while behaving like the old one — and each is now red-checked. **The server half needed one more change, found by measurement, not review: 1.0.0.8 was unusable.** Unfiltered, `RunMutantWithCoverage` never returned headers within 300 s even for `fixtures/sandbox-app`, whose whole test body is three lines — the client's fetch gave up, the transport classified it `in-flight-unknown`, and the session reported `killed=0 survived=0 noCoverage=0` while looking configured. The cost is the platform recording every line the Test Runner and Base App execute, so it does not shrink with the work and a bigger client timeout would not have fixed it. Control app **1.0.0.9** takes a `CoverageObjectIdFilter` (the artifact's own `idRanges` as an AL `SetFilter` over `"Object ID"`) and reports `coverageRunMs`/`coverageSerializeMs`/`coverageScannedRows`/`coverageEmittedRows`; the same call then answered in **126 ms / 1,546 bytes**. `MIN_CONTROL_VERSION` moved in lockstep; Cronus281 and Cronus283 republished; the Continia environment republished itself through `env-tool-session`. All four frozen gates re-verified per-mutant on 1.0.0.9 (`itest:bcdev` 3/10/3, `itest:tables` 64/9/2 with `untargetedTriggers` 0, `itest:alrunner` 3/13/0, `itest:envtool` 3/10/3). **Differential gate: RAN on all three targets, and DO FOUND A REAL DEFECT — R58 is NOT ready to become the default.** `sandbox-app`: identical verdicts 3/10/3, 0 covering-set changes, 0 attribution changes, 0 identity mismatches, and 4 member-level entries matching the hub's. `sandbox-data`: identical verdicts 64/9/2 — but that gate passed on the FIXTURE'S SHAPE, not on coverage equivalence: the fenced run named exactly ONE member (`Codeunit:79199::Active`, LethAL's own selector) plus two object-level entries, and its table-trigger mutants matched through `coverageFilter`'s `byObject` fallback. The hub is equally memberless there (triggers are absent from `SymbolReference.json`), so the comparison is honest — it simply exercises almost none of the line->procedure mapping. Only `sandbox-app` does. **Continia Document Output, both modes on `U:/Git/do-rel2`, 56 baseline tests each:** the hub reproduces the R37 benchmark EXACTLY (12 fail / 44 pass, 16 killed / 86 survived / 15 no-coverage / 21 error) and the fenced baseline is **56/56 GREEN** — R55's premise confirmed on one run pair. Per-mutant over the 105 mutants the fenced run reached, identity verified with 0 mismatches: **0 killed -> survived and 0 killed -> no-coverage, so the blocking criterion HOLDS**; 4 `error -> killed` and 7 `error -> survived` are pure gains (mutants the hub could only score through a red test); and **77 `survived -> no-coverage`** is the open question. **The line map is NOT the cause — measured, not argued.** The instrumented codeunit is 3,177 lines (original 364) and BC's rows carry `lineNo` 30..3171, so the lines are in the INSTRUMENTED frame (unknown #6, now properly settled). Bucketing the 410 real rows against the instrumented ranges names exactly the five procedures the run attributed; bucketing against the original frame names eleven, none matching. The rows for the other eight procedures are simply absent from the payload. Two readings remain with opposite consequences: either the fenced session genuinely does not execute them (R60, headless takes the non-GUI branch — in which case fenced coverage is RIGHT and the hub's 77 `survived` verdicts were mutated code that never ran), or fenced collection loses them (in which case R58 under-reports). Established either way: all eight missing procedures' covering tests pass on BOTH runners, so this is not R55's red-test mechanism. Discriminating experiment named and NOT yet run — execute one `CalcBalance` mutant through the fence against its five fence-green covering tests. **Method note:** a `--resume` run was briefly used as a gate input and appeared to show three lost kills; it showed nothing of the sort, because `--resume` carries prior verdicts while recomputing attribution, so each row mixed two runs. Resumed runs are not valid differential inputs. **No CLEAN COMPLETE fenced DO run exists**: mutant M0013 does not terminate (R53's class), each strand latches the session unsafe so the tail is never recorded (105 of 138) and leaves an op marker that quarantines the tier until an environment recycle + `force-reset-lease` + `clear-quarantine`. **Two blockers along the way were NOT what they first looked like, and both corrections matter.** (1) The hub reference run first failed with an Azure-token error, which read as "this environment needs Entra". It does not: VERIFIED in the NST that the dev endpoint has no auth path of its own (`Microsoft.Dynamics.Nav.Service.Dev.dll`'s `DevHostStartup` registers the shared `NavAuthentication` scheme), and `WebServiceCredentialsHelper.ValidAuthenticators` adds `WebServiceBasicAuthenticator` whenever `ClientServicesCredentialType` is `NavUserPassword`/`AccessControlService`, adding Bearer only when `AppIdUri` AND `WSFederationMetadataLocation` are both set — on these servers Bearer is never even constructed. The real cause was bc-dev-mcp merging `.vscode/launch.json` first: `U:/Git/DO/Cloud`'s configurations[0] is a Microsoft cloud sandbox (`Sandbox`/`TestAct`), LethAL never overrides `environmentType`/`environmentName`, so the cloud branch fired. `do-rel2` has a single OnPrem-shaped portal config and takes Basic. (2) Mutant **M0013** (`SendPeriodStatements_WhenCustomerSetToAutomatic_ShouldProcessCustomer`) does not terminate — stranded at both the 30 s and 120 s budgets, R53's class — and each strand leaves an op marker that quarantines the tier. **THE 77 ARE DISCRIMINATED 2026-07-28 — the fence is RIGHT, the hub's covering sets were false, and the answer was already in the dumps (no live run needed).** Inverting the hub dump's per-mutant `coveringTests` into per-test procedure sets: the five `ChangeAutomaticToManual_*` tests share ONE IDENTICAL 11-procedure set — `CalcBalance`, `BalanceDue`, `CheckForStatement`, `EntriesInPeriod`, `GetLastPerStatementSendingDate`, `GetLastStatementSendingDate`, `CreateAutomaticPeriodStatement`, `CreateAutomaticDueDateStatement`, `OpenEntriesInDueDatePeriod`, plus the two `ChangeAu(t)omaticToManual` they genuinely execute. An identical set across five tests with different bodies is structurally impossible as genuine per-test coverage, and it is PROVEN false for `...WhenNoLastDate...`: that test calls `ChangeAuatomaticToManual('CUST001', 0D, ...)` whose callee's FIRST statement is `if LastDate = 0D then exit;`, and its setup library (`CDOLibraryStatement.CreateSendCustomerStatementSetup`) only INSERTs records — so nine of the eleven are unreachable from it on ANY runner, hub or fence. The 11-set is exactly the `CreateOrSendAutStatements` call tree, i.e. the coverage of one of the 12 hub-FAILING AutStat tests, attached to five tests that never ran it. The client-side mechanism is precise: `buildCoverageMap` (bcdev-backend.ts) selects the payload entry with `wireCoverage.find(e => e.testObjectId === testCodeunitId)` and **never matches `testMethodId`**, though the wire protocol keys coverage entries per method — with a multi-entry payload the requested test is credited with another method's coverage (now R63). Consequences: **(a)** the 77 hub-mode `survived` verdicts were VACUOUS — mutants scored on the fence against tests that never executed the mutated code — and fenced `no-coverage` is the correct verdict, so the differential has NO unexplained moves and the gate PASSES; **(b)** DO's suite genuinely never executes those nine procedures on either runner — every test that tries flips the customer to Manual at the `ChangeAuatomaticToManual` early-exit or stops at the negative-balance guard — so the 77 are a DO test-gap finding, not a LethAL defect; **(c)** the previously-named discriminator (read `attestation.observedAny` on a fence run of M0120) would have been MISLEADING: M0120's hub-mode runs already carry `guardObserved: true`, and reading that as "the fence executed CalcBalance" is wrong because `observedAny` is ANY-guard — the five tests execute `ChangeAutomaticToManual`'s guards whether or not `CalcBalance` runs. The fence's per-test attribution is precise everywhere it can be checked: its 13 covered mutants are exactly `SendPeriodStatements` (12) plus one object-level entry — the only procedures containing mutants that any green fence test reaches. | **done 2026-07-28 — `"fenced" is the DEFAULT** (spec step 5): gate PASSED (0 killed->survived, 0 identity mismatches; the 77 survived->no-coverage moves explained as hub false attribution, R63), R63+R61 fixed in the same arc, and the default flipped via a single `DEFAULT_COVERAGE_MODE` constant. All four frozen gates re-verified per-mutant under the new default: `itest:bcdev` 3/10/3, `itest:tables` 64/9/2 with `untargetedTriggers` 0, `itest:alrunner` 3/13/0, `itest:envtool` 3/10/3 — the last now running REAL coverage through an environment bc-dev-mcp's SignalR hub cannot even reach (its portal 401s the negotiate). `"procedure"` survives one release as an escape hatch and is then deleted (spec decision 2). Remaining tail: no clean COMPLETE fenced DO run exists (M0013, R53's class — 105 of 138 reached), and the hub deletion itself is a separate change |
| **R59** | **The unsafe direction of the runner disagreement is undetected.** (2026-07-28: MOOT on the default path — R58's fenced default removed the second runner, so there is no hub green set for such a test to enter through. This entry now tracks ONLY the legacy `coverageMode: "procedure"` escape hatch, and closes when the hub is deleted per spec decision 2. Original text follows.) R55's measured direction is hub-red/fence-green, which is safe: verdicts come from the fence, and R37 confirms no false survivor. The REVERSE — a test the hub passes that the fence would fail — is not safe and nothing checks for it. Such a test enters the green set, then fails against every mutant it covers on the verdict path, and each of those reads as a KILL: a false kill, the one error class LethAL otherwise structurally avoids. Note R55's candidate fix does not close this, because it only re-checks tests the hub already failed. Nothing has yet measured whether this direction occurs; the fact that the observed disagreement went the other way on one product is not evidence that it cannot. | **done 2026-07-31 — and the entry's PREMISE IS REFUTED: the false kill it fears cannot occur.** Verified in code before anything was built (the lesson from R53/R19, where the prescribed fix was impossible as written): a failing covering run does NOT produce a kill. `runMutantsOnBackend`'s `v.outcome === "fail"` branch deactivates the mutant (`activate(null)`) and re-runs the same test through the FENCED transport (`coverage: "none"`), and only `confirm.outcome === "pass"` yields `killed`. A hub-green/fence-red test fails that confirmation and lands `error cause=unstable` — score-excluded, never a kill. **Reproduced live, not argued** (`docs/measurements/README.md` §R59): a throwaway `if not GuiAllowed then Error(...)` test covering `Sandbox Pricing.DiscountedPrice` (which no other test touches), published to Cronus281 and run in `coverageMode: "procedure"` — hub-green, fence-red, sole covering test of three mutants. All three came back **`error [unstable 3]`, zero killed.** **What WAS missing is the diagnosis, and that is what shipped.** The run said only "unstable test X: fails at baseline confirmation", which reads as flakiness in the user's own suite when the cause is deterministic and nameable: in a hub mode the green set is measured on `GuiAllowed=Yes`/`Web` and every verdict on `GuiAllowed=No`/`ODataV4` (R57). `packages/runner/src/runner-disagreement.ts` names it, at ZERO extra runs — in a hub mode a covering test was hub-green by construction, so a failed confirmation IS the observation, and it was simply being discarded. Surfaced per mutant (note), session-wide (`SessionReport.runnerDisagreement`), as a `runner-disagreement` caveat, and on the console at the same prominence as R35's permissions refusal. Verdict-neutral by construction — the mutants were already `error cause=unstable` and do not move. **What one confirmation cannot settle, and the note therefore does not claim:** a deterministic disagreement and an ordinary flaky test both present as one failed confirm; the note names both and points at `coverageMode: "fenced"`. A second confirmation was deliberately not spent on a mode scheduled for deletion. Red-checked four ways, including the false-POSITIVE direction (widening the hub-mode set to include `fenced` reddens the one-runner control) and the note-vs-field split (R35 exists because those two once disagreed) — all four red as predicted, no test survived its mutation. Gates: `itest:bcdev` 3/10/3, `itest:tables` 69/9/6, both per-mutant. Differential: `procedure` vs `fenced` on the clean fixture, **16 of 16 mutants identical, 0 differing**, and no diagnosis emitted — the detector is silent when the runners agree. Spec: `docs/superpowers/specs/2026-07-31-r59-runner-disagreement-design.md`. **The hub is NOT deleted**: no release has been cut (no git tags; `docs/releasing.md` says so in terms), so `coverageMode: "procedure"`'s one-release grace has not expired and deleting a documented escape hatch early was refused. |
| **R60** | **Every LethAL verdict describes the app's NON-GUI branch, and nothing says so.** Measured under R57: the fenced `RunMutant` path — the source of every verdict LethAL produces, on every backend and in both coverage modes — runs as **`GuiAllowed=No`, `ClientType=ODataV4`**. A developer running the same suite from VS Code runs GUI-allowed. So any AL guarded by `GuiAllowed`, or branching on `Confirm`/`Message`/`Page.RunModal`, takes the non-interactive path during mutation testing, always. Consequences: **(a)** a mutant inside a GUI-only branch can never be killed, because that branch never executes — it will be reported `survived` or `no-coverage`, and both readings are wrong ("your tests are weak here" when in truth LethAL never ran it); **(b)** a suite whose tests exercise UI handlers is measured against different behaviour than it was written for; **(c)** the mutation score's denominator silently includes code that was unreachable by construction. None of this is stated anywhere in the README, `design.md`, or the report's `validity` block. At minimum it belongs in Limits and probably wants a `guardObserved`-style signal, since R46 already proved this project cannot distinguish "ran and survived" from "never ran" without attestation. Not measured yet: how much real AL sits behind such guards — worth counting on Document Output before deciding how loud this needs to be. | done — **measured first, then stated.** `scripts/measure-gui-guarded.ts` (written under this item, run 2026-07-31 against Continia Document Output `DocumentOutput/Cloud`, 551 `.al` files, 19,850 mutation sites): **62 sites — 0.3% — sit lexically inside a `GuiAllowed`/`Confirm`-guarded branch.** A lower bound that is not hiding a large category — the `if not GuiAllowed then exit;` whole-procedure shape occurs **3 times in 551 files** — and the script's 5.7% upper bound is dominated by `Message`, which is a no-op and causes no unreachability at all. **That number is why this is a stated limit and not a `guardObserved`-style per-site signal:** 0.3% does not justify the machinery, and the fact is structural (no LethAL backend runs GUI-allowed), so there is nothing to detect per run. Shipped: `validity.executionContext` — REQUIRED, not optional, so it cannot be absent on the clean full-reliability run whose reader is likeliest to quote the score unqualified — plus a console line on every run, and a README Limits entry carrying the measurement and its method. The two backends report DIFFERENT bases: bcdev cites R57's direct measurement, al-runner says it is inferred from being a headless CLI and **not separately measured**, because claiming otherwise is the static-claim failure R7/R8 exist to stop. The measurement also corrected the entry's own framing: `Confirm` does not skip its branch, it FORCES the default (so the non-default arm is the unreachable one), and `Page.RunModal` ERRORS rather than no-opping (95 uses in DO) — a different failure mode that can fail a test for reasons unrelated to the mutant. Gate: `itest:bcdev` PASS, 3/10/3, per-mutant identical to frozen. |
| **R61** | **The hub's coverage map emits object-level entries for objects the target does not own, and nothing said so.** Surfaced by R58's per-test differential oracle, which was looking for the opposite failure. `buildCoverageMap`'s object-level fallback fires whenever `AppMethodIndex.lookup` misses AND the object declares no local procedure — and a Base App or test-framework object satisfies both, because it is not in the target's `SymbolReference.json` at all. Measured on `sandbox-data`: the hub attributes `Codeunit:198`, `Codeunit:423`, `Codeunit:2000000002`, `Table:5330`, `Codeunit:130011/130012` (the test framework), `Codeunit:71002` (LethAL's own control app) and nine more to individual tests. Harmless TODAY — no mutant lives in those objects, so no covering set changed on either fixture — but it is `byObject` credit for code the run does not own, which is one manifest-id coincidence away from `coverageFilter`'s object-level fallback returning a non-empty WRONG set, i.e. R29's exact shape. The fenced path does not have it: rows are scoped to `AppMethodIndex.declaredObjects()` before anything is emitted. Fix is the same scope check on the hub path, but it moves hub attribution and so must not ride along inside R58's differential. | done (0d10ded) — the declared-objects scope check shipped inside R63's fix (same function, one shared verification): foreign objects are skipped before anything is emitted, pinned by a new unit test and the re-verified frozen gates |
| **R62** | **`AppMethodIndex` never resolves an extension's method names, so every `tableextension`/`pageextension` procedure falls to object level.** `SYMBOL_ARRAYS` (app-package.ts) listed six object kinds and omitted `TableExtensions`/`PageExtensions`; MEASURED 2026-07-28 by compiling a probe with one of each — the arrays exist, are named exactly that, and carry `Id`/`Name`/`Methods` like the rest, so the data was there all along. Consequence: an extension's coverage missed at member level and landed as an object-level entry — safe, one precision level coarser, and invisible. (Post-R63 the fall-through branch is the object-level emission, not the deleted `findLocalProcedureNames` expansion this entry first cited.) No fixture declares an extension, so nothing gated it either way. | done — `SYMBOL_ARRAYS` now includes both extension kinds (14/15, the numbers measured live under R40), `DECLARED_OBJECT_ARRAYS` folded into the single loop since the two sets are now identical, and a unit test pins member resolution AND `declaredObjects()` for both kinds (red-checked: removing the arrays fails it). Frozen gates unaffected BY CONSTRUCTION: no fixture's SymbolReference declares an extension array, so both indexes are byte-identical there — the live re-run would prove nothing |

| **R63** | **The hub credited tests with coverage they could not have produced — measured on Document Output as 77 vacuous `survived` verdicts — and the mechanism was NOT the one first filed here.** R58's differential showed the five `ChangeAutomaticToManual_*` tests sharing one identical 11-procedure covering set equal to the `CreateOrSendAutStatements` call tree, proven unreachable for `...WhenNoLastDate...` by its call tree (literal `0D` LastDate -> immediate exit; the setup library only INSERTs records). The first filing blamed `buildCoverageMap` matching the payload by `testObjectId` only — **that hypothesis was then MEASURED WRONG, twice.** (1) A raw-payload probe (`packages/runner/scripts/probe-r63-hub-payload.ts`, run against the DO environment on BC28 and the sandbox fixture on Cronus281) shows a single-method run returns exactly ONE coverage entry keyed by a STABLE per-method `testMethodId` with per-method-correct content (fixture: 6 vs 4 procedures, disjoint as written; DO: `WhenNoLastDate` -> exactly `ChangeAuatomaticToManual` + one unresolved local — right). No multi-entry payload, no cross-call accumulation, nothing for a wrong `find` to grab. (2) The 11-set is not another method's coverage — it is EXACTLY `{the one public the test genuinely executes} U {ALL TEN local procedures of codeunit 6175297}`. **The real mechanism: `buildCoverageMap`'s local-procedure EXPANSION.** `AppMethodIndex.lookup` resolves public members only (locals are absent from SymbolReference.json — re-verified against DO's compiled app, whose `Codeunits[].Methods` lists exactly the 5 publics; the method ids are a hash that did not yield to java/djb2/fnv/crc32/dotnet-classic candidates — recorded so nobody re-tries blindly), so the fallback credited EVERY local `findLocalProcedureNames` could grep from source whenever ONE unresolvable member executed. The fixture runs (one local, one test) made it look exact; on DO (ten locals, five tests) it detonated into R29's exact shape: confident, non-empty, WRONG covering sets, and 77 mutants in never-executed procedures scored `survived`. The expansion's own doc comment called it safe because it "only ever turns a no-coverage skip into an actual test run, never hides a real kill" — true, and beside the point: it manufactures false SURVIVORS, the error class this project treats as the worst kind. **Fix shipped:** (a) the expansion is deleted — an unresolvable member is emitted at OBJECT level, feeding `byObject` (the R29-safe branch, one precision level coarser), and `findLocalProcedureNames` is gone with it; (b) R61's scope check landed with it — coverage for objects the compiled artifact does not declare (Base App, test framework, `Codeunit:71002`) is now SKIPPED on the hub path, same rule the fenced path has; (c) since `coverageFilter`'s object-level FALLBACK 1 is trigger-only, a mutant in a genuinely-executed LOCAL would now report `no-coverage` while running (measured live: the fixture's three `LogAudit` mutants moved survived -> no-coverage, failing the frozen gate) — so the manifest now carries `procedureScope: "local" | "public"` (schemata emits it from the parse; absent on triggers) and `coverageFilter` has an unnamed-member fallback keyed on it: local mutant + member miss + some unnameable member observed executing in the object -> covered at object grain, attribution `object`. The `=== "local"` gate is the load-bearing part: a PUBLIC procedure whose member key misses DID NOT EXECUTE, and widening it would re-manufacture the vacuous survivors this item exists to delete; a pre-existing test ("an ordinary procedure mutant is NOT credited by an object-level-only entry") pins exactly that doctrine and went red on the un-gated version. Every piece red-checked by revert (expansion deletion, scope skip, fallback, gate, scope emission). Frozen gates re-verified per-mutant on the new code: `itest:bcdev` 3/10/3 (LogAudit mutants back to `survived`, now via the honest object-grain path), `itest:tables` 64/9/2 with `untargetedTriggers` 0, `itest:alrunner` 3/13/0. Hub-mode verdicts on DO are unchanged by construction (same covering sets, coarser honest attribution) — the verdict-level answer for DO remains R58's fenced mode. | done (0d10ded) — mechanism measured (probe + symbol-file + call-tree proofs), fix shipped and gated |
| **R64** | **The bcdev backend could never run on a Linux or macOS host, and the publish tool was not pinnable.** Two defects, one report (PR #1, StefanMaron, measured against a live Linux Docker BC 28.1 container). (a) `defaultAlToolPaths()` hardcoded `bin/win32/` regardless of `process.platform`, so every non-Windows run spawned a Windows PE `alc.exe`/`altool.exe` that cannot execute — and the released Linux x64/arm64 and macOS binaries advertise this backend, so that was every non-Windows user. It surfaced as an opaque, MESSAGE-LESS `Error` many layers up (see R65). The extension ships per-RID builds side by side — verified here 2026-07-31 against `ms-dynamics-smb.al-18.0.2498801`: `bin/win32/{alc,altool}.exe`, `bin/linux/{alc,altool}`, `bin/darwin/{alc,altool}` — so the fix is a per-platform dir + suffix, with `platform` injected into `defaultAlToolPaths` so the branch is testable from any host, and a loud throw (not a guessed `linux`) on a host the extension ships nothing for. (b) `bcdev.altoolPath`, the publish-side twin of R43's `alcPath`: the extension's bundled `altool` 17.0.2273547 silently ignores `BC_SERVER_USERNAME`/`BC_SERVER_PASSWORD` for `publishapp` and its `auth login` is AAD-only, while the `microsoft.dynamics.businesscentral.development.tools` 18.x dotnet tool reads them and publishes. Follow-up fix on the same branch: the "no AL extension installed" gate keyed off the DISCOVERED install, so a config naming both tools was still refused on the container path — it now keys off the resolved pair (`resolveAlToolPaths`), which is what makes the override usable on a machine with no VS Code extension at all. Evidence: `packages/runner/src/publisher.ts`, `packages/runner/src/cli.ts`, `docs/benchmarks/runs.jsonl` (`bc-linux-sandbox-container`, the ledger's first plain-container rows). | done (PR #1 + follow-ups) — live gate reproduced exactly on Linux (`itest:bcdev` equivalent 3 killed / 10 survived / 3 no-coverage, authoritative, baselineGreen); both halves red-checked by revert |
| **R71** | **`SwapRecXRec`, scoped to `OnValidate`/`OnRename` — the signal is measured, the operator is not built.** R33's experiment answered only the `Modify(true)` shape (`differ=No`) and the conclusion drawn was a blanket no-go. Follow-up probes on Cronus281 (fenced path, `fixtures/sandbox-probes/src/Tier2Phase2Probe.Codeunit.al`) measured the two shapes it never touched: field `OnValidate` driven by `Validate(Amount, 250)` gives `rec=250 | xrec=100 | differ=YES`, and `OnRename` driven by `Rename('R2')` gives `rec.No=R2 | xrec.No=R1 | differ=YES`. Those are exactly the triggers where AL reads `xRec` (`if F <> xRec.F then` change detection; old-vs-new key), so swapping them is observable and killable. What the operator needs beyond the site predicate: it must NOT claim `OnModify`-shaped sites (measured equivalent — a survivor there means nothing), which makes it the first Tier-2 operator whose targeting depends on the enclosing TRIGGER KIND rather than on the receiver. | done (this commit) — `packages/builtin-tier2/src/swap-rec-xrec.ts`, scoped to field `OnValidate` and table `OnRename` by `OBSERVABLE_TRIGGERS`, the first Tier-2 operator whose targeting reads the enclosing TRIGGER KIND rather than the receiver. `OnModify` is refused BY MEASUREMENT (`differ=No`), and a conformance case pins that refusal so the site population the original measurement actually refuted cannot be re-acquired quietly. Two further refusals are structural rather than cautious: an ASSIGNMENT TARGET (`xRec.Amount := 5`) is refused because a mutant that fails to compile poisons its whole batch and sends bisection after a phantom — cheaper to refuse than to establish which AL versions accept it — and a BARE `Rec` passed whole to a procedure is refused because the callee is not locally visible. **Proven on a claim, not only on refusals:** `Data Scope Probe.Tracked`'s OnValidate carries the fixture's first `swap-rec-xrec` site and `Data Tests.ScopeProbeTracksFieldChange` KILLS it live (M0088). That was deliberate — R73 had just closed exactly this gap for `RemoveCommit`, and shipping another operator with zero fixture sites would have reopened it one operator later. `itest:tables` moves 81/12/10 over 103 to **84/12/10 over 106** (117 raw); re-freeze verified by diff — 3 added, **0 pre-existing changed**. |
| **R72** | **BC's "cannot run codeunit in a write transaction" is not distinguished from a genuine kill.** Spec §5 requires it for `RemoveCommit`: deleting a `Commit()` before a `Codeunit.Run(...)` makes the platform refuse the call, which kills the mutant for a reason that says nothing about assertion quality and inflates the score with platform noise. Deliberately NOT built with R33: nothing in any fixture has ever produced that message, so the detector would match an ASSUMED string — the R31 shape, where a reworded literal silently stops a diagnosis firing — resting on an unmeasured platform claim. An adversarial review named a second hole it must survive: `if not Codeunit.Run(...) then Error(PostFailedErr, GetLastErrorText())` re-wraps the artifact in the caller's own message, so a text match sees nothing and the reader counts a platform artifact as test quality. Order of work: produce the artifact once live from `sandbox-probes`, pin its exact text per runner, THEN write the detector — and word it as best-effort, never as a classification. The verdict must stay `killed` (a diagnosis must not move a verdict; design §6.7's timeout precedent). | **MEASURED 2026-08-02, detector NOT yet built — and the measurement changed what it must be.** `fixtures/sandbox-probes/src/WriteTxnProbe.Codeunit.al` produced the artifact live on Cronus281, three tests (A/B controls, because a single test reported an AL-callstack line that maps ambiguously onto the source). Results, IDENTICAL on the hub and on the fenced path — the per-runner pin this row asked for: write alone is fine; `Codeunit.Run` alone is fine; write-then-`Codeunit.Run` ABORTS with BC's generic **"An error occurred and the transaction is stopped. Contact your administrator or partner for further assistance."** Full method and reasoning: `docs/measurements/README.md` §"R72". **Two of the three findings change the design.** (a) The refusal is REAL — that half of the premise holds. (b) It is **NOT catchable**: `Ran := Codeunit.Run(...)` does not return false, the error escapes and the test dies, so the adversarial re-wrap hole this row was written to survive (`if not Codeunit.Run(...) then Error(PostFailedErr, GetLastErrorText())`) **cannot occur for this artifact** — the caller never regains control. (c) The text is BC's GENERIC transaction message, naming neither `Codeunit.Run` nor the rule, so a detector keyed on the string ALONE would fire on any platform-stopped transaction and mislabel genuine kills as platform noise — the unsafe direction. The detector must therefore condition on the OPERATOR (`lethal.remove-commit` at a site preceding a `Codeunit.Run`) with the text as corroboration, stay best-effort in wording, and leave the verdict `killed`. **Blocked on R73**, deliberately: no fixture has ever generated a `remove-commit` mutant, so the detector cannot be proven to fire against anything real, and a detector proven only against a constructed string is the R31 shape this row exists to avoid. | **blocked — and the premise is now MEASURED FALSE in the shape a gate can reach.** **NEW DATA POINT (R13, 2026-08-02):** the opener need not be a WRITE — a bare `LockTable()` (no insert, no modify) followed by `Codeunit.Run` produces the same abort, measured fenced on Cronus281 (`fixtures/sandbox-probes/src/Tier3Probe.Codeunit.al`, arms M2a/M2b). That narrows the candidate list by one: it is not the kind of transaction opener. It does NOT settle the question — R13 varied the opener and never the frame, and both its arms sit in the `[Test]` method’s own frame, so the frame stays a CANDIDATE variable rather than the established one. R72 remains blocked on the same measurement. The artifact reproduces in a `[Test]` method (write, then `Codeunit.Run` -> "An error occurred and the transaction is stopped.", both runners) but NOT when the write and the `Codeunit.Run` sit in an ordinary codeunit called from a test: `Data Commit Ops.CommitThenRun`'s `remove-commit` mutant was PREDICTED killed by the refusal and came back **survived** on the live gate, with the callee having run and flagged its row. Both measurements stand; the shapes differ. So the detector still has nothing real to fire on, and building it now would again prove it only against a constructed string — the R31 shape this row exists to avoid. **Next step is a measurement, not code:** determine WHICH shape triggers the refusal (call depth, `TableNo`, the test runner's own transaction), because a diagnosis that fires on the wrong shape mislabels genuine kills as platform noise, which is the unsafe direction. Everything already established stays valid: the refusal is real where it occurs, it is NOT catchable (so the adversarial re-wrap hole cannot occur), and its text is BC's GENERIC transaction message, so the detector must key on the operator with the text only as corroboration. Evidence: `docs/measurements/README.md` §R72 and §R73; `fixtures/sandbox-probes/src/WriteTxnProbe.Codeunit.al`. |
| **R73** | **`RemoveCommit` ships proven on its refusals and unproven on its claims.** Registering it changed no fixture's mutant set — both `Commit` sites in `fixtures/sandbox-data` are the shadowed negatives (`Data Shadow` declares `Commit`; `Data Ops.ShadowedBuiltins` calls it qualified) and both are correctly refused, verified offline at 93/16 specs. So no gate has ever generated, let alone killed, a `remove-commit` mutant. Adding a positive is not free: a `Commit()` in an object declaring no `Commit` lands `no-coverage`, not `survived`, unless a covering test comes with it, and the honest fixture shape is a transaction-boundary test — write, `Commit`, raise, then assert the write survived. That also settles what R33 left unmeasured: whether a committed write survives a later uncaught error under the platform runner's isolation, which is the operator's actual kill mechanism. (`Commit()` itself is measured to EXECUTE in the fenced run.) | done (this commit) — `codeunit 79312 "Data Commit Ops"` gives the fixture its first POSITIVE `remove-commit` sites, and `Data Tests.CommittedWriteSurvivesFailure` KILLS one: the first `lethal.remove-commit` mutant any gate has ever killed. The transaction-boundary shape this row prescribed is exactly what was built (write, `Commit`, raise, assert the write survived), and the platform claim under it was MEASURED FIRST on `sandbox-probes` rather than assumed: a committed write survives a later uncaught error (`survived=Yes`), an uncommitted one is rolled back (`survived=No`), identical on the hub and the fenced path — see `docs/measurements/README.md` §R73. Had that come out the other way the fixture would have failed unmutated and the red baseline would have been blamed on the wrong thing. `itest:tables` moves 71/9/10 over 90 to **81/12/10 over 103** (114 raw), re-freeze verified by diffing the old and new `tables.baseline.json`: 13 added, **0 pre-existing changed**. The second site added here (`CommitThenRun`) did NOT behave as predicted — see R72. |
| **R70** | **The symbol table keys scope on the BARE object name, so a page named after its table silently overwrites the table's variables — and that naming is the standard BC convention.** `buildSymbolTable` (`packages/engine/src/semantic/symbol-table.ts`) calls `globals.set(ownerName, …)` / `procedures.set(ownerName, procs)` with `header.name`, so `table 50000 "CDO Setup"` and `page 50000 "CDO Setup"` share one key and whichever is parsed LAST wins wholesale. Reproduced 2026-07-31 with exactly that pair: `globalsOf("CDO Setup")` returns the PAGE's `Helper: Record Customer` and the table's own `Helper: Integer` is gone; `localsOf("CDO Setup","P")` likewise. **Measured frequency on Continia Document Output Cloud: 13 names shared across kinds, 12 of them page+table** — i.e. the ordinary "card page named after its table" convention, not an edge case. The consumer is `claimsRecordMethod`'s `lookupVar`, and the direction is the DANGEROUS one: a receiver that should be unresolvable (rule 4 refusal) inside the table can resolve through the page's declaration and be CLAIMED, and a receiver that resolves to a DIFFERENT table sends rule 3's shadowing guard at the wrong table. A wrong claim mislabels the mutation and, under §3.2 dedup precedence, deletes the correct Tier-1 mutant at that site. Verdicts themselves stay honestly measured, which is why this is a wrong-CLAIM finding rather than a wrong-verdict one. `objects` is unaffected (both entries are present), so `buildTypeTable`/`buildCallerIndex` do not have this defect. The fix is the one R30 just applied one namespace over — key scope by (kind, name) — and it must not change `resolveObject`, which is already kind-aware. No fixture has a cross-kind name collision, so all four frozen gates are blind to this and a fixture is part of the fix. Found while self-reviewing R30's kind-namespacing. | done (`5921e06` fix + this commit's fixture) — scope keyed by `objectScopeKey(kind, name)`, reproduced first and red-checked at BOTH layers. **The red-check is the part worth reading:** reverting the consumer reddened 23 tests, which looks conclusive and was not — every one was a generic "can it find a variable at all" test that would break for ANY key mismatch, and NONE constructed two objects of different kinds sharing a name, so the property had no test where the wrong claim happens. `claimsRecordMethod — a page named after its table (R70)` now covers it in both directions. **The gates are no longer blind:** `table 79309` / `page 79324 "Data Scope Probe"` (same name, different kind) is live in `itest:tables`, which moved 69/9/9 over 87 to **71/9/10 over 90** (99 raw). Its detector is the OPERATOR NAME at the `SetRange` site — measured offline as `lethal.void-method-call` with the fix and `lethal.remove-setrange` without, with raw specs 99 -> 100 while DEPLOYED stayed 90, i.e. one wrong claim gained and one correct Tier-1 mutant DELETED by §3.2 precedence, exactly the mechanism this row described. Re-freeze verified by diffing the old and new `tables.baseline.json`: 3 entries added, **0 pre-existing entries changed**. `itest:bcdev` 3/10/3 and `itest:alrunner` 3/13/0 re-run unchanged. |
| **R69** | **`TestPage` is UNSUPPORTED on the fenced path, so every test that opens one fails there — and the original framing of this row ("hangs") was wrong.** Measured 2026-07-31 on Cronus283 while building R30's fixture: a `[Test]` whose body is `DataMainList.OpenView(); DataMainList.Close();` never returned. LethAL saw only its own abort, correctly classified it `in-flight-unknown` (BC may still be executing), and latched the tier quarantined at BASELINE — `killed=0 survived=0 noCoverage=0 baselineGreen=true`, plus the usual §8 recovery (`force-reset-lease` → serverGeneration rotated, epoch → 91). **This is much larger than the fixture that found it.** Every verdict runs in the fenced `GuiAllowed=No` / `ClientType=ODataV4` session (R57/R60), real BC suites use `TestPage` heavily, and R58 made the fenced path the DEFAULT for the baseline as well as the mutants — so a customer suite with TestPage tests may be unable to complete a baseline at all, which reads as "LethAL hung" rather than as a session-type limitation. Direction is safe (a quarantine, never a wrong verdict) and R53's `--stop-hung-sessions` may already make it survivable, but that is UNTESTED here — R53's stop path was proven on mutant runs, and this hang is at baseline. **What is not yet known**, and each is one probe: whether it is TestPage as such or this page's shape; whether it hangs or waits on something answerable; whether `--stop-hung-sessions` converts it into a scored/failed baseline test rather than a quarantine; and whether Document Output's 1,246-test suite (which completes on the fence) contains TestPage tests at all — if it does, the failure is narrower than this row assumes and the row must be corrected. Evidence: `fixtures/sandbox-data-tests/src/DataTests.Codeunit.al` (the removed test and why), `fixtures/README.md` §"Extension objects". **CORRECTED 2026-07-31 by a dedicated probe — `TestPage` does not hang, it is REFUSED, fast.** `fixtures/sandbox-probes/src/{ProbeList.Page.al,TestPageProbe.Codeunit.al}` — a code-free list page and a test that does `OpenView(); Close();` — run on Cronus281 through both paths: **hub** (`GuiAllowed=Yes`/`ClientType=Web`): opens and closes fine (`MEASURED testpage-open=OK`, 1766 ms). **fenced** (`GuiAllowed=No`/`ClientType=ODataV4`): fails in **87 ms** with `Unexpected CLR exception thrown.: System.NotSupportedException: Specified method is not supported. at Microsoft.Dynamics.Nav.Runtime.NavSession.CreateNavTestService()`. The run COMPLETED — no hang, no quarantine, no stranded operation; score `n/a` with all 16 mutants `no-coverage` because the baseline went red. **So the hazard is real but different from the one filed**, and milder: a suite using `TestPage` does not wedge a run, it loses those tests from the green set at baseline, and every mutant they alone covered becomes `no-coverage` — reported with the `baseline-red` caveat, never as a wrong verdict. **Sized on a real project: 9 of Continia Document Output's 104 test files declare a `TestPage`, 38+ declarations** (`CDO Email Editor` alone 18 times), so a DO run on the R58 fenced default silently drops that slice of the suite. **What remains genuinely unexplained, and must not be attributed to TestPage:** the ORIGINAL observation on Cronus283 (a `TestPage` test that went `in-flight-unknown` at baseline, quarantined the tier and scored nothing). If the platform refuses `CreateNavTestService` in 87 ms, that test should have failed fast too. The difference was the page (over a table carrying `OnModify`/`OnValidate` triggers, with a `pageextension` `OnOpenPage` that WROTE a row) and the container. That strand is its own open question — it was a hang with an unknown cause, not a TestPage property. Direction is safe either way (a red baseline test, or a quarantine — never a wrong verdict). Fix candidates, in cost order: name the `CreateNavTestService` refusal in the report the way R27/R35/R59 name theirs, so a reader sees "these tests cannot run on this path" rather than N unexplained baseline failures; and decide whether a `TestPage`-using suite should be routed to a hub baseline deliberately (which re-opens R55's dual-runner asymmetry, so not free). **SPIKE 2026-08-01 — the client-services (native WebSocket) route is de-risked on transport and session-capability, but NOT yet measured end-to-end for a TestPage test on that exact path.** Probed via `U:/Git/bc-mcp` (native BC client-services protocol) against Cronus281; scripts left at `bc-mcp/scripts/r69-*.ts`. Established, all measured today: (a) **transport works** — bc-mcp opens the AL Test Tool (page 130451), reads its lines, invokes actions, and answers its dialogs over the native WebSocket; the correct "Choose" StrMenu answer is `SaveValue` the option's TEXT label (`"&All"`, not a numeric index) then `InvokeAction Ok(300)`. (b) **the in-session Test Runner FIRES over this path** — invoking `&Run Tests` reset the result column to pending and ran `Test Runner - Isol. Codeunit`, PROVEN because the runner itself raised `You tried to invoke the CodeUnit object with the ID 80002 from the object Test Runner - Isol. Codeunit. An object with that ID does not exist in the current application compiled with emit version 28014` — the runner executed in-session and tried to invoke the suite's (now-deleted) codeunit. (The suite's persisted `Success` rows were STALE from a removed app `80002 "CG-AL-E002"`; freshness was checked, not trusted.) (c) **CORRECTION (Fable review 2026-08-01) — link (c) is measured on the WRONG transport and is correlation, not proof.** The `MEASURED testpage-open=OK | GuiAllowed=Yes | ClientType=Web` reading (79218 on Cronus281) came via `bcdev_test_run`, which is a THIRD transport — a dev-endpoint **SignalR `TestRunnerHub`** (`bc-dev-mcp/src/core/hubs/test-runner-hub.ts`), NOT bc-mcp's client-services WebSocket. TestPage-open has therefore been observed on the SignalR-hub session and REFUSED on the ODataV4 session, and **never observed on a genuine client-services session.** The refusal is `NavSession.CreateNavTestService()` throwing — a **virtual-override question on the concrete `NavSession` subclass**; `(GuiAllowed, ClientType)` is a tuple the session *reports*, not the mechanism that grants the capability, so two different session classes can both report `Web/Yes` yet differ on the override. The bc-mcp client-services session's own `GuiAllowed`/`ClientType` were not captured. **The seam I named was also wrong:** it is NOT "does the isolation runner preserve/​fork the session" — `TestIsolation=Codeunit` is *transaction* isolation, a TestRunner codeunit runs via `CODEUNIT.RUN` in the driving session (the 80002 error surfacing on the driving WebSocket confirms this). The real seam is link (c) itself, on-path: is the client-services `NavSession` subclass one that overrides `CreateNavTestService`. **Practical risk is nonetheless LOW:** bccontainerhelper's `Run-TestsInBcContainer` / `ClientContext.ps1` drives the **Command Line TestTool (page 130455)** over `Microsoft.Dynamics.Framework.UI.Client` — the SAME client-services protocol bc-mcp reimplements — and TestPage-heavy suites pass through it in CI across the BC ecosystem daily; the composite (client-services session + isolation runner + TestPage) is precedent-proven at scale. So close it by measuring, cheaply. **The one unmeasured seam concretely:** running `79218` on the bc-mcp client-services transport. Over the STOCK AL Test Tool (page 130451) this is blocked by seeding — stale DEFAULT suite codeunit gone; "Get Test Codeunits by Range" filter did not match `79218` through this driver; the "Select Tests" modal lookup is NOT drivable via bc-mcp (`openedPages=[]`, form absent from `ctx.forms`). **Corrected next step (was: build a control action first — WRONG ORDER):** drive **page 130455 "Command Line TestTool"** over bc-mcp instead — it ships in the same Microsoft Test Runner app as 130451, exists to be driven programmatically, and seeds via a plain editable range-filter field whose `OnValidate` populates the suite (no modal, no "Get by Range" dialog — neither 130451 failure transfers). Zero new AL, runs the already-published 79218, ~30 lines off `bc-mcp/scripts/r69-run79218.ts`. **Highest-value single measurement:** open 130455 → `SaveValue "79218"` into the range field → confirm the line appears → set/confirm the isolation Test Runner → Run → read 79218's line for `MEASURED …` produced ON the client-services transport — closes link (c) on-path and records the tuple the test actually sees in one reading. **Probe 0 (ILSpy `Microsoft.Dynamics.Nav.Ncl.dll`, enumerate `NavSession` subclasses overriding `CreateNavTestService`)** is the FALLBACK if the 130455 run fails ambiguously — it discriminates capability-failure from driver-failure but proves neither seeding nor readback. Build the dedicated control action AFTER, as the batch-runner's first component, inheriting the measurement. **Design constraints the batch-runner MUST carry (Fable):** (1) one WebSocket session per BATCH + `TestIsolation=Codeunit` means each test-codeunit run's rollback also rolls back any UNCOMMITTED control-action writes since the last commit — the loop must `Commit()` after every result row and work-queue state change, or a later test's rollback silently eats earlier results (this project's empty-vs-empty signature bug); (2) the control table needs a per-invocation NONCE, load-bearing (the stale-suite discovery generalises); (3) the Cronus283 `in-flight-unknown` hang is NOT retired by this route — it can wedge the batch WebSocket mid-queue, so the design needs a hang story distinguishing "not run" from "run, unrecorded" for items after a wedge. Still a product call (R55/R57/R58): the path runs `GuiAllowed=Yes`, so verdicts carry interactive semantics; scope to fence-REFUSED tests with per-test runner provenance. (Sizing caveat: "9 of 104 files" counts FILES, not tests or mutant coverage — the lost verdict slice may differ. **AND THE FIGURE ITSELF IS NOW UNVERIFIED — see R76:** no Document Output checkout on this machine reproduces 104 test files or a `CDO Email Editor`; the largest available has 17 test codeunits / 146 tests, of which 3 files / 27 tests (18%) declare a TestPage. The finding's SHAPE holds; the number does not.) **SEED FOLLOW-UP 2026-08-01 — the corrected zero-AL route (page 130455) is ALSO bc-mcp-seed-blocked; BOTH stock test-tool pages are dead ends for seeding over bc-mcp, so the control action is REQUIRED, not optional.** Drove page 130455 over bc-mcp (`bc-mcp/scripts/r69-cmdline.ts`): it opens fine and IS an editable list (a trailing `DraftRecord…` row, editable `Codeunit ID`), but it does NOT expose the header range-filter field Fable expected — seeding needs a validated repeater-cell edit, which bc-mcp cannot commit. Overwriting an existing codeunit-header row's `Codeunit ID` to `79218` returns `success:true` with `newValue:"79218"` echoed at the control, but the record never changes (re-read still shows `80002`) — `Codeunit ID` is a primary-key/non-editable field on an existing Test Method Line, so the `SaveValue` is swallowed with no `OnValidate` cascade (two `InvokeCompleted`, `durationMs:0`, zero `PropertyChanged`); forcing a commit by moving the current row does not help. Writing to the draft row instead commits a new line with `Codeunit ID = 0` — the value never binds on insert either. So across THREE routes (130451 range-filter no-match; 130451 "Select Tests" modal not drivable; 130455 repeater edit not committing / key field), seeding a stock suite fails at the bc-mcp DRIVER level — the RUN action works, only the stateful edit needed to seed does not. **Net: the empirical result re-justifies the dedicated control-app action** (a fixed-shape action that runs one method in-session and writes to a table — no suite, no repeater edit, no modal), converging back to "build the control action" as MEASURED-necessary. Remaining non-AL alternative if a direct reading is still wanted first: SQL-seed the `Test Method Line` rows out-of-band then invoke RUN over bc-mcp (fragile — method lines are normally reflected, and raw tenant-DB writes are risky), or take the reading via bccontainerhelper's own `ClientContext` (proves Microsoft's client-services protocol per Fable's precedent, but not bc-mcp specifically). **SPIKE RESOLVED 2026-08-01 — PROVEN on-path. Link (c) is closed: a genuine bc-mcp client-services session DOES open a `TestPage`.** The control-app batch runner shipped (LethAL Control **1.0.0.11**, objects 71011 "LC Batch Queue" / 71012 "LC Batch Result" / 71013 "LC Batch Runner" codeunit / 71014 "LC Batch Runner" page, plus Control API OData actions `SeedBatchItem` / `ClearBatch` / `GetBatchResults`; published tenant-installed on Cronus281 + Cronus283). Probe `bc-mcp/scripts/r69-batch-spike.ts`: seeded `79218`/`ReportsTestPageOpen` over OData (derived port 7048, Basic auth, `company`+`tenant` — **verified working**, the assumed auth held), drove page 71014's `Run Batch` action over the native client-services WebSocket, read results back over OData. Result: **`MEASURED testpage-open=OK | GuiAllowed=Yes | ClientType=Web`** — and 79218 raises that string at its LAST line, reachable only after `ProbeList.OpenView()` AND `.Close()` both returned, so it cannot be produced by anything but a real TestPage open. **On-path is proven by the AL stack trace, not by inference:** `Test Page Probe(79218).ReportsTestPageOpen ← Test Runner - Mgt(130454).RunTests ← Test Runner - Isol. Codeunit(130450).OnRun ← Test Suite Mgt.(130456).RunAllTests ← LC Run Method(71007).RunOneMethod ← LC Batch Runner(71013).RunBatch ← LC Batch Runner(Page 71014)."Run Batch"-OnAction` — the chain bottoms out at the page action the WebSocket invoked, so the test executed inside the client-services session, through the platform's own isolation test runner, exactly as a productized batch would. **The tuple did NOT discriminate, and that vindicates Fable's correction rather than the original framing:** the client-services session reports `Yes/Web`, the SAME tuple the SignalR hub reported — so the tuple was never the mechanism, and the real question (does the concrete client-services `NavSession` subclass override `CreateNavTestService`) is now answered **YES empirically**, making **Probe 0 (ILSpy) unnecessary**. `Run Batch` returned `success=true` with **zero dialogs**, and the run took well under a second — no hang, no prompt, no quarantine. **What this does NOT prove, stated plainly:** (a) it ran **ONE** item, so nothing here exercises session reuse across many methods — `design.md:276` ("never batched, never reused across tests") is still an OPEN design tension, untested; (b) `Attested`/`Mutant Id`/`Target App Id` were INERT (`attested:false` with no mutant in play), so the attestation-leak fix is unmeasured; (c) the commit-per-row guarantee is written but never exercised against a real multi-row rollback; (d) the Cronus283 `in-flight-unknown` hang is NOT retired — it can still wedge the batch WebSocket mid-queue; (e) `GuiAllowed=Yes` means verdicts produced here carry INTERACTIVE semantics (an unhandled `Confirm` raises instead of defaulting) — still a product call (R55/R57/R58), unresolved. Phase 2 (productization) is gated behind a spec + `spec-adversary` pass on exactly these. **REPORT FIX LANDED 2026-08-01 (076b726) — the cheap half of R69 is done and is INDEPENDENT of the client-services route.** `packages/runner/src/testpage-unsupported.ts`: `describeTestPageUnsupported` anchors on the `CreateNavTestService` MECHANISM token rather than the generic "Specified method is not supported" prose (red-checked: broadening the anchor makes an unrelated `NavStream.Seek()` `NotSupportedException` claim a TestPage diagnosis). Surfaces as `SessionReport.testPageUnsupported`, the `tests-testpage-unsupported` caveat, a `TESTPAGE UNSUPPORTED ON THIS PATH` console section at the same prominence as `PERMISSIONS REFUSED`, and a per-mutant note. **Deliberately a SEPARATE category from R35, not another branch of it:** R35's refusal has a one-line fix in the reader's own source (`TestPermissions = Disabled`), this one has NO target-side fix — so `unsupportedCoverageNote` lets the ACTIONABLE permissions cause win when a mutant is covered by one of each (red-checked: flipping that precedence tells a reader whose problem IS fixable that nothing can be done). Diagnosis-only — never decides a verdict, never suppresses a failure. Red-checked FOUR ways (anchor, precedence, session-level set wiring, per-mutant note wiring); each went RED for its predicted reason and GREEN on restore; full suite 1443 pass / 0 fail. **Two honest limits of the fix:** (a) the per-mutant note fires only when the refused test still REPORTED coverage — a TestPage refusal that dies before executing anything reports none, so its mutants land plain `no-coverage` and only the SESSION-level caveat names the cause; attaching the reason to those individually would claim a causation that is not established (the mutant may be genuinely uncovered) and was deliberately not done. (b) The localization question is REASONED, not measured: the anchor is a .NET member name rather than English prose so it should survive a non-English server where R35's regex does not (R66), but no localized server has been probed and it must not be claimed as a property. **TASK 0 MEASURED 2026-08-01 — the gate is UNANSWERED, not failed: the probe measured an UNWIRED INSTRUMENT, not the transport's capability. (First recorded as ABANDONED; corrected the same day on review — the measurement was right, the inference was not.)** Probe `U:/Git/bc-mcp/scripts/r69-coverage-probe.ts` (copied from the proven `r69-batch-spike.ts` wiring — `odataPost`/`odataPostString` against `cfg.bc.odataUrl`, `SessionFactory`/`OpenPageOperation`/`ExecuteActionOperation` over the native client-services WebSocket), run against Cronus281 exactly as `docs/superpowers/plans/2026-08-01-r69-phase2-batch-runner.md` Task 0 specifies: two full `ClearBatch`→`SeedBatchItem`→fresh-WebSocket-session→`Run Batch`→`GetBatchResults` round trips, one method per session. **POSITIVE reading** (79218 `ReportsTestPageOpen`, which opens `TestPage "Probe List"` — an instrumented site) returned `result` = `{"name":"Test Page Probe","codeUnit":79218,"codeunitName":"Test Page Probe",...,"testResults":[{"method":"ReportsTestPageOpen","result":1,"message":"MEASURED testpage-open=OK | GuiAllowed=Yes | ClientType=Web",...}]}` — **no `coverage` key anywhere in the entry.** **NEGATIVE CONTROL reading** (79213 `ReportsSessionCapabilities` — the closest existing analog to an empty `[Test]` body; no literal empty-body test exists in `fixtures/sandbox-probes`, and none was published, per the task's constraint against publishing new AL in this task) returned the equivalent shape with `"message":"MEASURED GuiAllowed=Yes | ClientType=Web | Company=CRONUS Danmark A/S | UserId=SSHADOWS"` — also **no `coverage` key**. **Root cause, confirmed by reading the deployed AL before the live run and then corroborated by it:** `extensions/lethal-control/src/BatchRunner.Codeunit.al` codeunit 71013 `RunBatch` never calls `Codeunit "Code Coverage Mgt."`'s `StartApplicationCoverage`/`StopApplicationCoverage` — that pairing exists only in `ControlApi.Codeunit.al`'s `RunMutantWithCoverage`, a *different* OData action used exclusively by the fenced path. So on the deployed **1.0.0.11** build, `GetBatchResults`' `result` field is built solely from `Test Suite Mgt.TestResultsToJSON` and cannot carry a `coverage` array. **That is a property of THIS BUILD's `RunBatch`, not of the client-services transport** — and the distinction is the whole verdict. `RunMutantWithCoverage` is a THIN, GENERIC WRAPPER: `StartApplicationCoverage()` → run → `StopApplicationCoverage()` → attach `CoverageArray(filter)`. `Code Coverage Mgt.` is a platform codeunit and nothing in that pairing is fenced-path-specific; `RunBatch` simply never asks. **So the probe answered "the instrument was not switched on", which is not evidence about what the instrument would read.** The gate's actual question — does a client-services session RECORD per-procedure coverage, and is it clean — remains open and cannot be asked without first adding the pairing to `RunBatch`, i.e. a slice of the plan's own Task 1. **GATE: UNANSWERED. Phase B is NOT abandoned on this reading**; the plan's Task 0 is defective as written (it assumed `RunBatch` already collected coverage, which no one had checked) and must be re-run after the instrument is wired. Recording the wrong verdict here first, and correcting it, is itself the R69 pattern: a prior that looked measured and was not. Full raw console output and both verbatim JSON entries: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-0-report.md`. **TASK 0b MEASURED 2026-08-01 — the instrument is now WIRED and the gate ANSWERS CLEAN. GATE: PROCEED to Phase B.** `extensions/lethal-control/src/BatchRunner.Codeunit.al` codeunit 71013 `RunBatch` now wraps each queue item's `Runner.Run()` in `Code Coverage Mgt.`'s `StartApplicationCoverage`/`StopApplicationCoverage`, attaches `{coverage, coverageScannedRows, coverageEmittedRows}` per row via `"LC Batch Queue"."Coverage Filter"` → `ControlApi.CoverageArray`, and `GetBatchResults` unpacks those three keys onto each result row (`ControlApi.Codeunit.al`, `SeedBatchItem`/`GetBatchResults`). Published as LethAL Control **1.0.0.12**, verified `IsInstalled=True` on both Cronus281 and Cronus283. **`SeedBatchItem`'s real signature, verified against the deployed AL rather than assumed: THREE arguments — `codeunitId: Integer; method: Text; coverageFilter: Text`** — a trailing `coverageFilter` added to the prior two, not the six-argument surface a draft of this task's brief guessed at; the probe was corrected to match the real AL before calling it. `bc-mcp/scripts/r69-coverage-probe.ts` updated in place (not rewritten) and re-run against Cronus281 with `CoverageObjectIdFilter`/`coverageFilter` = `79200..79299` (`fixtures/sandbox-probes/app.json`'s own idRange — mandatory per `RunMutantWithCoverage`'s doc comment, an empty filter does not return inside 300s). **POSITIVE reading** (79214 "Rec XRec Probe Tests" / `ReportsRecVsXRecOnModify` — inserts then `Modify(true)`s a row of table 79201 "Rec XRec Probe", whose `OnModify` trigger raises, i.e. it executes a DIFFERENT in-range object than its own test codeunit): `"coverage":[{"objectType":1,"objectId":79201,"lineNo":29,"hits":1},{"objectType":5,"objectId":79214,"lineNo":27,"hits":1},{"objectType":5,"objectId":79214,"lineNo":29,"hits":1},{"objectType":5,"objectId":79214,"lineNo":30,"hits":1},{"objectType":5,"objectId":79214,"lineNo":31,"hits":1},{"objectType":5,"objectId":79214,"lineNo":32,"hits":1},{"objectType":5,"objectId":79214,"lineNo":36,"hits":1},{"objectType":5,"objectId":79214,"lineNo":37,"hits":1},{"objectType":5,"objectId":79214,"lineNo":40,"hits":1}],"coverageScannedRows":31,"coverageEmittedRows":9` — coverage correctly NAMES the table (79201, objectType 1) whose trigger the test actually executed, alongside the test codeunit's own lines (79214, objectType 5). **NEGATIVE CONTROL reading** (79213 "Session Capability Probe" / `ReportsSessionCapabilities` — same closest-analog test as Task 0, since `fixtures/sandbox-probes` still has no literal empty-body `[Test]` and none was published, per this task's constraint against publishing new AL): `"coverage":[{"objectType":5,"objectId":79213,"lineNo":34,"hits":1}],"coverageScannedRows":13,"coverageEmittedRows":1` — reproduced identically on two consecutive full round trips. **Judgement on the negative control's single row:** it is the test codeunit's OWN executed line (its `Error(...)` call, line 34 of 79213) — self-attribution of a session that genuinely ran, not leakage of unrelated activity; ZERO objects other than the running test codeunit's own id appear. This is how the filter is INTENDED to be used in Phase B — a design assumption this reading does not verify, not an established fact about an architecture that does not exist yet: mutation targets and the tests that cover them are meant to live in a target app's own idRanges, disjoint from the test codeunit's — this fixture uniquely co-locates test and "target-like" fixture objects in one idRange, so the running test's self-coverage is a fixture artifact, not evidence the mechanism would misattribute a real mutation target. **Also observed, on two consecutive full round trips:** the negative control (run second, moments after the positive) emitted NO rows for the positive reading's objects (79201/79214). **INFERRED, not proven:** consistent with `Code Coverage Mgt.`'s Start/Stop bracket not leaking across separate batch-loop iterations or separate WebSocket sessions on the same NST — but one ordered A-then-B pair is not a test of the mechanism; a leak that only appears under concurrency, under a longer queue, or in the reverse order would not show up here. **`coverageScannedRows` > 0 on BOTH readings proves collection ran** (never the `scanned=0` "instrument recorded nothing" failure mode); **`coverageEmittedRows` on the negative control names no foreign object**, satisfying the brief's actual load-bearing question (does the bracket attribute activity the test never executed) even though the literal count is 1, not 0 — the brief's stated expectation of a literal zero assumed a true empty-body test that this fixture does not have. Exact commands, full console transcripts and the corrected verdict logic (an earlier verdict branch in the probe mis-graded this exact reading as `INCONCLUSIVE` by testing `coverageEmittedRows === 0` literally instead of "no foreign object id"; caught and fixed before this row was written): `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-0b-report.md`. **TASK 7 MEASURED 2026-08-01 (live gate, all three itests) — Phase 2 shipped (Tasks 0a-6, commits 5f4660d..9aed254: batch transport, two-gate router, per-verdict runner provenance) and validated against real containers, with one structural limit found.** `itest:bcdev` and `itest:envtool` (Cronus281) reproduce the frozen **3 killed / 10 survived / 3 no-coverage** per-mutant identical to the committed baselines; queried the results DB directly, and every one of the 32 verdicts across both runs carries `runner: null` (== `"fenced"` by `RunnerKind`'s own contract) — zero `client-services` leakage, as required, since neither fixture declares a TestPage. `itest:tables` (Cronus283) reproduces the frozen **69 killed / 9 survived / 6 no-coverage** over 84 mutants, per-mutant identical to `tables.baseline.json`, also 100% `runner: fenced` — but ONLY with the pageextension unreached, i.e. unchanged from before Phase 2 existed. **Reinstating `PageExtCountsMatchingRelated` (the exact test this row's 2026-07-31 entry named) reproduced the ORIGINAL hang, not the 87 ms fast refusal — twice, deterministically, on a freshly recovered container.** First attempt: the fenced session went `in-flight-unknown` on it at baseline (`quarantined: {"reason":"baseline test in-flight-unknown running PageExtCountsMatchingRelated"}`, `killed=0 survived=0 noCoverage=0` for the WHOLE 84-mutant run), and the container's NST was found genuinely stuck `StopPending` afterward — recovery needed a Docker-level restart (`Restart-BcContainerServiceTier` itself timed out) plus `force-reset-lease`. Second attempt, same test, freshly recovered container: byte-identical outcome (the NST stayed `Running` this time, but the fence still answered `in-flight-unknown` and quarantined), recovered again with `force-reset-lease` alone. **So the code-free probe's 87 ms REFUSED finding does not generalise past a code-free page**: a page whose `SourceTable` carries real triggers/a FlowField, extended by a pageextension that WRITES a row from `OnOpenPage` (exactly `Data Main List` / `Data Main List Ext`, R30's fixture), still wedges the fenced session at baseline — confirming, not resolving, this row's own "what hung on Cronus283 is still unexplained" residual. **This also means Phase 2's router cannot rescue this shape by construction, independent of whether gate 2 would pass it**: `runSession`'s baseline-discovery loop (`packages/runner/src/orchestrator.ts`, the `for (const ref of tests)` loop preceding all mutant scheduling) runs every discovered test once on the fenced path and calls `quarantineInFlight` — latching the WHOLE session unsafe and stopping — the instant ANY one of them comes back `in-flight-unknown`, unconditionally, before gate 1's routing logic (which only pattern-matches a FAST, completed baseline failure's text) is ever reached. A baseline hang takes the entire suite down with it, not just the mutants the hanging test would have covered. The test was therefore NOT committed (reverted to the pre-existing no-test state, comment updated with this measurement) so `itest:tables` stays a working regression gate; the pageextension mutants remain `no-coverage`, unchanged. No `packages/` change was made pursuing this — closing it needs one (e.g. isolating TestPage-suspected baseline tests behind their own short deadline, run separately from the rest of baseline discovery, so one hang does not quarantine tests that never needed rescuing), which was out of Task 7's scope and is filed here rather than attempted. Full transcripts, recovery commands and the per-mutant join: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-7-report.md`. | open — **capability PROVEN 2026-08-01** (client-services route measured end-to-end: seed → in-session batch loop → TestPage open → readback, `GuiAllowed=Yes`/`ClientType=Web`); **Phase 2 Task 0 gate PASSED 2026-08-01** — 1.0.0.12 wires `Code Coverage Mgt.` into `RunBatch`, and a re-measured positive/negative pair shows collection running clean (scanned>0 both readings; negative control names zero objects other than its own running test codeunit). **Phase B (control-app changes) is UNBLOCKED** per the task-0b brief's gate; the shipped Phase-3 report-naming fix (`describeTestPageUnsupported`, 076b726) remains delivered value independent of Phase B's outcome. **Phase 2 (Tasks 0a-6) SHIPPED and reviewed** (batch transport, two-gate router, per-verdict runner provenance — commits 5f4660d..9aed254). **Task 7 (live validation) measured 2026-08-01: router correctness CONFIRMED on the fenced/client-services boundary it targets** (bcdev+envtool unchanged at 3/10/3, 100% fenced, no leak) **but it structurally cannot rescue a baseline HANG (only a fast refusal)**, so the pageextension mutants this row opened stay `no-coverage` and this row stays open. **HONESTY QUALIFIER (final whole-branch review, 2026-08-01): the client-services transport is BUILT AND UNIT-TESTED BUT NOT WIRED.** `SessionConfig.routedTransport` is declared (`orchestrator.ts:511`) and consumed (`orchestrator.ts:2817-2861`), but grep across production code (everything outside `packages/runner/tests/orchestrator.test.ts`) finds NOTHING that constructs it — so `runSession` always takes the `routedTransport === undefined` branch, in every one of the live gates measured above. Task 7's clean bcdev/envtool/tables figures therefore demonstrate **NO REGRESSION** from Phase 2 landing on the fenced path that already shipped — they are NOT evidence of a working routed path in production, and must not be read as Phase 2 having been live-validated end-to-end. Two blockers must be fixed before any production code is allowed to construct `routedTransport`: R74 (routed survivors discard the attestation) and R75 (routed verdicts discard `coverageAttribution`) — see below. **GO/NO-GO THRESHOLD, WRITTEN 2026-08-02 BEFORE THE NUMBER WAS KNOWN** (this row has produced five retracted over-generalisations in two days; pre-commitment is the countermeasure, and it is honoured either way). The measurement: on the Document Output corpus at `/u/Git/DO`, under `coverageMode: "procedure"`, **the share of deployed mutants whose covering-test set is NON-EMPTY and consists ONLY of tests that declare a `TestPage`** — the exact set the whole R69 apparatus would recover, and the first R69 figure stated in the unit that matters (every earlier one was test-denominated or unreproducible; see R76). Denominator: mutants that received a verdict (killed + survived + no-coverage). **≥ 5% → R69 CONTINUES** in the order R74/R75 → R78 → wire → four-gate per-mutant join. **< 5% → R69 CLOSES** as "named, not recovered; recovery measured unprofitable", the routed path and control-app objects 71011-71014 are DELETED (a dead OData surface on customer containers is liability, not insurance; the spec plus `47fbd19` is a complete re-derivation kit), and `testpage-unsupported.ts` plus the fixture pair are KEPT. Why 5%: what remains to build is not the ~30 lines of R74/R75 but a permanent client-services surface on every customer container plus a second verdict path to keep honest across four gates; below 5% the recovered mutants move the mutation score by less than the gaps this project already accepts and names (R40 alone concedes 41% of a real app's sites), so the surface would cost more than it buys. The absolute count is recorded alongside the share for the record, but **the share is the decision variable** — one number, applied without re-argument. **MEASURED 2026-08-02: 439 of 19,081 deployable mutants = 2.30%. BELOW THRESHOLD. R69 CLOSES — named, not recovered; recovery measured unprofitable.** Full method, bounds and reproduction: `docs/measurements/README.md` §"R69's go/no-go". Corpus `U:/Git/do-rel2` (Cloud 28.4.0.0, the version the environment actually runs) through the hub on Continia environment `f19aca88`; 19 TestPage tests (18 executable), 1,197 other tests. Generous bound was 980 (5.14%); subtracting sites any other test also covers leaves 439. Every variant stays below: own-source only (excluding the 137 `.dependencies` files whose objects belong to other apps) **370 of 15,331 = 2.41%**; plus every mutant of the one page whose TestPage test is absent from the deployed test app, **2.37%**. The one test codeunit that died mid-run can only make the exclusive set look BIGGER, so the missing direction is the safe one. **Two earlier framings of this row's size were wrong and are corrected here:** R76's "9 of 104 cannot be reproduced against any checkout on this machine" is false — it reproduces exactly against `do-rel2` (R76 was reading `U:/Git/DO`, a different, 38-file checkout) — and the figure was a `grep -l` artifact regardless: 9 files MENTION `TestPage`, **5** contain a TestPage test, and per test it is 19 of 1,287 = **1.5%**. | done (`c1da575`, measured unprofitable 2026-08-02) — routed path and control-app objects 71011-71014 DELETED, verified per-mutant on all four gates afterwards (`itest:bcdev` 3/10/3 · `itest:alrunner` 3/13/0 · `itest:tables` 69/9/9 over 87 · `itest:envtool` 3/10/3, on LethAL Control 1.0.0.14); `testpage-unsupported.ts`, its report wiring, and the fixture pair with its three `no-coverage` mutants are KEPT as the permanent measured statement of what is not recovered |
| **R78** | **The R69 router's gate 1 is DEAD in the only configuration where a real TestPage suite survives — so the routed path could never have fired for the case it was built for.** Gate 1 (`selectRoutedTests`, `testpage-router.ts`) keys on the FENCED BASELINE's failure text: it routes a test only if `describeTestPageUnsupported` matches the baseline failure. But a suite containing a real TestPage test only completes baseline at all under `coverageMode: "procedure"` (R76), and on that path the TestPage test **PASSES baseline** — no failure message exists, so gate 1 never fires. **This was already OBSERVED and misread.** R76's measured run reported the 4 pageextension mutants as `error(unstable)` from the fenced VERDICT run refusing fast (140-181 ms, `CreateNavTestService`). That was recorded as "verdicts always run fenced" — true, but it buried the real finding: the refusal had MOVED from baseline to the mutant run, which is exactly where gate 1 cannot see it. The router was watching the wrong event. **Fix is a redesign of the routing TRIGGER, not of the transport:** key on `describeTestPageUnsupported` applied to the MUTANT RUN's refusal, not the baseline's. That is the actual design work standing between R69's built-but-unwired path and a scored TestPage mutant — and it is independent of R74/R75, which are separate discards. Raised by Fable 2026-08-02 while checking a synthesis that was otherwise half wrong. | **MOOT (2026-08-02) — R69 closed, the router is DELETED.** The measurement stands and is why this row was worth writing: gate 1 keyed on the FENCED BASELINE's failure text, and in the only configuration where a real TestPage suite survives baseline (`coverageMode: "procedure"`) that test PASSES baseline — so the gate could never fire for the case it existed for. The fixture pair it produced is KEPT (`Data Value Card` / `Data Value Source`, 3 `no-coverage` mutants pinned in `tables.baseline.json`) as the permanent measured statement of what R69 does not recover. |
| **R79** | **A COMMENT containing `codeunit <id> "Name"` silently truncates test discovery — every `[Test]` after it in the file is dropped, and the mutants those tests covered are reported `no-coverage` with no warning anywhere.** `discoverTests` (`packages/runner/src/discovery.ts`) is static and regex-based: `CODEUNIT_HEADER_GLOBAL = /codeunit\s+(\d+)\s+("([^"]+)"|(\w+))/gi` splits the file into codeunit SECTIONS, and a section is skipped entirely unless `SUBTYPE_TEST` matches inside it. The regex does not skip comments, so **prose** matching that shape opens a bogus section; the remainder of the file falls into it; it contains no `Subtype = Test;`; and every test from that point on vanishes. **FOUND BY ACCIDENT 2026-08-02, which is the alarming part** — while building the R78 fixture I wrote a doc comment reading ``codeunit 79308 "Data Value Source".GetValue`` above a new `[Test]`. Source had 22 `[Test]` methods; `discoverTests` returned **21**; the new test was simply absent. Rewording the comment to `` `"Data Value Source"` (object 79308) `` restored 22. Nothing in the pipeline noticed: the live run reported `baselineGreen=true`, `baselineTests {total: 21, failing: 0}`, an empty `unsupportedTests`, no caveats — and the three mutants that test exclusively covers came back `no-coverage`. A dropped test is INDISTINGUISHABLE from a test that never existed. **Why this is worse than a normal bug:** the direction is silent under-reporting of coverage, which this project's whole design is built to make impossible to do quietly (cf. R31's stale-test-app, R35's permissions refusal — both of which are NAMED in the report). A real suite is far likelier to hit this than a fixture: referring to `codeunit 50100 "Sales Post"` in a comment above a test is ordinary AL commenting. Every test below it disappears from the green set, and its mutants read `no-coverage`, which a reader interprets as 'nobody tests this'. **Fix candidates, in order:** (a) strip comments before sectioning (`//` to EOL and `/* */`), which is a small, testable change and the obviously correct one; (b) require the header match to be at line start with only whitespace before it; (c) at minimum, WARN when a file yields a section with no `Subtype = Test` yet contains `[Test]` — a loud approximation that catches the class even if the parse stays naive. Also worth asserting: total discovered `[Test]` count equals a naive whole-file `[Test]` count, and refuse loudly on mismatch. Needs a red-check: a fixture file with a `codeunit N "X"` comment above a test must go RED before the fix. | done (`48b6dce`) — fix (a) as written, plus a narrowed form of the count assertion. `maskNonCode` blanks comments (`//`, `/* */`) and single-quoted string literals before sectioning, character-for-character so every offset still indexes the source; AL's DOUBLE-quoted identifiers are stepped over intact, since `codeunit 79210 "First Suite"` needs its name. Masking beats anchoring to line start because a string literal carries the same shape mid-line, and it closes the OTHER direction too — a commented-out `Subtype = Test;` no longer promotes a helper codeunit into a test suite (that case was written as a test and fails without the fix). The count assertion had to be narrowed to stay honest: a bare "discovered == naive whole-file `[Test]` count" refuses on a LEGITIMATE shape this repo's own fixture has (`MultipleCodeunits.Codeunit.al`'s `Third Suite` — a codeunit with `[Test]` methods and no `Subtype = Test;`, which BC would not run either). `assertEveryTestAttributed` instead refuses when a `[Test]` lands in NO codeunit section at all — the sibling silent-loss shape, where a header fails to parse and silently takes that file's whole suite with it. Both halves red-checked: defeating the masking reddens exactly the four prose tests and nothing else; removing the guard reddens exactly its own test and nothing else. `fixtures/sandbox-data-tests` had its defensive rewording REVERTED to the natural `codeunit 79308 "Data Value Source".GetValue`, and discovery still returns 22. Live: `itest:tables` PASS at the frozen 69/9/9 over 87 with the one named baseline failure — this fix changes no verdict, which is the expected result, since the fixture was already working around the bug. |
| **R74** | **BLOCKS WIRING `routedTransport`: routed survivors discard the attestation, and the identity gate is vacuous without it.** `runOneBatchMethod` returns `attested` (`batch-transport.ts:180`); `callRouted` (`orchestrator.ts:336-342`) never reads it; the routed `record(...)` passes `guardObserved: undefined` (`orchestrator.ts:628-629`). `report.ts:565` documents `guardObserved === false` as "DECISIVE and damning for a survivor" while ABSENT means "not measured". Worse, `ControlState.Codeunit.al:190-192` sets `ObservedIdentityMismatch` ONLY inside the `ObservedAny := true` branch, so `batch-transport.ts:165`'s fail-closed `identityMismatch !== false` check passes trivially when no guard ever fired — nothing compared to nothing, this project's signature bug. Failing sequence: mutant M is object-attributed to routed test T; T passes on client-services without ever reaching M's site; `attested=false`, `identityMismatch=false` → M recorded **`survived`**, counted in `mutationScore`, and EXCLUDED from the `UNEXERCISED SURVIVORS` callout (`report.ts:1246`) because `guardObserved` is absent rather than false. A false survive. Filed, not fixed — do not implement without a design pass; blocks any production code constructing `routedTransport`. | **MOOT (2026-08-02) — R69 closed, the routed path is DELETED.** This blocker described a false-survive hazard on code that no longer exists: `batch-transport.ts`, `testpage-router.ts`, the routed orchestrator helpers and the R74/R75 tripwire were removed once the recovery was measured at 2.30% (see R69). Kept as a row rather than deleted because the UNDERLYING trap is general and will be met again by anything that scores a verdict from a second execution path: `LC Control State` sets `ObservedIdentityMismatch` only INSIDE its `ObservedAny := true` branch, so a fail-closed `identityMismatch !== false` check passes TRIVIALLY when no instrumented site ever executed. Nothing compared to nothing — this project's signature bug. |
| **R75** | **BLOCKS WIRING `routedTransport`: routed verdicts discard `coverageAttribution`.** `orchestrator.ts:2522` calls `coverageFilter(...)` and keeps only `.covered`, dropping `.attribution`; the routed `record(...)` passes `undefined`. `report.ts:553` documents absent as "nothing ran (`no-coverage`)". An `object`-attributed routed survivor — the exact R29 shape that produced 10 false survivors out of 20 — becomes indistinguishable from an `exact` one. Bounded to `exact` vs `object` (FALLBACK 2 / `all-green` cannot reach this path; the table-trigger gate excludes it). Filed, not fixed — do not implement without a design pass; blocks any production code constructing `routedTransport`. | **MOOT (2026-08-02) — R69 closed, the routed path is DELETED.** Same disposition as R74: the code that discarded `coverageAttribution` is gone. The general lesson stands — a verdict recorded without its attribution cannot be told apart from an exactly-attributed one afterwards, and `report.ts`'s `guardObserved === false` survivor category is only as good as the data reaching it. |
| **R76** | **A real `TestPage` test may make a fenced BASELINE unsurvivable — which would put the shipped R69 diagnosis out of reach on the very corpus that motivated it.** MEASURED 2026-08-01 (R69 Task 7): a `[Test]` opening a REAL page (triggers, FlowFields, a writing `pageextension`) reproduced the Cronus283 `in-flight-unknown` hang **deterministically, twice**, wedging the NST (`StopPending`) and needing a Docker-level restart plus `force-reset-lease`. This RETIRES an over-generalisation of LethAL's own: the 2026-07-31 reading "TestPage does not hang, it is REFUSED, fast (87 ms)" was taken on a CODE-FREE probe page and does not generalise. Both behaviours are real and are DIFFERENT failure modes — trivial page: fast refusal (routable); real page: hang. **Why this outranks the routing question** (Fable, 2026-08-01): `orchestrator.ts` quarantines and breaks on `in-flight-unknown` during BASELINE DISCOVERY (~:2377-2392), long before `resolveRoutedCandidates` (~:2825) — so a hanging baseline test ends the session before any routing decision exists, AND before the R69 report that names the refusal is ever produced. If Continia Document Output's 9 TestPage test files hang rather than fast-refuse, a DO run never completes baseline discovery at all: that is a product-survivability defect independent of routing, and it would mean the other ~95 test files cannot be scored either. **The one experiment that settles it, and it settles three questions at once:** run each of the 9 DO TestPage files individually through a fenced session with R53's `--stop-hung-sessions` ARMED, and classify each into {fast-refuse, hang-recovered-to-scored, hang-wedges-NST}. That yields (a) the fast-refuse split, which decides whether wiring the R69 router could ever pay, (b) whether R53's stop path — proven on MUTANT runs only — generalises to a BASELINE hang, and (c) whether a real suite's baseline is survivable at all. **Threshold pre-committed BEFORE measuring, so the result cannot be rationalised:** wire the router only if at least ~a third of the 9 fast-refuse AND hang containment works; otherwise DELETE the client-services half (control-app batch path, `batch-transport.ts`, `testpage-router.ts`, the routing entry), keep `testpage-unsupported.ts`, and close R69 as "named, not recovered; recovery measured unprofitable". Carrying dead false-verdict-capable code as insurance is the trade this repo's history says loses; the spec plus commit `47fbd19` is a complete re-derivation kit. **Prediction on record:** the fast-refuse slice is expected near-empty — a TestPage test that never exercises a real page barely exists. **STATIC READING TAKEN 2026-08-01, and it confirms the prediction without spending a live run — plus it CORRECTS a sizing figure R69 has been citing.** (1) THE "9 of 104 test files" FIGURE IS NOT REPRODUCIBLE against any Document Output checkout on this machine. The largest available (`/u/Git/DO/Test`) has **17** test codeunits and **146** `[Test]` methods, not 78/1,246; `DocumentOutputRelease` has 16 codeunits. Neither contains a `CDO Email Editor` at all, which the R69 row cites as declaring a TestPage 18 times. So the corpus that produced 9-of-104 is either a branch not checked out here or the count was wrong. Treat 9-of-104 as UNVERIFIED until someone reproduces it; the shape of the finding survives, the number does not. (2) On the corpus that IS available: **3 of 17 test codeunits declare a TestPage** (29 declarations), and those files hold **27 of the suite's 146 `[Test]` methods — 18%**. Proportionally similar to the unverified 8.6%, so the slice is real and non-trivial either way. (3) DECISIVE FOR THE THRESHOLD'S FIRST HALF: **every one of those TestPages opens a REAL page. Not one is trivial.** `CDO Setup Wizard`, `CDO Setup`, `CDO SMTP Setup`, `CDO Azure Application Setup`, `Email Account Wizard`, `CDO E-Mail Templates`, `CDO eSeal Service Setup` — setup and wizard pages with source tables and open-time logic — plus a MODAL handler (`ConfirmEnableModalHandler(var ConfirmManagement: TestPage "Cust. Consent Confirmation")`), and base-app `Issued Reminder` with `.Visible()` assertions on its actions. That is precisely the shape R69 Task 7 measured as HANGING, and the opposite of the code-free probe page that fast-refused. So the fast-refuse slice on real code is predicted EMPTY, and the router — even fully fixed and wired — would rescue nothing here. The remaining open half of the threshold is hang CONTAINMENT. **CONTAINMENT MEASURED 2026-08-01 — IT DOES NOT WORK. Classification: QUARANTINED. Both halves of the pre-committed threshold have now FAILED.** Reinstated the real-page TestPage test in `fixtures/sandbox-data-tests`, published to Cronus283, ran the tables gate with `--stop-hung-sessions` ARMED. The flag DOES act: the run resolved in **34.3 s** instead of hanging indefinitely, and the hung test returned `outcome: "error"` with `socket connection was closed unexpectedly` — BC's session was genuinely killed. But that forced-stop result is still `in-flight-unknown`, so baseline discovery still called `quarantineInFlight` and broke: `quarantined: {"reason":"baseline test in-flight-unknown running PageExtCountsMatchingRelated"}`, **byte-identical to the un-flagged case, 0 mutants scored. The flag changes LATENCY, NOT OUTCOME.** **This is structural, not one observation** (verified in code, so it generalises): `stopHungSessions` occurs exactly THREE times in `orchestrator.ts` — the config field (:397), `buildResumeIndex` (:1847), and the report caveat (:3032). It is NEVER consulted by the baseline-discovery loop, and `requiresUnsafeLatch` (`operation-outcome.ts:29-31`) returns true for `in-flight-unknown` unconditionally, taking no flag input. R53's stop path was built for the MUTANT loop and cannot reach baseline. Cheaper failure than expected, and that IS the flag's one benefit: recovery took `force-reset-lease` + `clear-quarantine` (seconds), with the NST staying `Running` — no Docker restart, unlike Task 7's un-flagged reproduction. Honest limit: n=1 clean armed observation (a first run was lost to a tooling artifact, not BC behaviour), so do not over-read the exact timing or the no-wedge specific; the QUARANTINED mechanism rests on the code, not the count. Gate restored and re-verified at 69/9/6. Evidence: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/r76-containment-report.md`. **CONSEQUENCE, and it outranks R69:** a suite containing ONE hanging TestPage test cannot complete a run at all — not the mutants, not the other tests, and not the R69 report that would have named the refusal. Fixing THAT (making a baseline hang a scored, session-safe skip rather than a tier quarantine) is the real successor here, and it is verdict-neutral and cheaper than the router. **SOLVED 2026-08-01, AND THE FIX WAS ALREADY IN THE PRODUCT — `coverageMode: "procedure"`. MEASURED, not reasoned.** `bcdev-backend.ts`'s `run()` dispatches to `runOnHub()` (`bcdev_test_run`, the SignalR TestRunnerHub, `GuiAllowed=Yes`/`ClientType=Web`) whenever the call's coverage mode is `procedure`/`line`; the gates only ever ran fenced because `DEFAULT_COVERAGE_MODE = "fenced"` and no fixture config overrode it. Re-ran the tables fixture with `PageExtCountsMatchingRelated` LIVE and `coverageMode: "procedure"`: the hub **opened the real `TestPage "Data Main List"` — live triggers, a FlowField, and a pageextension that WRITES from `OnOpenPage` — and returned `pass` in 451 ms** at baseline, where the fenced path hangs indefinitely and quarantines. Exit code 0, **no `quarantined` key anywhere, ZERO recovery needed** (NST stayed `Running`; no `force-reset-lease`, no restart). Counts under that mode: **69 killed / 9 survived / 2 no-coverage / 4 error(unstable)** over 84 recorded — and `no-coverage` fell 6→2 precisely BECAUSE the TestPage test now contributes coverage. So R76's product-survivability defect is closed by a CONFIG VALUE: one hanging TestPage test no longer takes a whole suite down. **What it does NOT fix, and this is the R69/R76 split made concrete:** mutant VERDICTS always run fenced regardless of `coverageMode` — `orchestrator.ts:3316`/`:3445` pass `{ coverage: "none" }`, and `run()` routes anything that is not `procedure`/`line` to `runViaTransport`, the fenced RunMutant OData path. So the 4 pageextension mutants the TestPage test now COVERS still cannot be SCORED: their fenced verdict run refuses. **But it refuses FAST — 140-181 ms, `CreateNavTestService` — it does NOT hang.** That is the decisive new datum: with the hub supplying baseline, the refusal MOVES from an unroutable baseline hang to exactly the fast, completed refusal R69's gate 1 was built to recognise. Caveat recorded honestly: `bun run itest:tables` cannot exercise `coverageMode` (the itest script hand-builds its backend config and never forwards `bcdev.coverageMode`); the direct CLI was used instead, and that forwarding gap is itself a small filed defect. Evidence: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/hub-testpage-report.md`. Fixture and gate restored and re-verified at 69/9/6 twice. Evidence: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/task-7-report.md`, ROADMAP R69, R74, R75. | open |
| **R77** | **LethAL could supply its OWN test runner, which would move mutant activation and attestation INSIDE the test session — the thing that currently makes the hub unable to carry a verdict at all.** Today `LC Run Method` calls `Test Suite Mgt.RunAllTests`, driving Microsoft's DEFAULT runner, so activation must happen OUTSIDE the test session. Three consequences, all verified in code 2026-08-01: `runOnHub` (`bcdev-backend.ts:771`) never reads `pendingMutantId`, so routing mutants to `coverageMode: "procedure"` today would run UNMUTATED code and report **100% silent false survivors** with no error; `WriteActive` is `local` by construction so any hybrid needs a new persistent-activate action whose committed row outlives the session with no clear path; and attestation (`ObservedAny`/`ObservedIdentityMismatch`) lives in a SingleInstance codeunit — session-local memory — so a hub session's `Expected*` is blank and an OData readback lands on a different pooled session. Nothing compared to nothing. **A custom `Subtype = TestRunner` codeunit runs INSIDE the test session**, so it could activate in `OnBeforeTestRun` and read attestation in `OnAfterTestRun`, in the same session as the test, on ANY transport — which would make the fence's same-session co-location a property LethAL owns rather than a property of one OData action. **FEASIBILITY MEASURED 2026-08-01 (`docs/measurements/r77-custom-testrunner-probe.al`), offline alc against the EXISTING symbol set (sole dependency Microsoft `Test Runner`): COMPILES CLEAN.** `Subtype = TestRunner` with `OnBeforeTestRun`/`OnAfterTestRun` is accepted, and `Record "AL Test Suite"."Test Runner Id"` is settable — note the guessed name `"Test Runner Codeunit ID"` does NOT exist and alc rejected it; the real field came out of the Test Runner symbol package, which also exposes `Test Suite Mgt.ChangeTestRunner`/`GetDefaultTestRunner`/`GetCodeIsolationTestRunner`/`GetIsolationDisabledTestRunner`. **NOT established, and each can still kill it:** (1) whether `bcdev_test_run` runs a suite LethAL controls or builds its own — if the hub never uses our suite, our runner never loads and none of this reaches the hub, which is the whole point; (2) whether `OnBeforeTestRun` runs inside per-test isolation and what that does to a write (in-memory SingleInstance state is not transactional and should survive, and the in-memory half is the half activation needs — but "should" is reasoning, not measurement); (3) whether replacing the runner changes isolation semantics, which would MOVE VERDICTS and must be gated on a per-mutant join against all four frozen gates; (4) `TestPermissions` interaction. **Why this outranks wiring R69's routed path:** it attacks the root constraint rather than routing around it, and it is transport-agnostic — it would serve fenced, hub and client-services alike. Entry criterion: answer (1) first, because it is cheap and it gates everything else. Raised by the project owner ("can't we call our own test runner or extend the standard?") after Fable's analysis concluded the bcdev hub was structurally dead for verdicts — that conclusion is correct about the DEFAULT runner and may not survive a custom one. **CORROBORATED AND SHARPENED 2026-08-01 by a second session's research + a wire read of `bc-dev-mcp/src/core/hubs/test-runner-hub.ts`.** The dev-endpoint hub has NO runner knob and this is now established from the DECOMPILED PLATFORM SOURCE, not inferred: `HubBasedTestRunnerService` exposes exactly `Initialize(company, debuggingContext, coverageMode)`, `RunTests(codeunitId, testMethods[])`, `StopTestExecution()` — no isolation, no runner codeunit, no suite name — and `test-runner-hub.ts:76,138` already sends everything the wire accepts. Microsoft's own AL MCP tool exposes the same three fields. **So controlling the bc-dev-mcp SOURCE does not help: the limit is server-side platform code, not the client.** **CORRECTION to this row's own first draft:** it claimed a custom runner would be "transport-agnostic — fenced, hub and client-services alike". That is WRONG for the hub. A custom runner only applies where execution goes through `Test Suite Mgt.RunAllTests`, which honours `AL Test Suite."Test Runner Id"`. The hub bypasses that machinery entirely. Per MS Learn the invocation model is that you RUN the runner codeunit and it does `Codeunit.Run(testCodeunitId)` itself — "runner id" is a Test Framework APP concept, not a platform parameter. **TWO THINGS THIS UNLOCKS ANYWAY, both bigger than they first look.** (1) **LethAL already owns the escape hatch other tools had to build.** The `jimmymcp/test-runner-service` pattern — a companion app exposing a TestRunner via an OData unbound action — is what `LC Run Method` (71007) ALREADY IS, and because it goes through `Test Suite Mgt.RunAllTests` the suite's `Test Runner Id` is honoured. So LethAL can adopt a custom runner on its OWN paths (fenced AND the client-services batch loop) with no new app and no new dependency. (2) **`TestIsolation = Function` COMPILES CLEAN** (measured with the same probe): `design.md` §6.2 records Function-level isolation as a stated future hardening goal — "a test codeunit whose methods share intra-codeunit DB state could, under Codeunit isolation, let one method's writes reach the next" — and a custom runner is how it is reached. That is a correctness improvement available independently of any hub question. **THE ONE PROBE THAT COULD STILL REVIVE HUB-CARRIES-VERDICTS, and it is cheap:** what does the NST do when `RunTests(codeunitId, [])` names a codeunit whose `Subtype = TestRunner` rather than `Subtype = Test`? If the platform simply RUNS it, LethAL passes its OWN runner id, that runner activates the mutant in `OnBeforeTestRun` **inside the hub session** and reads attestation in `OnAfterTestRun` — which is precisely the same-session co-location the fence exists to provide, obtained on a GUI-allowed session. The other session framed this as "users get custom isolation and hooks for free"; for LethAL it is the whole question. ~15 minutes on a container, and a rejection is an equally good answer. **Not viable, recorded so it is not re-tried:** bccontainerhelper CAN select a runner (`Run-TestsInBcContainer -testRunnerCodeunitId`), but by driving page **130455 "Command Line Test Tool"** over the UI client stack (`SaveValue` on a `TestRunnerCodeunitId` control, then `InvokeAction RunNextTest`) — the client-services protocol, needing `Microsoft.Dynamics.Framework.UI.Client.dll`. R69 already attempted page 130455 over bc-mcp and it failed at SEEDING (repeater edit never commits; `Codeunit ID` is a key field). Note bccontainerhelper writes a DIFFERENT control than the ones R69 failed on, so this is discouraging rather than definitively closed. **PROBE RUN 2026-08-01 — ANSWERED: REJECTED. The hub is now CLOSED for verdicts on direct evidence, not inference.** Plan `docs/superpowers/plans/2026-08-01-r77-custom-testrunner-probe.md` (rejected once by adversarial review, revised twice, then cleared) executed on Cronus281. The platform TYPE-CHECKS the named codeunit and refuses: server event id 701, both attempts, timestamps matching — `TestRunnerExecutionException: "The codeunit with ID 79220 is not a test codeunit."`, thrown by `DevTestRunnerCodeunit.RunTestCodeunit()` **before `OnRun`/`OnBeforeTestRun` ever execute**. The discrimination the plan was built for worked: this is DISTINCT IN KIND from Control B's clean lookup miss (79299 does not exist; 79220 exists and fails a type check). The paranoid instrumentation earned itself by staying silent: the `R77-RUNNER` token did NOT appear and no positive was filed on anything weaker; marker 77770 was absent at T0 and after both attempts (the `Commit()` line was never reached); and 79220 did not enter `bcdev_test_discover` (structural — a TestRunner declares no `[Test]`), which also answers the safety question the plan raised about a deployed TestRunner on a shared container. Restore confirmed and `itest:bcdev` re-verified at **3/10/3**. **So all three lines now agree and the hub question is finished:** the wire has no runner knob (decompiled `HubBasedTestRunnerService`), controlling the bc-dev-mcp client cannot add one (server-side limit), and the platform actively refuses a TestRunner codeunit named in `RunTests`. Do not re-open this without new platform evidence. **WHAT SURVIVES R77, and is now the whole of its value:** a custom runner remains usable on LethAL's OWN paths, because `LC Run Method` goes through `Test Suite Mgt.RunAllTests`, which honours `AL Test Suite."Test Runner Id"` — LethAL already owns the `jimmymcp/test-runner-service` escape hatch other tools had to build, so no new app is needed. The concrete prize there is **`TestIsolation = Function`** (compiles clean), which `design.md` §6.2 records as a stated future hardening goal: under today's Codeunit isolation, one method's writes can reach the next within a test codeunit. That is a correctness item independent of any hub question, and it must be gated on a per-mutant join across all four frozen gates because changing isolation can MOVE VERDICTS. **MEASUREMENT-INFRASTRUCTURE DEFECT FOUND EN ROUTE (bc-dev-mcp, not LethAL, and actionable because we own it):** the rejection text exists ONLY in the container's Windows Event Log. On the wire the MCP client receives `"Hub connection closed: unknown reason"` — **indistinguishable from a genuine wedge**. Any future hub failure is therefore undiagnosable from the client alone, which cost this probe real effort and will recur. Also: `bcdev_debug_sql` cannot read or delete an arbitrary table row by key (it reports SQL cost at a debugger break), so the plan's stale-marker guard had to be served by a throwaway `Subtype = Test` readback codeunit over the already-proven `bcdev_test_run` path — disclosed rather than silently substituted. Evidence: `.superpowers/sdd/2026-08-01-r69-phase2-batch-runner/r77-probe-report.md`. **ARCHITECTURE READ FROM MICROSOFT'S OWN SOURCE 2026-08-02 — the probe result is now EXPLAINED, not merely observed, and it settles the remaining candidates without further probing.** The BC test framework source is on this machine at `U:/Git/DO.Support-SlowDOSetup/BC/testframework/TestRunner/Test Runner/src` (a reference the project did not know it had; worth remembering). It shows **TWO ENTIRELY SEPARATE EXECUTION STACKS**: **(1) The Test Framework APP stack** — `AL Test Suite` + `Test Method Line` -> a `Subtype = TestRunner` codeunit (`TestRunnerIsolCodeunit` 130450 / `TestRunnerIsolDisabled` 130451, selectable via `AL Test Suite."Test Runner Id"`, with `SelectTestRunner.Page.al` as Microsoft's own UI for it) -> `Test Runner - Mgt` (130454), which PUBLISHES `OnBeforeTestMethodRun` and `OnAfterTestMethodRun` as `[IntegrationEvent(false, false)]` on a codeunit carrying no `Access = Internal` — i.e. **subscribable from another app** -> runs the test. `CommandLineTestTool.Page.al` (130455) is here too, which is why bccontainerhelper's `-testRunnerCodeunitId` works. **LethAL's `LC Run Method` already uses this stack** (`Test Suite Mgt.RunAllTests`). **(2) The dev-endpoint/HUB stack** — platform `DevTestRunnerCodeunit.RunTestCodeunit()`, which is NOT in the Test Framework app source at all. It type-checks for `Subtype = Test` and dispatches directly: no `AL Test Suite`, no `Test Runner - Mgt`, and therefore NO events. Its coverage comes from the platform's own `Initialize(..., coverageMode)` instrumentation, NOT from `AL Code Coverage Subscribers` (130471), whose subscribers all hang off 130454 and are driven by the suite's `CC Tracking Type` — a suite the hub never creates. **Consequences, both directions.** The hub is closed for verdicts AND for event hooks, and that is now an ARCHITECTURAL fact rather than a probe outcome: candidate 'subscribe to `OnBeforeTestMethodRun`' fires on stack 1 only, so it dies on the hub for the same reason the custom runner did. **But on LethAL's OWN path BOTH mechanisms are available today** — a custom runner via `Test Runner Id`, AND integration-event subscribers giving per-method in-session hooks. That is a strictly better place to put activation and attestation than the bespoke wiring in `RunBatch`, and it may simplify R74's fix rather than merely enabling it, since a subscriber fires inside the same session as the test on ANY stack-1 transport (fenced OData and the client-services batch loop alike). Route future work here, not at the hub. **VERIFIED AGAINST THE FULL BCApps SOURCE 2026-08-02** (`U:/Git/DO.Support-SlowDOSetup/BC`, **16,897 AL files, version `w1-28.0.46665.48549` — the SAME build as our containers**, so this describes the runtime under test, not a nearby one). Two checks close the remaining doubt: **`DevTestRunner` appears in ZERO of those 16,897 files**, confirming the hub's dispatcher is platform C#/NST with no AL surface to hook — the hub verdict is architectural and final; and **Microsoft ships exactly five `Subtype = TestRunner` codeunits**, of which two are legacy C/AL (`CALCommandLineTestRunner`, `CALTestRunner` — the 130409 path bccontainerhelper refuses), one is discovery-only (`TestRunnerGetMethods`), leaving **`Test Runner - Isol. Codeunit` (130450, `TestIsolation = Codeunit`)** and **`Test Runner - Isol. Disabled` (130451, `Disabled`)**. **MICROSOFT SHIPS NO Function-isolation runner** — grepping the whole tree for `TestIsolation = Function` returns nothing — yet the property compiles (measured). So `design.md` §6.2's stated hardening goal is reachable ONLY by writing our own runner, which makes this R77's concrete surviving deliverable rather than an aspiration. **And the implementation shape is now known, because 130450 is thin and can be mirrored almost verbatim** (~40 lines): `TableNo = "Test Method Line"`; `OnRun` does `ALTestSuite.Get(Rec."Test Suite")`, `CurrentTestMethodLine.Copy(Rec)`, `TestRunnerMgt.RunTests(Rec)`; `OnBeforeTestRun`/`OnAfterTestRun` delegate to `TestRunnerMgt.PlatformBeforeTestRun`/`PlatformAfterTestRun`. A LethAL runner copies that delegation EXACTLY — preserving every platform bookkeeping call — and adds only `TestIsolation = Function` plus activation before, and attestation read after, the delegated calls. Low risk precisely because the platform half is unchanged; the gate remains a per-mutant join across all four frozen gates, since isolation changes can MOVE VERDICTS. | **hub half CLOSED (rejected on platform evidence 2026-08-01); custom-runner-on-own-path + `TestIsolation = Function` remain open** |
| **R68** | **A variable declared in a TRIGGER's own `var` section is never resolved, so Tier 2 refuses every site whose receiver is declared there.** `lookupVar` (`packages/builtin-tier2/src/receiver.ts`) resolves procedure locals, procedure parameters and object globals; `buildSymbolTable` indexes `procedure` members only, so a trigger's `var` section is invisible in EVERY object kind — table triggers, page triggers, `OnRun`, field `OnValidate`. Safe direction (rule 4 refuses; Tier-1 `void-method-call` still covers the site) and already stated in `lookupVar`'s doc comment as deliberate conservatism — but it is a real coverage loss, not a design choice, and trigger bodies are where a great deal of BC logic lives. Measured incidentally 2026-07-31: R30's fixture pageextension first declared its records inside `OnOpenPage` and generated four specs, **not one of them `remove-setrange`**; moving the same declarations to the object's `var` section made the site claimable. Sizing it on a real project has NOT been done — `scripts/probe-r30-pageext.ts` counts receivers declared in procedures and globals, and would need a trigger-scope arm added. | open — **BLOCKED ON HARDENING R70's GATE DETECTOR FIRST, and that dependency is not obvious.** Fixing R68 (resolving a trigger's own `var` section) would SILENTLY DISARM the R70 regression detector added in `086875d`. That detector works precisely because `Data Scope Probe.OnInsert`'s `Helper` is declared in the TRIGGER's var section and is therefore unresolvable today: Tier 2 refuses the site, Tier 1 claims it, and a regression of R70 flips the operator to `lethal.remove-setrange`. Once trigger vars resolve, the trigger-local `Helper` wins scope resolution outright — before the (kind, name) key is ever consulted — so the site claims identically whether or not R70 is regressed, and the gate stops detecting the class it was built for while still passing. **The fix is small and must land first:** change `page 79324 "Data Scope Probe"`'s `Helper` from `Record "Data Main"` to a NON-record type (e.g. `Integer`). Then under a regressed R70 the page's declaration makes the receiver resolve to a non-record and Tier 2 REFUSES — the mutant flips `remove-setrange` -> `void-method-call`, an observable that does not depend on R68 either way. Re-freeze `tables.baseline.json` with the usual old/new diff after that change. Found by checking the interaction before implementing, not by a gate — no gate would have reported it, which is the point. | done (this commit) — `triggerScopeVar` (`packages/builtin-tier2/src/receiver.ts`) resolves the nearest enclosing TRIGGER's own `var` section, checked ahead of object globals so a trigger-local shadows a global of the same name, exactly as a procedure local does. Resolved from the AST NODE rather than a name-keyed map, and that is forced rather than stylistic: trigger names repeat across an object (every field may declare its own `OnValidate`), so a name key would be ambiguous — the node is the unambiguous identity. `collectVarDeclarations` is now exported from `@lethal/engine` and SHARED with `buildSymbolTable` instead of reimplemented, so a grammar change moves both (R80 is the version of that mistake already in the tree). Rule 4 still refuses a receiver declared nowhere, pinned by its own test — without it "resolve trigger vars" could be satisfied by resolving anything. Measured on the fixture: raw specs 117 -> 118, DEPLOYED unchanged at 106, and `Data Scope Probe.OnInsert` flips `void-method-call` -> `remove-setrange` — §3.2 precedence deleting the Tier-1 mutant, which is the dedup mechanism working rather than a lost site. `itest:tables` holds 84/12/10 over 106; the re-freeze diff is ONE key gone and ONE key new (the operator flip) with **zero same-key verdict changes**. Sizing on a real project was NOT done and is not needed to justify this — the coverage loss was structural. |
| **R67** | **A plain `page`'s implicit `Rec` is refused, though its `SourceTable` is declared in the page itself.** `resolveReceiver` (`packages/builtin-tier2/src/receiver.ts`) resolves an implicit `Rec`/`xRec` for a `table` (its own name) and for a `tableextension` (the header's `base_object`), and returns `unresolved` for every other kind — including `page`, whose record is named by its own `SourceTable = "X";` property, in the same file, with nothing to guess. Measured 2026-07-31 with `scripts/probe-r30-pageext.ts` on Continia Document Output Cloud: **66 Tier-2-shaped calls on a page's implicit `Rec`** (against 210 on record vars declared in the same page, which DO already claim). Safe direction — a missed site costs one operator's signal and Tier-1 `void-method-call` still covers it. Two things to get right if it is built: `SourceTable` is a page PROPERTY, not a grammar field the symbol table indexes today, and rule 3's `projectDeclaresProcedureOnTable` must then be applied to the resolved table exactly as the `tableextension` path does, or the shadowing guard is bypassed for a whole object kind. Found while measuring R30's remaining `pageextension` hole; NOT the same item (R30 is extension objects). | done (this commit) — `sourceTableOf` reads the page's own `SourceTable = "X";` PROPERTY (the grammar exposes `name`/`value` fields on a `property` node — measured against the vendored tree-sitter-al v3.0.1 wasm, not scraped from text) and feeds it to BOTH implicit-receiver branches, so rule 3's `projectDeclaresProcedureOnTable` reaches the resolved table exactly as it does for a `tableextension`'s base object. That guard has its own test: a page sourced on a table declaring its own `SetRange` is REFUSED, because omitting it would bypass the shadowing guard for a whole object kind — the specific hazard this row named. A `pageextension` stays refused and the distinction is the point: reading a property that is PRESENT is resolution, guessing one that lives in an object this project cannot see is the R29 shape. **No fixture site, and deliberately so** — unlike R71 and R73, where I added one because the site was killable. A plain page's trigger only runs when the page opens, which needs a `TestPage`, which the fenced path refuses (R69), so a fixture site here would land `no-coverage` and prove INSTRUMENTATION rather than scoring — and `DataMainListExt.PageExt`'s `remove-setrange` mutant (M0043, no-coverage) already demonstrates a Tier-2 claim compiling and deploying inside a page-family object. The claim change is fully verifiable offline and is verified there. **The 66-site gain is NOT re-measured** post-fix: `scripts/probe-r30-pageext.ts` counts candidate call shapes, not claims, so confirming the recovery would need its own arm. Filed as unmeasured rather than asserted. |
| **R82** | **`SwapCallArguments` — swap two same-typed arguments at a call site.** What is left of `EventPublisherSignature` once R13 measured that the operator as sketched cannot be emitted: a publisher's signature is declarative and the only emittable form (a duplicate publisher) is equivalent by construction because subscribers bind to the original, while the OBSERVABLE effect — a callee receiving `(B, A)` instead of `(A, B)` — sits at the raise site, in ordinary executable position the existing guard already reaches. It is filed as its own item because it is **not event-specific**: restricting an argument swap to event raises is arbitrary, and the general operator applies to every call in the language. Measured footprint so far is the event-scoped slice only, and both figures are GENEROUS upper bounds (publisher map keyed on bare name, subscriber match ignores the publishing object): 140 event publishers on `do-rel2/Cloud`, 40 with two parameters of one type, **44** raise sites reaching such a publisher, **21** whose event name is subscribed anywhere in the checkout. The general footprint — any call with two same-typed arguments — is UNMEASURED and needs `buildTypeTable`, not a name census. Two things to settle before writing code: the kill signal (a swap is only observable if the callee distinguishes the two arguments, which for two Booleans it often does not — this is `swap-modify-flag`'s equivalence problem at a larger site count), and whether a same-typed swap can be proven type-safe from source alone. Evidence: `docs/superpowers/specs/2026-08-02-r13-tier3-decision.md` §1, §3. **COUNTING RULE PRE-COMMITTED 2026-08-03, BEFORE any general count was taken** (R13's discipline at `349901a`, which is what makes a refusal a measurement rather than a rationalisation): (1) **Unit** — one mutant per CALL SITE (`call_expression` carrying an `argument_list`), as every shipped operator emits; a call with three same-typed arguments still counts **1**. (2) **A site qualifies iff** it has >=2 arguments AND >=2 of them resolve to the SAME non-null type through the real `buildTypeTable` over the whole project (not a name census) AND those two arguments differ in whitespace-normalised source text (a swap of textually identical arguments is a no-op, equivalent by construction, and must never be counted) AND `isMutableSite` holds, since the guard chain wraps the enclosing STATEMENT and a call in a page property has none. (3) **Denominator** 19,132 — R13's shipped-mutant count on `do-rel2/Cloud` — so the result lands on the same axis as bar (a)'s **>=13** and bar (b)'s 957. (4) **Bar (a) applies, at >=13 MARGINAL mutants**, and marginal here is expected to equal gross: dedup identity is `kind:start:end:after.text` (`packages/schemata/src/dedup.ts:23`) and `emitDispatch` chains multiple mutants per component, so a swap's replacement text is never byte-identical to `void-method-call`'s empty deletion at the same call — unlike `IsolationLevelSwap`, whose 25-of-36 overlap was a genuine identity collision. The overlap is reported anyway rather than assumed away. (5) **Reported, not hidden:** the share of >=2-argument sites where fewer than two arguments type at all, because that bounds the count from BELOW and states how much of the corpus the type table cannot see; and the Boolean/Boolean share, which is the slice carrying `swap-modify-flag`'s equivalence problem. (6) **Type-safety split** — same declared type on both arguments makes the swap type-safe *by the arguments*, but a callee `var` parameter is decided by the CALLEE, so the census reports separately the slice where both arguments are plain variable references (safe whatever the callee's var-ness) and the slice where at least one is a literal or expression (a `var` parameter there is a compile error the operator would have to avoid). Excluded from every count: `.dependencies/` files and files whose basename starts with `Mutation`. **MEASURED 2026-08-03 — the footprint question is SETTLED, R82 is NOT refusable on cost, and killability is now the only gate left.** `scripts/census-swap-call-arguments.ts` over `do-rel2/Cloud` (417 files): 9,798 calls carry an argument, 4,573 carry two or more, **893** qualify under the pre-committed rule = **4.67%** of 19,132, and **zero** are dropped as non-executable — unlike `NegateConditional`, whose 154 page-property sites R40 had to refuse, every argument-swap site is already executable. Full numbers and bounds: `docs/measurements/README.md` §R82. **Three findings the count alone would have hidden.** (1) **893 is the LOOSE number and 135 of it would not compile.** `buildTypeTable`'s `extractType` keeps the first whitespace-delimited token, so `Record "Sales Header"` and `Record "Purchase Header"` both answer `Record`; re-checking each pair against its FULL declared type leaves **340** genuinely same-typed, 135 same-head-different-type (118 `Record`, 9 `Codeunit`, 4 `List`, 2 `Option`, 2 `Enum`), 418 with a literal or expression member. Filed as **R84** — nothing shipped reads the type table today, so it is this operator's precondition rather than a live defect. (2) **Type safety IS provable from source alone, for those 340, with no callee resolution** — the proof is about the arguments, not the callee: if the call compiles, A fits parameter *i* and B fits *j*, so equal declared types make the swap fit both ways. The single hazard the arguments cannot settle is a callee `var` parameter (AL matches those by exact type and refuses a non-lvalue), and it vanishes when both members are bare variables, which is exactly the 340. That is **26x bar (a)**, so the cost bar cannot refuse this operator. (3) **Boolean/Boolean is a minority, measured: 40 of 340 (11.76%).** The fear that an argument swap is mostly `swap-modify-flag`'s equivalence problem at a larger site count is false on this project. **What is still NOT measured, and it is the whole remaining question: whether a swapped call ever changes a verdict.** No current fixture can answer it — `fixtures/sandbox-data` has 3 loose sites and **0** provable ones, `fixtures/sandbox-app` none — so a live kill measurement needs a new fixture site, the way R30, R70 and R78 each grew the table fixture. Also unmeasured: the true footprint is LARGER by an unknown amount, since 2,689 of the 4,573 two-argument sites (58.80%) have fewer than two arguments `buildTypeTable` can type at all. **SHIPPED AND MEASURED LIVE 2026-08-03 (`f9e055c`): 30 of 30 pre-committed per-mutant predictions matched, exactly.** The operator is `lethal.swap-call-arguments`, **tier 1** (design.md §4's generic set — an argument swap knows nothing about AL), and it is `ctx.types`'s FIRST consumer in the shipped pipeline, which is why R84 was its precondition rather than a nicety. Predicate: two bare identifiers of one FULL declared type, differing text. Bare identifiers are the `var`-parameter guard — AL matches those by exact type and refuses a non-lvalue, and the callee is never resolved — and arm A proves against a real compiler that the argument holds. `itest:tables` moved 84/12/10 over 106 to **109/17/10 over 136** (148 raw), `untargetedTriggerCount` 0, twice. Six arms, all as predicted: **A** kill via redirected `var` writeback; **B** kill in EXPRESSION position (the shape 452 of 893 real sites have); **C** an EQUIVALENT survivor (`or` is commutative — no assertion can ever kill it); **D** an UNDERTESTED survivor, deliberately readable apart from C; **E** a FALSE KILL, killed by a platform length overflow under a test that asserts NOTHING; **F** the R84 refusal, no swap mutant emitted. Four sites carry BOTH a swap and a `void-method-call` mutant and both scored, so **"marginal == gross" is now measured** rather than argued. Evidence: `docs/measurements/README.md` §"R82 live". **What is NOT established, and it is the whole remaining question: the RATE at which a real suite notices a real swap — filed as R85 with its own pre-committed bar.** Arm E also found a gap it was not aimed at: **none of the 109 kills records WHY it died** (`failure_note` is NULL for every one), so a platform-artifact kill is indistinguishable from an assertion kill in the stored record — filed as **R86**, and it is R85's precondition. | **operator SHIPPED; mechanism killability proven live in both directions; RATE on a real project unmeasured (R85)** |
| **R83** | **Is `InherentPermissions` a better `PermissionReduce` target than `Permissions`? Unknown, and worth one measurement before anyone assumes either way.** R13 measured that `Permissions` is purely ADDITIVE — it grants indirect rights to code executing in the object (arm A5: BC’s demand moves from `Insert` to `IndirectInsert`), so reducing it is observable only in a session that already lacks the right, which is why `PermissionReduce`’s kill needs the rare permission-lowering test. `InherentPermissions` is a DIFFERENT property and the R13 decision explicitly does not cover it. **What is measured:** Continia Document Output carries **2** occurrences across 554 files, both on CODEUNITS and both `InherentPermissions = X` (zero on tables — consistent with R1’s note), and this repo’s own `fixtures/sandbox-probes` table 79200 carries `InherentPermissions = RIMD`. **What is NOT measured, and must not be assumed:** whether it can REFUSE an operation a SUPER session would otherwise be allowed — the claim that it constrains rather than grants comes from reading the AL compiler’s option list, not from any run, and an unmeasured platform claim written into this file is the exact failure R33 §2 records. Order of work: probe table 79200 through the fenced path with the property reduced and intact, THEN decide. Even a positive result leaves 2 sites on the one project censused, far below the >=13 bar — so this is a question to close cheaply, not a build. | open — footprint measured (2 sites, both codeunits, both `= X`), semantics unmeasured |
| **R84** | **`buildTypeTable` truncates every subtype, so "same type" is wrong for exactly the types where it matters.** `extractType` (`packages/engine/src/semantic/types.ts`) keeps only the first whitespace-delimited token of a declaration, so `Record "Sales Header"` and `Record "Purchase Header"` both answer `Record` — and so do two different `Codeunit`s, two `List of [...]`s, two `Option`s, two `Enum`s. **Measured while counting R82** (`scripts/census-swap-call-arguments.ts`, `do-rel2/Cloud`): of 893 call sites whose arguments the table calls same-typed, **135 (15.1%) are not** — 118 `Record`, 9 `Codeunit`, 4 `List`, 2 `Option`, 2 `Enum`. An operator that trusted the answer would emit an artifact that does not compile at those sites, and the failure would arrive as an `AlcCompileError` on a full project instead of as a wrong mutant, i.e. after the expensive part. **Not a live defect today, and that is the only reason this is a row rather than a fix:** `ctx.types` has NO consumer in the shipped pipeline — no operator, no analysis reads it, only its own unit tests do. So it is a precondition of the first operator that does, which is R82. The fix is the declaration text the symbol table already holds (`VarSymbol.typeText` is the FULL text; the census resolves it by walking out from the identifier, which also avoids `resolveIdentifierType`'s object-iteration scoping), plus a decision on what "same type" means for `Label` (two labels with different constant text ARE the same type — the census collapses them deliberately) and for `Code[20]` vs `Code[10]` (the census keeps them distinct, which is conservative but rejects safe swaps). Evidence: `docs/measurements/README.md` §R82. | done (`f9e055c`) — `extractType` keeps the whole declaration, whitespace-collapsed. No special case was needed for `Label`: the grammar's `type` field already answers the bare word `Label` with the constant in a sibling `string_literal`, so two labels compare EQUAL, which is correct. `Code[20]` stays distinct from `Code[10]`, deliberately — R82 arm E measured that the conservative direction is right, since the compiler ACCEPTS the wider value and BC raises at runtime instead. Red-checked: reverting to the first-token form turns exactly three new `types.test.ts` cases red (Record subtype, Codeunit subtype, `List of [Text]`) and leaves the Label case green, which is what proves the collapse is about subtypes rather than text equality — plus, one layer up, exactly the R82 arm-F refusal test. |
| **R85** | **The RATE at which a real suite notices a real argument swap — unmeasured, and it is the only R82 question left.** R82 proved the MECHANISM live (30/30 predicted verdicts, `f9e055c`), on a fixture written for it. A fixture cannot produce a rate: six arms chosen to be readable say nothing about how often Continia Document Output's 1,290 tests would catch a swap at one of its real sites. **The population is 390, not 340, and sampling the wrong one would have described something LethAL does not emit.** 340 is the CENSUS's number, and the census pairs over all arguments and only then asks whether the chosen pair happened to be two identifiers; the shipped operator filters to identifiers FIRST and then pairs, so it finds pairs the census skipped. Running the actual `swapCallArguments.targets` over `do-rel2/Cloud` claims **390** sites. Sample from 390. **Two instruments, and the cheap one does not need the operator:** (a) hand-swap a SEEDED random sample of the 390, run DO's own suite per swap, count what it notices — zero self-construction and it exercises none of LethAL, which is the point; (b) a post-ship LethAL-on-DO run, which also measures the pipeline at scale but costs a real run. **Pre-commit the bar BEFORE any rate is seen** (R13's discipline at `349901a`) and pre-commit the sampling rule with its seed, or the sample becomes the argument. **Both instruments are exposed to arm-E-shaped platform kills, and an earlier version of this row wrongly granted (a) immunity.** Of the 390 claimed sites, 233 have a project-local callee that resolves; **12 of those (5.2%) declare DIFFERENT parameter types at the two swapped positions, 6 of them same-base-different-length** — the arm E shape exactly. A real one: `Al\Codeunit\Codeunit 6175293 CDO Payment Link Handler.al:187` swaps a 17-character label into a `Text[50]` and a 72-character label into it. At 5.2%, a 30-swap sample should expect roughly one, so **the sampling rule must require recording the FAILURE TEXT of every noticed swap, not just the count** — that is what makes (a)'s immunity a discipline someone actually performs rather than a property of the instrument. (b) additionally needs R86, since a LethAL run cannot yet split kills by cause. | open — mechanism proven, rate unmeasured; population is 390; bar, seed and text-recording rule must be pre-committed first |
| **R87** | **`resolveIdentifierType` answers from the WRONG OBJECT when two objects declare a same-named procedure, and R82 made that reachable.** `packages/engine/src/semantic/types.ts`'s `findEnclosingProcedure(node, obj.node)` returns the enclosing `procedure`'s name before the walk can ever reach `obj.node`, so the `current !== objectNode` guard gates nothing: `resolveIdentifierType` iterates `symbols.objects` in PARSE ORDER, answers from the first object declaring a procedure of that name, and — worse — `return null`s without trying the remaining objects when that object does not declare the identifier. R84 fixed `extractType`, the other half of the same row, and left this; the R84 row itself names it ("the census resolves it by walking out from the identifier, which ALSO avoids `resolveIdentifierType`'s object-iteration scoping"). **Measured, not argued.** A two-codeunit counterexample: `Zzz Scope Victim.RunLink` declares `Record "Data Main"` + `Record "Data Related"` and calls `Link(MainRow, RelatedRow)`; `Aaa Scope Decoy` declares its own `RunLink` with BOTH locals as `Record "Data Main"`. Victim alone: **0 specs, correctly refused.** Decoy parsed first: **`swap-call-arguments` CLAIMS the site** and emits `Link(RelatedRow, MainRow)`, which `alc` 18.0 rejects — `error AL0133: Argument 1: cannot convert from 'Record "Data Related"' to 'Record "Data Main"'` (and the mirror for argument 2). That is the exact failure R84 exists to prevent, arriving as an `AlcCompileError` on a whole project, after the expensive part. **Calibration on `do-rel2/Cloud`** (244 objects): 1,793 distinct procedure names, **184 (10.3%) declared by more than one object**; of 390 claimed sites, **27 (6.9%) are typed by an object other than the one declaring the enclosing procedure** — 0 of them wrongly TODAY — and **73 of 463 candidate sites (15.8%) are LOST** to the same defect, which is the safe direction. So: live mechanism, common precondition, zero wrong instances on the one project censused, and the direction is asymmetric only by luck of naming. **Fix:** key the scope from the identifier's OWN enclosing object (`objectScopeKeyOfNode` already exists for this), or at minimum `continue` instead of `return null` so a wrong first match cannot both shadow and terminate. Not R82's to fix — R82 is what made it reachable, being `ctx.types`'s first consumer. Found by an adversarial review of the landed work; counterexample and `alc` rejection re-measured independently. | open — live, 0 wrong / 73 lost on the one project measured |
| **R88** | **A released `lethal` binary carries no evidence of which commit built it, and the filename cannot tell a stale one from a fresh one.** `build/` is gitignored (`.gitignore:4`), so the artifact is untracked and git records nothing about its provenance; the name carries only the package version (`lethal-0.1.0-alpha.1-windows-x64.exe`), which does not move when `packages/` does. **Measured 2026-08-04** while preparing the DO campaign: the local binary was built 2026-07-27 20:49 and is **56 package-commits stale** — `grep -c` against it returns **0** for both `swap-call-arguments` and `remove-commit`, two operators that ship today, while `negate-conditional` returns 3. A run driven by that binary silently measures a smaller operator set than the same source would, and nothing in the report, the filename, or `--version` says so. It surfaced because the campaign's rung 3 hands an agent the binary while rungs 0–2 run from source — the two would have disagreed by 13 of ~176 mutants on one codeunit. **Mitigated for the campaign, not fixed for users:** plan Task 8 step 1b rebuilds from the pinned commit, greps each depended-on operator (zero ⇒ abort) and records commit + sha256 + build time in the run manifest. The product fix is for `--version` to report the build commit and the registered operator set, so a downloader can tell what they are holding. Evidence: `docs/superpowers/plans/2026-08-03-do-live-campaign.md` Task 8 step 1b; `scripts/build-binary.ts`. | open |
| **R89** | **`--stop-hung-sessions` is only PARTIAL on a hosted environment, and a real DO codeunit still cannot complete a clean run.** R53 built the flag for the non-terminating-mutant class and its own caveat said the stop was unmeasured on the hosted topology. **Measured 2026-08-04** on Continia environment `f5f11bf2` against DO codeunit 6175297 (148 deployed mutants, pinned source `5f2a71d3`): with the flag ON, one mutant scored `timeout-killed` — the stop worked — while another stranded anyway and quarantined the tier. Three fresh attempts, three strands (run 1 at M0092, run 2 twice). `docs/measurements` says of 2026-07-28 that *"a clean, COMPLETE fenced DO run does not exist"*; that is STILL true, with the flag available. What has changed is that `--resume` can carry a run to completion — run 1 resumed correctly, carried 63 verdicts and skipped 2 stranded identities unscored (the safe direction), reaching 148. **ROOT CAUSE FOUND 2026-08-04: the strands were SLOW mutants, not hung ones.** Both stranding mutants are `void-method-call` deleting a `SetCurrentKey` — which does not hang, it makes the following filtered query pick a worse plan and scan. They exceeded R47's hardcoded 30 s floor, and on the fenced path a budget overrun is indistinguishable from a genuine strand, so the tier quarantined instead of scoring them. `--mutant-timeout-ms 180000` eliminated BOTH strands entirely (errors 2 -> 0, single-pass completion, twice) while M0013 — `negate-conditional` on `until ... Next() = 0`, the genuinely non-terminating one — still scored `timeout-killed` at 30172 ms. That contrast within one run is the control proving the distinction. So `--stop-hung-sessions` is NOT deficient for this class; the 30 s FLOOR is, and R47 already records that the floor has no config key reaching it. **Two clean verdict-identical runs now exist (148 mutants, 25/107/15/1/0), so `docs/measurements`'s "a clean, COMPLETE fenced DO run does not exist" is no longer true.** **Second defect, still unexplained and now sharper:** run 2's `--resume last` printed NO `RESUMED:` banner and produced 86 fresh verdicts instead of continuing the 113 already recorded, where run 1's engaged and said so. **The store DISPROVES the first hypothesis** — queried directly, run 3 was `finished_at` NULL with 113 verdicts recorded, i.e. a valid unfinished target, when `--resume` ran; it created run 4 fresh anyway and re-measured from scratch. A resume that cannot use its target must THROW, not silently start over (this project's own fail-loudly rule). Cause undiagnosed; reproduction precise. **Measured cost this imposes:** ~10 min recovery per strand (env stop/start ~3.5 min + `force-reset-lease` + `clear-quarantine` + the resumed run), which no plan budgets. Evidence: `docs/campaign/2026-08-03-do/rung1.result.md`, `rung1.baseline.json`. | open |
| **R90** | **A single file whose instrumented form exceeds the publish budget cannot be measured at all, and `--max-guards-per-batch` cannot rescue it.** R44 shipped the flag and correctly reports an oversized batch rather than dropping it, but **splitting is at FILE granularity** — so a file that alone exceeds the budget becomes its own oversized batch and the flag is powerless. **Measured 2026-08-05** on Continia environment `f5f11bf2`, DO pinned at `5f2a71d3`, one file per publish: **176 guards publish (36–97 s)** · **229 publish** · **331 TIMES OUT** · **660 TIMES OUT**. The failure is `continia publish` returning `{"success": false, "message": "The operation timed out."}`, after which the deployment verifier correctly reports an artifact identity mismatch. So the hosted ceiling sits between **229 and 331 guards in one file**, and `Codeunit 6175296 CDO E-Mail Template Management` (660 sites) is **unmeasurable on a hosted environment** — not slow, not partial: it cannot be published. This bounds what LethAL can say about a real app far more sharply than R40's object-kind gap: DO has 85 codeunits, and the largest are exactly the ones worth mutating. **What is missing is not a fix but a REFUSAL**: nothing warns before the run that a file is over the ceiling, so the user pays generate + instrument + compile + publish before learning it was impossible. A pre-flight check against a configurable per-file guard ceiling would cost nothing and fail in milliseconds. Evidence: `docs/campaign/2026-08-03-do/rung2.precommit.md` §SUPERSEDED, `rung2.result.md`. | **done (`b95426b`)** — the refusal exists and is MEASURED, not configured: `packages/runner/src/publish-ceiling.ts` learns a per-tier bracket from `publish_outcomes` (store.ts) and `runSession` calls `assertUnderCeiling` per FILE immediately after the manifest is written — ahead of compile and ahead of publish, pinned by a test asserting the fake deployer's publish counter is still 0 (`publish-ceiling.test.ts`). No hardcoded constant: with no recorded failure NOTHING is refused, because a fresh topology has to discover its own ceiling by failing once — this prevents the second waste, not the first. The stored value is `decidePublishOutcome`'s CATEGORY, not a boolean, and only `failed` rows move the ceiling. That distinction is the entry's own reproduction: `continia publish` returns `{"success": false, ...}` while EXITING 0, so `decidePublishOutcome` reaches `indeterminate`, never `failed` — a boolean would record nothing for the exact failure this row is about. See **R107**. Per-file check, not per-batch: a batch whose TOTAL exceeds the bracket while no single file does is what `--max-guards-per-batch` is for, and refusing it would be a false refusal. Scope: the bracket lives in the project's own `lethal.sqlite`, so two projects publishing to the same tier do not yet share the measurement. |
| **R91** | **The 30 s mutant budget floor is wrong by default for real AL, and the failure it produces is a tier quarantine rather than a verdict.** R47 records that `MIN_MUTANT_BUDGET_MS` is a hardcoded floor; `--mutant-timeout-ms` now reaches it, so what remains is that the DEFAULT is wrong and the consequence of it being wrong is disproportionate. **Measured 2026-08-04/05** on DO codeunit 6175297: at the default floor, three consecutive runs each stranded and quarantined the tier, costing ~10 min of recycle + `force-reset-lease` + `clear-quarantine` + resume each time. The stranding mutants were **`void-method-call` deleting a `SetCurrentKey`** — which does not hang, it makes the following filtered query pick a worse plan and scan. They are SLOW, not hung. `--mutant-timeout-ms 180000` eliminated them entirely (errors 2 → 0, single-pass completion, twice) while `M0013` — `negate-conditional` on `until ... Next() = 0`, genuinely non-terminating — still scored `timeout-killed` at 30172 ms. **That contrast within one run is the control**: the machinery distinguishes the two correctly once the budget is honest. Options, none yet chosen: raise the default; derive it from the observed per-mutant distribution (median here was 433 ms, p95 6.4 s, so 180 s is ~28× p95 and cost nothing); or make a budget overrun score `timeout-killed` rather than strand, which is the real asymmetry — an overrun is currently indistinguishable from a genuine strand on the fenced path. Evidence: `docs/campaign/2026-08-03-do/rung1.result.md`. | open |
| **R92** | **Nothing lets a user predict the DEPLOYED mutant count, and the two available numbers differ by enough to break a plan.** `--dry-run` reports mutation **sites** (raw specs); `SessionReport.mutants[]` holds **deployed** mutants after §3.2 dedup drops a Tier-1 mutant wherever a Tier-2 operator claims the same site. Measured on DO: **176 sites → 148 deployed** (-16%), **991 → 973**, **476 → 473**. The gap is not a constant and depends on the operator mix of the specific file. This is not cosmetic: the campaign's own rung-1 gate pre-committed the site count as the expected mutant count and `assertCardinality` correctly refused every anchor until it was corrected — the tool was right and the plan was wrong, but nothing in the tool's output would have prevented the mistake. Capacity planning, cost estimation and any pre-committed cardinality all need the deployed figure BEFORE the run. Cheapest fix: have `--dry-run` report both, since it already runs `generateMutationSet` and dedup is pure. Evidence: `docs/campaign/2026-08-03-do/rung0.precommit.md`, `rung1.result.md`. | **partial (`599b427`)** — Task 3 (event-stream refactor, spec 2026-08-05 §A) computes BOTH counts unconditionally in `runSession` (`orchestrator.ts:2111-2112`: `siteCount` from `allFiles`' raw specs, `deployedCount` via `dedupeSpecs(f.specs, tierOf)` over the same `allFiles` — `generateMutationSet` itself returns no dedup information at all, just `{ files, skipped, totalFiles, excludedByOnly }`), carries both on the `mutation-set-generated` event, and the stderr progress renderer (Task 5, landed on this branch after R92 was filed) prints both live. `--dry-run` specifically — the ORIGINAL ask, a number available before committing to a full run — is still not what changed; the two counts are only visible once a real session starts. Re-close only once `--dry-run` itself reports both. **done (`b95426b`)** — `--dry-run` now reports both: the header carries `N mutant site(s), D deployed mutant(s)`, a per-file section prints `sites=` and `deployed=` for every file (largest deployed first, since that ordering is what R90's per-file ceiling bites on), and every individual site the tier-precedence rule drops is marked `[not deployed — displaced by a higher-tier operator]` — a site listed identically to a mutant that will be measured is precisely how a site count gets pre-committed as a mutant count. The deployed figure comes from the SAME `dedupeSpecs` + `operatorTiers` pair `writeInstrumentedProject` and `runSession` use, never a second implementation of §3.2. Pinned by a test over a real cross-tier collision fixture asserting `sites > deployed`, so a fixture that stopped exercising the gap would say so. |
| **R104** | **`SessionConfig.emit` accepting a single subscriber, plus a separate `fanOutEmit` helper to combine several, is one layer of indirection more than the design needs.** `runSession` (`orchestrator.ts`) already fans events out to multiple listeners via `createEmitter([collector, ...])` internally; `cli.ts`'s `fanOutEmit` (`packages/runner/src/cli.ts:1714`) exists ONLY to pre-combine the stderr progress renderer (Task 5) and the `--progress-out` NDJSON sink (Task 6) into the ONE subscriber slot `SessionConfig.emit` currently allows, then unwraps that combination again inside `runSession`. Widening `SessionConfig.emit` to `readonly EventSubscriber[]` would let `runSession` fan out to all of them directly and delete `fanOutEmit` (and its dedicated test suite, `cli.test.ts`'s `describe("fanOutEmit (Task 6 review round 1)")`) entirely — one fan-out implementation instead of two. Found during the whole-branch review of the event-stream branch (2026-08-05); not fixed there since it touches Task 5/6's already-landed, already-tested surface rather than Task 4's. Evidence: `packages/runner/src/cli.ts:1699-1916`, `packages/runner/tests/cli.test.ts:987-1030`. | open |
| **R105** | **Two session-level bookkeeping structures in `runSession` are write-only and confirmed dead, kept only to avoid reopening a frozen contract.** `outcomes: SessionOutcome[]` (built by `record()`, `orchestrator.ts`) and `permissionRefusedTests`/`runnerDisagreementTests` (the two `Set<string>`s threaded through `RunMutantsOnBackendArgs`) were the event-stream refactor's (spec 2026-08-05 §A, `7449a89`) intended casualties — `buildReport` reads `collectedEvents` now, never `outcomes[]`, and the two report fields these Sets used to feed (`permissionsRefused`/`runnerDisagreement`) are folded from `mutant-scored.permissionRefusedTest`/`.runnerDisagreementTest` instead. Left in place deliberately: `record()`'s `outcomes` parameter is a Task 3 contract `events-orchestrator.test.ts` drives directly, and `RunMutantsOnBackendArgs`'s two Sets would need touching both its interface and its two call sites (sequential + worker fan-out) to remove — real surface area for zero behavioural gain. Confirmed genuinely dead (not merely believed so) during the whole-branch review: no consumer reaches either on any path to `buildReport`. Evidence: `packages/runner/src/orchestrator.ts` (the `outcomes`/`permissionRefusedTests`/`runnerDisagreementTests` declarations in `runSession`, each now documented in place as dead), `.superpowers/sdd/2026-08-05-event-stream/task-4-report.md` §"Residual dead code". | open |
| **R106** | **`report-fold.ts`'s `untargetedTriggerCount` has no presence check of its own, unlike the three events the fold treats as mandatory.** It sums `coverage-split` events with no assertion that any arrived — `orchestrator.ts` emits one per batch whenever `caps.coverage !== "none"`, and the fold currently trusts that unconditionally, the same trust model `deployMs`/`baselineMs`/`generateMutationSetMs` rely on for their own `phase-left` sums. If that assumption were ever violated (a batch running coverage-mode filtering that never emits `coverage-split`), the fold would silently under-report the count as a plausible zero rather than throw — the exact "absent tally read as a measured zero" shape the fold's mandatory-event design otherwise exists to close. Closing it needs a third signal (was `caps.coverage` ever non-`"none"` for a published batch) with the same shape as the `batchPublishedCount` reasoning the mandatory-baseline-evidence check already uses; deferred rather than added to the event-stream refactor without a concrete failure it is known to prevent. Evidence: `packages/runner/src/report-fold.ts:148-158`. | open |
| **R86** | **No kill records WHY it died, so a false kill and a real one are indistinguishable in the record.** MEASURED 2026-08-03 on the R82 gate run: `failure_note` is `NULL` for **all 109** killed mutants in the run database. The fixture's arm E — a swap killed by BC's *"The length of the string is 18, but it must be less than or equal to 10 characters"* under a test that asserts NOTHING — is stored identically to arm A, killed by a genuine assertion. The distinction survives there only because the fixture was built to make it inferable; on a real project nobody can infer it, and the reader is told their tests caught something when the platform did. Direction of the error is the bad one: it flatters the suite. Not hypothetical and not operator-specific — any mutant that makes the platform raise (overflow, division by zero, a JIT field-load failure) reads as a kill the assertions earned. **THE GAP IS A READ, NOT A CAPTURE — this row said the opposite for one commit and the correction is the useful part.** The text is already persisted: `test_results.failure_message` is NOT NULL for **110 of 110** failing rows in the same run 148, message plus `;`-separated AL callstack, written by `RunMutantTransport.failureTextOf` (`packages/runner/src/run-mutant-transport.ts`) on the fenced path mutants actually take and stored with a `mutantRowId` FK. What is missing is one JOIN: `MutantOutcome` (`packages/runner/src/report.ts`) carries `killingTest` and `failureNote` but never the killing test's failure text. Anyone sent to the backend to re-derive this is re-deriving something already in the database. **AND THE DISCRIMINATOR THIS ROW FIRST PROPOSED IS MEASURABLY WRONG.** It said the top callstack frame being in the TARGET app identifies a platform artifact. Classified over all 109 mutant-attributed failing rows in run 148: **4 have a target-app top frame, and only ONE is arm E.** The other three (M0059, M0062, M0066) are ordinary kills where the target's own `TestField`/`Get` raised — *"Category must have a value in Data Main"*, *"The Data Main does not exist"* — which is exactly the class the R82 spec §4 insists must be read APART from a false kill, since production validation detecting a bad state is a real kill. **75% false-positive rate on the only run the rule has been checked against.** A candidate the data does support, and which needs its own measurement before it is written down as fact: the failing frame's statement is not the mutated statement AND the message is a platform-class error. Note also the fixture is structurally blind to how real assertion kills look — all 22 of its tests raise via bare `Error(...)`, while Continia Document Output's suite uses Microsoft's Library Assert ~1,886 times, which puts a THIRD app on top of the stack. Per R72's discipline a diagnosis must NOT move a verdict: a killed mutant stays killed, this is a `cause` field, never a re-scoring. The message is also PROSE and will localise (R66 measured that class), so an English-sentence detector would be another English-only detector. Evidence: `docs/measurements/README.md` §"R82 live"; both corrections found by an adversarial review of the landed work and re-measured independently. | open — the text is persisted and unread (110/110); the proposed top-frame discriminator is FALSIFIED (3 of 4 are ordinary kills); precondition for R85's kill-rate split |
| **R81** | **`buildCallerIndex` keys call sites on the BARE owner name, so two same-named objects merge their callers — the sibling of R70.** `siteKey(owner, proc)` (`packages/engine/src/semantic/callers.ts`) built `"<owner>::<proc>"` from `obj.name`, so `table 50000 "CDO Setup".Configure` and `page 50000 "CDO Setup".Configure` landed in ONE bucket and each was reported as a caller of the other. The row asked for a MEASUREMENT before a fix — *what reads `callersOf`, and can a merged bucket reach a claim or a verdict?* | **done (`2af5e23`) — measured first, and the measurement made the fix free.** **Severity today is ZERO, for a reason worth recording: `callersOf` has NO production consumer.** No package’s `src` reads it; `SemanticCapability` is `"symbol-table" | "cfg" | "type-info"`, with no value an operator could use to request caller lookup; its only reader in the whole repo is one assertion in `packages/engine/tests/semantic/context.test.ts`. So a merged bucket could never reach a claim or a verdict — and R70’s stated reason for deferring this (widening the key “would have changed a second consumer under the same commit”) is now measured FALSE: there is no second consumer to change. Fixed rather than left, because the safety was routed through the ABSENCE of a consumer, which is exactly the kind of guarantee this project has learned that future work erodes — the first operator to ask “who calls this?” would have inherited a silent wrong answer. `siteKey` now keys on `objectScopeKey(kind, name)`, the same currency R70 established for the scope maps, so this is the layer’s existing design rather than an API invented for a hypothetical consumer. `CallSite.fromOwner` stays the bare display name (it is not a lookup key). Pinned by “keeps two same-named objects of different KINDS in separate buckets (R81)”, which asserts each bucket’s CONTENTS rather than its size — a size-only check passes if the two are merely swapped. Red-checked surgically: restoring the bare-name key in `siteKey` alone, leaving resolution untouched, fails exactly that one test and nothing else. |
| **R80** | **Two AL comment-strippers existed in two packages, and they disagreed about string literals.** `stripAlComments` (`packages/schemata/src/project.ts`) tracked strings but left their CONTENTS intact; `maskNonCode` (`packages/runner/src/discovery.ts`, R79) blanked the contents too, because a literal `codeunit 50100 "Sales Post"` has the same shape as a header. Written three weeks apart, in one codebase, for one class of bug, neither knowing about the other. The row warned the fix is NOT a blind merge: `stripAlComments` feeds `objectHeadersOf`, so widening it changes mutant ATTRIBUTION. | **done (`bad29a1`) — measured first, then unified with the policy kept as a flag.** The row's premise ("nothing is WRONG today") was asserted, never measured. Measured now: over **717** real `.al` files (Continia Document Output's Cloud + Test, every fixture, the control app), the two policies produce **identical object-header sets on every file — 0 differences**. The attribution risk is therefore real in principle and fires nowhere, which is what made the merge safe to make rather than safe to assume. One lexer now lives in `@lethal/engine` (`maskAlNonCode`, `packages/engine/src/ast/mask.ts`) with `blankStringContents` as the ONLY policy difference: discovery keeps `true` (the half R79's bug turned on), attribution keeps `false`. **Behaviour delta characterised rather than assumed:** the runner's masked output is **byte-identical on all 717 files**, and schemata's differs on 369 of them at *carriage returns inside blanked comments only* — the old stripper turned `\r` into a space, the shared one preserves it — verified by diffing every differing offset against the pre-refactor implementation recovered from git: **0 differences anywhere else**. Both live gates re-run per-mutant: `itest:bcdev` 3/10/3, `itest:tables` 84/12/10 with `untargetedTriggers` 0. Ten unit tests pin the contract, each red-checked — and **three were rewritten because the first versions passed with the branch deleted**: the `''` escape is output-equivalent on well-formed AL (adjacent literals tile the same span), so pinning it needs an odd-quote line; and both unterminated-quote rules need a LATER quote in the file to pair against, or there is nothing to swallow and both rules look alike. |
| **R93** | **al-runner v2 landed upstream and every flag we send is gone — the gate is measured RED.** Upstream PR #1654 "AL Runner v2: full architecture cutover" merged 2026-08-05 (2,340 files); v2 links unmodified MS/ISV DLLs via Cecil + JMP hooks instead of v1's Roslyn type-renaming rewriter, and rewrote the CLI. `OneShotTransport` (`packages/runner/src/al-runner-transport.ts:44-59`) sends `--run <method>`, `--packages`, `--stubs`, `--test-timeout`; **none exist in v2** — `--test`/`--filter` replaces `--run`, `--package-cache` replaces `--packages`, `--stubs` is dropped, `--test-timeout` became an env var. Any unknown `--` flag prints `Unknown option '--run'.` and exits 2 (upstream `AlRunner/Program.cs:305-308`). Independently, per-test names are now qualified — `Codeunit79100.OverBudgetDetected` (`AlRunner/Reporter.cs:190`, `AlRunner/ServerProtocol.cs:119`) — while `run()` matches the bare `ref.method` (`al-runner-backend.ts:281`), so even a corrected argv would report every mutant as "al-runner output missing the requested test". **Measured 2026-08-05**, both runs on the same fixture, same session: v1.0.31 (`~/.dotnet/tools/al-runner.exe`) → `itest:alrunner` PASS at the frozen 3 killed / 13 survived / 0 no-coverage, `baselineGreen=true`; a local Release build of upstream `main` + PR #1657 → all 16 mutants `error note=no green baseline tests`, `baselineGreen=false`, gate aborts on `baseline must be green`. The failure is loud, which is the one good thing here. Server mode is closer but also broken — see R97. Work needed: version-detect on `al-runner --version` (which v2 has, contradicting the comment at `al-runner-backend.ts:121`) and branch the argv/decode, keeping the v1 path until 2.0.0 is actually published. **Do not start until R98 clears** — v2 does not run on Windows yet. | open |
| **R97** | **v2's server protocol keeps our wire shape but ignores our second bundle.** `ServerTransport` sends `sourcePaths: [sourceDir, testDir]` (`packages/runner/src/al-runner-transport.ts:234`). v2's `runTests` handler uses **`SourcePaths[0]` only** (upstream `AlRunner/Program.cs` runTests branch; stated in `docs/server-mode.md`: "bundle dir(s); uses the first"), so the test app never runs and every mutant decodes as "output missing the requested test". Everything else about the protocol survived deliberately — `{"ready":true}` handshake, `runTests`/`shutdown`, the response envelope, and `stubPaths` accepted-and-ignored (`ServerProtocol.cs:22-30`) — so this transport is the cheaper one to port. Two v2-only properties to design against: the AL-output cache is keyed on a hash of the `.al` sources (a selector rewrite per mutant is a guaranteed miss, so no staleness risk, but also no free ride), and a warm reload does **not** clear BC's table field-set cache, so a table *shape* change needs a server restart (`docs/server-mode.md` §"Known limitation"). Statement-level mutants are unaffected; a batch swap that changes table fields is not. | open — blocked on R93 |
| **R98** | **al-runner v2 does not run on Windows, and we are Windows.** Upstream `main` P/Invokes `libc`'s `mprotect` for page protection: `System.DllNotFoundException: Unable to load DLL 'libc'` at `AlRunner.Infrastructure.JmpHook.InstallIndirect`, thrown from `BcRuntime.EnsureApplied` before any test runs (measured 2026-08-05 on a local Release build of `main`). Upstream PR **#1657** ("feat(windows): real Windows support via VirtualProtect", OPEN, MERGEABLE, 4 files) adds the `VirtualProtect` path and a `File.Replace` fix for the Defender file-lock on the rewritten DLL. Verified here: with #1657 checked out, the same build runs `fixtures/sandbox-app` + `fixtures/sandbox-tests` to 2P/0F/0E, `exit=0`. Also note the release is not out — tag `v2.0.0` exists but NuGet `MSDyn365BC.AL.Runner` is still at 1.0.31 (two failed publish attempts, upstream PR #1656 fixes the workflow). So: no v2 adoption work lands until #1657 merges AND 2.0.0 publishes, and the first thing the adapter does is refuse a v2 build without the Windows fix rather than let it fail as an unexplained patch-time crash. | blocked (upstream PR #1657 + the 2.0.0 NuGet release) |
| **R99** | **v2 fixes R7 and R8 — measured — so the canary and `authoritative: false` both need re-deciding, not re-asserting.** Both defects that justify `capabilities().authoritative = false` (`packages/runner/src/al-runner-backend.ts:104-118`) are gone on v2, measured 2026-08-05 with the same two probes `al-runner-canary.ts` uses, run against a local v2 build: `asserterror I := 1;` correctly **fails** — `NavNCLAssertErrorException: An error was expected inside an ASSERTERROR statement.` (v1: reported `pass`, R7); and a table-object global written by an `OnValidate` trigger **survives** the return from `Validate()` — `TouchCount()` returns 1 (v1: 0, R8). That is exactly the outcome the canary was built to detect instead of asserting a frozen claim, so the canary's design held; but the canary itself runs through `OneShotTransport` and therefore cannot execute at all on v2 (R93). Work: port the canary with the transport, and re-derive `authoritative` from what is actually left on v2 — `coverage: "none"` still stands, as do the architectural limits (no service tier, no transactions, `Commit()`/`Rollback()` no-ops, `StartSession` inline, empty base-app tables — upstream `docs/limitations.md`). Do NOT flip `authoritative` on the strength of these two probes alone. | open — blocked on R93 |
| **R101** | **v2 surfaces we should use, not just survive.** From the v2 CLI (`al-runner --help`, `--guide`), measured against a local build 2026-08-05: (a) **`--bc-version` becomes mandatory for us** — the runner picks "latest artifact present in the cache" by default and, per its own guide, **does not print the selection**, so a second artifact appearing on the machine can silently change verdicts between runs; a verdict gate cannot accept that. (b) `--out <path>` (failure-classification JSON) and `--output-junit` give per-kill evidence, which is what **R86** is about. (c) `--define` / `--preprocessor-symbols` — real projects need AL preprocessor symbols to compile outside their environment, and LethAL has no way to pass any today; omitting a required one produces real-looking test failures. (d) `--no-cache` — upstream notes defect classes that appear only on a cache HIT or only on a MISS; worth having on the red-check path. (e) `--version` exists now, which is what R93's version-detect branches on. (f) `RunnerOutOfScopeException` — SMTP, outbound HTTP, printing, external file I/O and web-service publishing now throw loudly instead of returning a fake value, so tests touching them error on v2 where v1 may have passed them; that changes what a `survived` verdict means for those tests and should be named in the report. | open — blocked on R93 |
| **R65** | **A failed tool spawn can report nothing at all — and it happened again on the PUBLISH path 2026-08-05.** Second measured instance: an env-tool publish of a 660-guard instrumented file failed and surfaced as a bare `Error` with no text at `bcdev-backend.ts:468`, exactly the shape R64's wrong-platform binary produced. The user sees a stack trace and no cause; the actual cause was a proxy timeout (R90). Confirms this is a CLASS rather than the single path R64 fixed. Original text follows.  `ArtifactCompiler.compile`'s catch stringifies `err.message` (`packages/runner/src/artifact.ts`), and a Bun spawn `ENOENT` arrives with an EMPTY message — so R64's wrong-platform binary presented as a bare `Error` with no text, and took a long external debugging session to trace. The path fix removed one CAUSE, not the class: a missing exec bit on `bin/linux/alc` (the AL extension's own activation is what chmods those, so an unpacked-but-never-activated VSIX has them non-executable), a pinned `alcPath` typo, or a partial install all still land in the same silent catch. Fix: include `err.code`/`err.path` (and a `String(err)` fallback) in both spawn catches — `artifact.ts` and `publisher.ts`'s `altool publishapp failed` wrapper — and consider an existence/executability check inside `defaultAlToolPaths` so the failure names the file instead of the operation. | done — `describeThrown` (`packages/runner/src/describe-error.ts`) backs the catches in `artifact.ts`, `publisher.ts` AND `env-tool.ts`. Two contracts, each pinned by a red-checked test: it never returns an empty string, and it **never throws** — it runs inside catch blocks, and `String(err)` alone raises on `Object.create(null)` or a hostile `toString`, which would replace the caller's real error with the describer's own (R65's own failure mode, worse). The env-tool site was added beyond the two the entry named: that is the one path where the tool path comes from the USER'S config, so a typo is likeliest there, and its diagnosis stays inside `redact()`. `ERRNO_FIELDS` is deliberately `code`/`syscall`/`path` and a test pins it: `artifact.ts` and `publisher.ts` do NOT redact what they print, and Node hangs `spawnargs`/`cmd`/`env` on spawn failures, so widening the list would leak `BC_SERVER_PASSWORD` into an unredacted string. **The executability check was deliberately NOT built**: the R64 tests stage an extension dir with no `bin/` at all, so an existence check inside `defaultAlToolPaths` would break them and make per-RID selection untestable without staging fake binaries per platform — the exact testability property R64 fought for. The errno `path` names the file anyway, which is what that sub-item wanted. `orchestrator.ts`'s `messageOf` still uses the bare idiom and is left alone: every spawn failure reaching it has already been described upstream. |

---

**Recently closed** (delete these once a release has passed):

- Tier-2 Phase 0 — table triggers mutate, execute and kill on a live server; merged 2026-07-25 (`841069c`), frozen at `itest:tables` 3 killed / 2 survived / 2 no-coverage. Superseded by Phase 1's 63 / 10 / 2 over the same, larger fixture.
- Tier-2 Phase 1 (R10) and the live dedup-collision proof (R12) — see their rows above.
- Coverage keyed on `(objectType, objectId)` rather than the bare id (`6e89948`) — a table and a codeunit sharing an id sent a trigger mutant at the wrong object's tests.
- Per-mutant time budget floored at 30 s (`ab58469`) — an unfloored `2 × baseline` quarantined a cold start as in-flight-unknown.
