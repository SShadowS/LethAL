# LethAL Roadmap — open work and known limitations

<!-- GENERATED FILE. Do not edit ROADMAP.md. Edit docs/roadmap/R<nnn>.md, or this header in
     docs/roadmap/_template.md, then run `bun scripts/roadmap-index.ts`. -->

Living list of everything known-but-not-done: planned work, correctness risks we have measured and
not yet closed, and product gaps a real user would hit. Session ledgers under `.superpowers/` are
scratch and get archived; **`docs/roadmap/` is the durable record.**

**This page is generated.** One item = one file, `docs/roadmap/R<nnn>.md`; this index is built from
them by `bun scripts/roadmap-index.ts` and must never be hand-edited. Reading an item means reading
its file — which is also why the old single-table form is gone: its cells contained inline `|`, so
a field-wise read silently returned a fraction of a row and looked complete (R118).

## How to use this file

- **Add an item the moment it is discovered**, even mid-task — one line of "what breaks, for whom"
  beats a perfect write-up later. Items get a stable `R<n>` id; never renumber, never reuse an id.
  Adding one means writing `docs/roadmap/R<nnn>.md` (zero-padded) and re-running the generator;
  two sessions filing at once cannot collide.
- **Every item names its evidence** — a file, a commit, a measured result. An item with no evidence
  pointer is a rumour, not a roadmap entry.
- **Status:** `open` · `in progress` · `blocked (<on what>)` · `done (<commit>)`. The `status`
  frontmatter field holds it in full; this index shows an abbreviation.
- **Closing an item:** mark it `done` with the commit, leave it in place for one release cycle, then
  delete its file. Deleted items live on in git history.
- **Do not** duplicate what the code or `design.md` already says. This file records what is *missing*
  or *wrong*, not how the system works.

Priority is deliberately not a column: the `order` field sets the ordering inside each section, and
that ordering is the priority.

**7 of 174 items are OPEN. A row counts as closed only when its status opens with `done` or `closed`; `PARTIALLY fixed`, `additive half DONE`, `SPIKED` and `blocked` are all open.**

---

## Next up

- **R10** · Tier-2 Phase 1 — the first real Tier-2 operators · [R010.md](docs/roadmap/R010.md) · done (branch `tier2-phase1`, final fixes `d0070db`) — four operators shipped…
- **R12** · Live proof of a dedup collision · [R012.md](docs/roadmap/R012.md) · done (branch `tier2-phase1`) — the Phase-1 fixture fires it for real: 6 Tier-1 `lethal.void-method-call`…
- **R11** · `tierRank` has no tier-3 rank · [R011.md](docs/roadmap/R011.md) · done (`d2b3236`) — by deciding it, not by adding the rank. R13 measured that tier 3 does not become real, so…
- **R13** · Tier-3 operators · [R013.md](docs/roadmap/R013.md) · done (`d2b3236`)
- **R15** · Custom environment tool support · [R015.md](docs/roadmap/R015.md) · done (gate PASSED 2026-07-26, baseline frozen)
- **R16** · Run the `envtool` live gate and record its baseline · [R016.md](docs/roadmap/R016.md) · done (2026-07-26 — 3 killed / 10 survived / 3 no-coverage, matching the container gate)
- **R133** · Vendored tree-sitter-al is 3.2.1; upstream 4.0.0 is a breaking release that moves named trees LethAL hashes · [R133.md](docs/roadmap/R133.md) · done (af3d9a3) — bumped 2026-08-12, same day as filed. One grammar-caused code fix…

## Correctness risks (measured, not closed)

- **R1** · ~~Fenced-path writes fail~~ — REFUTED 2026-07-26, twice over · [R001.md](docs/roadmap/R001.md) · done (refuted — the limitation did not exist)
- **R2** · Single-tenant containers only, unenforced · [R002.md](docs/roadmap/R002.md) · done (b2339d3 — warns once per process; limitation stated in fixtures/README.md)
- **R94** · al-runner v2 turns a hung mutant into a KILL · [R094.md](docs/roadmap/R094.md) · done (`51e6415`) — and the fix had to change shape within one session: 2.0.0.0 said `TIMEOUT after <n>s`…
- **R95** · v2 re-meaned exit codes 2 and 3, and our decode swallows the dangerous one · [R095.md](docs/roadmap/R095.md) · done (`51e6415`) — exit 2 maps to `error`, and the `skip` member is DELETED from the transport union rather…
- **R96** · `--test-isolation method` silently stopped meaning per-method isolation · [R096.md](docs/roadmap/R096.md) · done (`51e6415`) — the transport sends `--isolation test`, so `capabilities().isolation = "full-reset"` is…
- **R8** · al-runner drops a table global var written by a trigger · [R008.md](docs/roadmap/R008.md) · done (Wave 1 Stream D — root-caused, upstream, detectable by canary)
- **R121** · A false kill is now VISIBLE but still unclassified — the only proposed discriminator is measured wrong at 75% · [R121.md](docs/roadmap/R121.md) · done (2026-08-09) — the SCREEN shipped, and only the screen. `SessionReport.assertionScreen` reports how many…
- **R132** · No gate ever exercises the assertion screen in the mode where it discriminates · [R132.md](docs/roadmap/R132.md) · done (6acca8c) — itest:tables now measures `partial` through a twin pair, itest:bcdev picks up the `vacuous`…
- **R137** · swap-rec-xrec's conformance refusals assert nothing — empty expectedSpecs passes on any input · [R137.md](docs/roadmap/R137.md) · done (3d2c336) — the harness now fails a refusal case that emits, and the sweep found a refusal that had…
- **R138** · The platform-artifact screen cannot see run-trigger-flag kills: Insert(false) kills by duplicate key, and nothing tags it · [R138.md](docs/roadmap/R138.md) · done…
- **R140** · Fallback 2 scores a trigger mutant `survived` when its only covering test is red, where member-level attribution declines · [R140.md](docs/roadmap/R140.md) · done (fallback 2 declines; itest:tables unchanged at 191/31/10, untargetedTriggerCount 0)
- **R141** · flip-filter-literal's character-class refusals are proven offline only: no live negative anywhere · [R141.md](docs/roadmap/R141.md) · done (e66c78d) — arm I measures the character refusal live; the tables gate is frozen at 187/31/10 over 228…
- **R142** · A conformance case with a non-empty expectation still never checks for EXTRA specs · [R142.md](docs/roadmap/R142.md) · done — measured first (31 non-empty cases across all 15 operators, ZERO over-produce), then the exactness…
- **R143** · `run-trigger-skipped-insert` tags every Insert mutant, so the platform screen over-reports; narrowing it needs the target table's OnInsert · [R143.md](docs/roadmap/R143.md) · done (efb6076) — LIVE-CONFIRMED 2026-08-14 by `itest:tables`. `insertSkipCanRaise`…
- **R144** · The declarative sites LethAL drops are counted and then thrown away in a stderr warning the report never carries · [R144.md](docs/roadmap/R144.md) · done (c322539) — LIVE-CONFIRMED 2026-08-14 by `itest:tables`, which now pins `declarativeSites` at 1 site in…
- **R156** · Test-isolation granularity was filed as unmeasured; it is defined at procedure level by the NST, and LethAL runs ONE method per invocation · [R156.md](docs/roadmap/R156.md) · closed 2026-08-16 — filed and withdrawn the same day: the premise was wrong. Isolation is per test procedure…
- **R157** · REQUIRED fields were added to the report without bumping `schemaVersion`, so one version number describes several shapes · [R157.md](docs/roadmap/R157.md) · done (this commit) — DECISION taken: an added OPTIONAL field is free, an added REQUIRED field bumps. Enforced…
- **R160** · `typeOf` cannot type a call or a record field, which is where BC arithmetic actually lives · [R160.md](docs/roadmap/R160.md) · done (this commit) — `member_expression` and `call_expression` now type; PROVABLY INERT for every shipped…
- **R166** · `astSubtreeHash` erases method and label names, so three different guard deletions share one identity · [R166.md](docs/roadmap/R166.md) · done (this commit) — the enclosing PROCEDURE (or trigger) joined the identity. `itest:tables` goes from 225…
- **R167** · The AL extension moved `alc.exe` out of `bin/win32/`, and tool discovery built a path it never checked · [R167.md](docs/roadmap/R167.md) · done (this commit) — discovery walks newest-first and verifies the tool EXISTS, trying the per-RID directory…
- **R172** · An equivalent mutant and a coverage gap both report `survived`, and nothing in the record separates them · [R172.md](docs/roadmap/R172.md) · PARTLY fixed (this commit): proposal 1 shipped. An operator can declare an `EquivalenceRisk` and the report…
- **R175** · `no-coverage` on a PUBLIC procedure is an inference, not an observation, and the inference is wrong · [R175.md](docs/roadmap/R175.md) · FIXED for the fenced default (this commit), and the mechanism is now measured rather than guessed: the gate…

## Product gaps a real project hits

- **R3** · Selector object ids are hardcoded · [R003.md](docs/roadmap/R003.md) · done (Wave 1 Stream B — CLI flags + config, validated against app.json idRanges)
- **R4** · Two instrumented projects cannot share one BC container · [R004.md](docs/roadmap/R004.md) · done (Wave 1 Stream B — proven live on Cronus281, two instrumented projects side by side)
- **R5** · The report does not say how much of the project was skipped · [R005.md](docs/roadmap/R005.md) · done (Wave 1 Stream C — SessionReport.notInstrumented, into --out and --dry-run)
- **R6** · A file declaring two AL objects is refused outright · [R006.md](docs/roadmap/R006.md) · done (Wave 1 Stream C — per-object attribution for all-injectable files)
- **R49** · `lethal --help` does not exist, and it is the first command a new user runs · [R049.md](docs/roadmap/R049.md) · done — `--help`/`-h`, `--version`/`-V`, and a bare `lethal`, all intercepted BEFORE `parseArgs`…
- **R50** · Neither wasm asset survived `bun build --compile`, so the standalone binary could not parse a single line of… · [R050.md](docs/roadmap/R050.md) · done — both now reached via Bun's `file` loader (`import ... with { type: "file" }`), which is what makes…
- **R100** · al-runner v2 makes BC artifacts a hard local prerequisite, and rejects a symbols-only `.alpackages` · [R100.md](docs/roadmap/R100.md) · done (`4727279`) — a dead baseline now carries the backend's own failure message, so al-runner's…
- **R66** · The `TestPermissions` refusal detector is English-only · [R066.md](docs/roadmap/R066.md) · done (this commit) — `PERMISSIONS_REFUSAL_STRUCTURAL_RE` matches the `(TableData <id> <name> <op>: <suite>)`…
- **R109** · No single command answers "is this environment ready to measure?" — the pre-flight refusals exist but only… · [R109.md](docs/roadmap/R109.md) · done (`58708e9`) — shipped, then reviewed across three fix waves (a3c9a93, 8ea4eb5, 5f57e39). Round 2 is the…
- **R110** · The control app exposes no read-only peek at the lease, so nothing can answer "is this tier held?" without… · [R110.md](docs/roadmap/R110.md) · done (`pending`) — `HarnessInfo` now reports `leaseOwner`/`leaseOpKind`/`leaseExpiresAt`/`leaseTokenPresent`…
- **R111** · The env-tool read-only resolution is DUPLICATED across two commands, and the safety property is carried by a… · [R111.md](docs/roadmap/R111.md) · done (`pending`) — extracted as `prepareBcdevReadOnly`, and the no-provisioning boundary is now carried by…
- **R112** · R90's publish ceiling can be INSPECTED and CLEARED only by a bcdev-shaped config, so on an env-tool project… · [R112.md](docs/roadmap/R112.md) · done (`pending`) — `--dry-run` resolves an env-tool tier read-only and prints the bracket; `clear-ceiling`…
- **R113** · Nothing structurally couples `report.ts`'s `Caveat` union to the code that re-declares or reads it, and the… · [R113.md](docs/roadmap/R113.md) · done (`pending`) — (1) closed with `as const satisfies readonly Caveat[]` plus a RUNTIME test, deliberately…
- **R114** · A STRANDED mutant records no `cause`, so every consumer keyed on `MutantOutcome.cause` is blind to exactly… · [R114.md](docs/roadmap/R114.md) · done (`77a670c`) — `cause: "stranded"` is its own member, set at exactly the two `STRANDED_NOTE_PREFIX`…
- **R122** · Two more `error` shapes still record no `cause`, and one of them is labelled `deadline-exceeded` when it is not one · [R122.md](docs/roadmap/R122.md) · done (`2740d53`) — (a) the op-in-flight-never-cleared pair now records `stranded` and writes the strand note…
- **R115** · `lethal explain`'s output guard is DATA-derived, not TYPE-derived, and the checks that would close the gap… · [R115.md](docs/roadmap/R115.md) · done (`pending`) — all three gaps closed at RUNTIME. (2) `e4f72f4`. (1) the leaf pin is now asserted equal to…
- **R127** · The only scope knob is a FILE glob, so measuring one operator on a real project costs every other operator's mutants in those files · [R127.md](docs/roadmap/R127.md) · done (`0d78df4`) — `--operator <name>`, repeatable, the `lethal.` prefix optional, resolved once in…
- **R116** · A survivor can carry `executionProven: true` beside `guardEvidence: "not-observed"` — two of our own signals… · [R116.md](docs/roadmap/R116.md) · done (`pending`) — the pair is NOT a contradiction, and the projection now NAMES it: a derived `reach` field…
- **R117** · Cross-file `file.ts:NNN` citations rot silently, and this repo now has four measured instances — two of which… · [R117.md](docs/roadmap/R117.md) · done — 97 pointers replaced with greppable names across live source, tests and the roadmap, and…
- **R118** · `ROADMAP.md` rows are read field-wise on `|`, and 115 of 166 rows contain INLINE pipes — so a field-wise read… · [R118.md](docs/roadmap/R118.md) · done (this commit) — the table is GONE, not parsed more carefully: one item per file under `docs/roadmap/`…
- **R134** · Mutate inside SetFilter's filter-expression literal — the one filter class no operator can see · [R134.md](docs/roadmap/R134.md) · done (4f5e896)
- **R135** · Declarative surfaces (FlowFields/CalcFormula, query objects, filter properties) need a mutation mechanism before any operator · [R135.md](docs/roadmap/R135.md) · closed 2026-08-14 — RULING: Option C. LethAL does not mutate declarative surfaces. Option B is dead on…
- **R136** · Tier-2 executable candidates: FindFirst/FindLast swap, Validate-to-assign, run-trigger flag on Insert/Delete · [R136.md](docs/roadmap/R136.md) · done (1d259ca)
- **R139** · A stale published TEST app is indistinguishable from genuinely failing tests, and costs a full gate run to diagnose · [R139.md](docs/roadmap/R139.md) · done — check 1 (16b4014) refuses on the server's own words, check 2 (91bf897) compares the PUBLISHED test set…
- **R145** · Per-mutant artifact variants for declarative surfaces — measured, costed, and blocked on demand rather than on effort · [R145.md](docs/roadmap/R145.md) · open — filed 2026-08-14 by R135's ruling, deliberately NOT scheduled
- **R150** · `lethal explain`'s survivor list is unbounded, so the projection built for agents does not fit an agent · [R150.md](docs/roadmap/R150.md) · done (`--top <n>`, this commit) — the cap ships with the count block that keeps a capped list from reading as…
- **R151** · `lethal doctor` renders prose only, so the pre-flight an agent should run FIRST is the one it cannot parse · [R151.md](docs/roadmap/R151.md) · done (`--json`, this commit) — doctor emits `DoctorJsonOutput`; `run` and `campaign` deliberately unchanged
- **R152** · Three versioned output surfaces, no published schema for any of them, so a consumer validates by hoping · [R152.md](docs/roadmap/R152.md) · done (this commit) — all four surfaces published: explain and doctor hand-written and pinned, report and…
- **R153** · Nothing in the repository tells an OUTSIDE agent how to call LethAL or how to read what it returns · [R153.md](docs/roadmap/R153.md) · done (this commit) — a reference doc and a copyable skill, both checked against the code by…
- **R155** · The gift card demo app's VERDICTS have never been measured against a live server, only its mutant inventory · [R155.md](docs/roadmap/R155.md) · done (this commit) — measured on Cronus281 2026-08-16: 20 killed / 9 survived / 7 no-coverage over 36, score…
- **R158** · Every refusal reached the user as a stack trace, including the first command the README tells them to run · [R158.md](docs/roadmap/R158.md) · done (this commit) — the message by default, the stack behind LETHAL_DEBUG=1, and a line saying where it went
- **R159** · 26 of 31 behaviour-carrying AL node kinds are never claimed by any operator, and arithmetic is the one with no recorded reason · [R159.md](docs/roadmap/R159.md) · open, FIVE built. `shift-integer` shipped (this commit) with `codeunit 79325 "Data Shift Ops"`; all 16 new…
- **R161** · 1,118 call sites are refused because they are an un-braced branch body, and ten operators share the loss · [R161.md](docs/roadmap/R161.md) · done (`6e0a64b` + this commit) — 1,280 sites gained, 0 lost, set-diffed per operator; `itest:tables`…
- **R162** · `swap-enum-member`, built; 2,355 sites exist but only 65 are the project's own enums · [R162.md](docs/roadmap/R162.md) · done (this commit) — `lethal.swap-enum-member` (Tier 2, 1.0.0); footprint 65, not 2,355; demo campaign…
- **R163** · `not` on a bare call or identifier is invisible to every operator: 1,093 unclaimed sites · [R163.md](docs/roadmap/R163.md) · done (this commit) — `lethal.remove-not` (Tier 1, 1.0.0), 1,106 corpus sites; `itest:tables` re-frozen LIVE…
- **R164** · 290 loop-exit negations score only with an opt-in flag that ends a session on the user's server · [R164.md](docs/roadmap/R164.md) · done (this commit) — `lethal.loop-truncate` (Tier 1, 1.0.0) ships WITH the cession it needs…
- **R165** · `swap-modify-flag` has no forward direction; probed at 49 honestly-claimable sites, not the 419 first counted · [R165.md](docs/roadmap/R165.md) · done (this commit) — `swap-modify-flag` 1.2.0 claims the argument-less form, scoped to tables this project…
- **R171** · A bare-Boolean `if` guard has no polarity mutant: 1,891 marginal sites, and the ceiling does not collapse · [R171.md](docs/roadmap/R171.md) · done (this commit) — `lethal.negate-guard` (Tier 1, 1.0.0) and `remove-not`'s corrected cession, landed with…
- **R173** · Three operators can still strand a loop, and R164's cession deliberately did not cover them · [R173.md](docs/roadmap/R173.md) · open, and NARROWED by measurement 2026-08-27. The residual is not 57 vague sites, it is ONE operator on ONE…
- **R174** · A Stryker.NET-style `ignoreMethods` call-site ignore list — designed, measured on real AL, refused on value · [R174.md](docs/roadmap/R174.md) · closed 2026-08-26 — refused on VALUE after measurement: 0.38% of raw specs for the safe pattern set, 1.70%…
- **R176** · Six shared CLI flags were accepted by subcommands that ignore them, and R151 had already named the hazard · [R176.md](docs/roadmap/R176.md) · done (this commit) — the per-flag guard R151 built for `--json` is now a table over every shared flag…

## Backends and tooling

- **R7** · al-runner's `asserterror` never fails a test · [R007.md](docs/roadmap/R007.md) · done (Wave 1 Stream D — startup canary measures the real binary each session)
- **R9** · `itest:tables` runs its session once · [R009.md](docs/roadmap/R009.md) · done (86fb417 — runs twice, asserts verdict-identical; live-verified)
- **R14** · Stay on the newest tree-sitter-al · [R014.md](docs/roadmap/R014.md) · recurring — RE-CHECKED 2026-08-19: `git ls-remote --tags` still lists `v4.0.1` (`58c236f`) as the newest…
- **R17** · The env-tool crash-recovery record has a writer and no reader · [R017.md](docs/roadmap/R017.md) · done (f616885)
- **R18** · `envTool` + `--backend al-runner` is silently ignored · [R018.md](docs/roadmap/R018.md) · done (f616885)
- **R19** · The prepublish step and the control-app republish both happen BEFORE the lease is acquired · [R019.md](docs/roadmap/R019.md) · done for the half that is possible; the other half is impossible by construction. The entry says "move both…
- **R20** · `HarnessVerificationError` conflates "wrong build" with transport failures including 401/403 · [R020.md](docs/roadmap/R020.md) · done — `HarnessAuthError` is thrown from `fetchHarnessInfo`'s 401/403 branch, checked BEFORE the stale-build…
- **R21** · Env-tool mode still hard-requires `altool.exe` to exist · [R021.md](docs/roadmap/R021.md) · done (f616885)
- **R22** · Deferred test-quality gaps on the env-tool code · [R022.md](docs/roadmap/R022.md) · done (b629d2a, f616885 — all five now discriminate)
- **R23** · Nothing forbids `username`/`password` in `envTool.publish.reads` · [R023.md](docs/roadmap/R023.md) · done (b629d2a)
- **R24** · The env-tool bcdev-key collision check is opt-in · [R024.md](docs/roadmap/R024.md) · done (b629d2a)
- **R25** · A stale locally-built `lethal-control.app` fails with a confusing error · [R025.md](docs/roadmap/R025.md) · done (b2339d3 — names the stale build, keeps BC's text)
- **R26** · The permission canary measures itself, not the target · [R026.md](docs/roadmap/R026.md) · done — canary now declares `TestPermissions = Disabled`, so it measures the platform rather than its own…
- **R27** · ~~Trigger mutants whose tests write are unscored~~ — VOID, follows R1 · [R027.md](docs/roadmap/R027.md) · done — a permissions refusal is no longer a bare `unstable`: the failure note names the likely missing…
- **R28** · Nothing detects a stale `LethAL Control` build · [R028.md](docs/roadmap/R028.md) · done (`see below`) — `ControlApi.CurrentAppVersion` now reports the module's real `AppVersion` via…
- **R29** · False survivors from coverage attribution · [R029.md](docs/roadmap/R029.md) · done (Tier-2 Phase 1)
- **R30** · Mutation sites written inside `tableextension`/`pageextension` are refused · [R030.md](docs/roadmap/R030.md) · done (`c1da575` closed the last dependency) — CLOSED 2026-08-08 after re-deciding. All three halves shipped…
- **R31** · Nothing detects a stale published test app, and it has now cost two debugging sessions · [R031.md](docs/roadmap/R031.md) · done (`see below`) — the evidence was already present per-test and nothing aggregated it…
- **R32** · The 10 surviving Phase-1 mutants were never individually verified as genuine · [R032.md](docs/roadmap/R032.md) · done (2026-07-27) — all ten genuine, zero false survivors. Each driven through the fenced path against ALL 19…
- **R33** · Tier-2 Phase 2 operators · [R033.md](docs/roadmap/R033.md) · done 2026-07-31 — ONE operator ships, and the two refusals are each measured rather than argued. Spec…
- **R34** · A reused environment that is `Stopped` is not handled · [R034.md](docs/roadmap/R034.md) · done (`9505f9a`) — opt-in `envTool.requireStatus.equals` plus a `resolve[].reads.status` entry: a reused…
- **R35** · The `TestPermissions` diagnosis had two blind spots · [R035.md](docs/roadmap/R035.md) · done — (1) fixed: refusals are collected per batch…
- **R36** · A fixture `asserterror` accepts the wrong error, hiding a real mutant · [R036.md](docs/roadmap/R036.md) · done — the test now asserts the error message NAMES THE RECORD it loaded (`T-REQ`), which a blank record…
- **R37** · Coverage subsets are narrower than the gate's confidence implies · [R037.md](docs/roadmap/R037.md) · done (2026-07-27) — answered on a REAL project, and the answer is zero false survivors. Two runs of one…
- **R46** · A survivor no instrumented guard fired for is not a finding, and until now nothing distinguished one · [R046.md](docs/roadmap/R046.md) · done (`see commit`)
- **R47** · The per-mutant time budget is a hardcoded floor, and one slow (mutant, test) pair costs the WHOLE run · [R047.md](docs/roadmap/R047.md) · done — both halves shipped. `--mutant-timeout-ms <n>` (`SessionConfig.mutantTimeoutMs`) raises the FLOOR; the…
- **R38** · The codeunit selector var is anchored before the object's PROPERTIES, so any codeunit that declares one does… · [R038.md](docs/roadmap/R038.md) · done (`2237d91`) — the codeunit anchor is now the first non-property member; all-properties falls through to…
- **R39** · The batch project copies only `*.al`, so a real app's resources never reach `alc` · [R039.md](docs/roadmap/R039.md) · done (`d724b62`) — resources copy with their relative paths intact…
- **R42** · Instrumentation multiplies ONE existing diagnostic ~18×, and introduces none · [R042.md](docs/roadmap/R042.md) · done (measured; no code change — see rationale)
- **R43** · `alc 18` writes OPC part names with single-encoded spaces, producing a package BC 28 cannot load — and LethAL… · [R043.md](docs/roadmap/R043.md) · done (`46a4a46`) — `bcdev.alcPath` pins the compiler; a configured path also satisfies the "no AL extension…
- **R44** · Publish cost scales with INJECTED GUARD COUNT, because BC recompiles the extension server-side — and a fully… · [R044.md](docs/roadmap/R044.md) · done (`dd6c069`) — `--max-guards-per-batch` bounds a batch by guards; splitting stays at FILE granularity…
- **R45** · The baseline runs the WHOLE test suite, so `--only` narrows cost by far less than it appears to · [R045.md](docs/roadmap/R045.md) · done (`59e603d`) — `--tests-only <glob>` narrows the baseline. Measured on Document Output: baseline 744.8 s…
- **R40** · The uninstrumentable-object-kind gap costs 41% of a real app's mutation sites — and the reason given for it… · [R040.md](docs/roadmap/R040.md) · done
- **R41** · There is no way to run a subset of a project's mutants, so a large app has no cheap first run · [R041.md](docs/roadmap/R041.md) · done (`8f24ea9`) — `--only <glob>`, repeatable, patterns union, matched on project-relative forward-slash…
- **R48** · Nothing stopped a user pointing `lethal run` at a whole real app, and that path cannot succeed · [R048.md](docs/roadmap/R048.md) · done — `assertRunSizeAcceptable` refuses above `LARGE_RUN_MUTANT_THRESHOLD` (1,000) before anything is…
- **R51** · `force-reset-lease` could not reach an env-tool environment at all — the one recovery path a proxy-severed… · [R051.md](docs/roadmap/R051.md) · done — `recoveryBaseUrl` honours a configured `bcdev.baseUrl` and REFUSES one whose path segments do not…
- **R52** · `--resume` picked the most recent unfinished run even when it had recorded NOTHING · [R052.md](docs/roadmap/R052.md) · done — the query now also requires at least one row with a CARRYABLE verdict, with `CARRYABLE_VERDICTS`…
- **R53** · A mutant that makes a test hang is indistinguishable from a stranded operation, so it quarantines instead of… · [R053.md](docs/roadmap/R053.md) · done — the blocking half was fixed earlier; the scoring half is built, proven, and now GATED (`itest:hang`).…
- **R54** · A resumed run's phase clocks did not add up, and the report hid it · [R054.md](docs/roadmap/R054.md) · done — `carried` is recorded per outcome and surfaced on `MutantOutcome.carried`…
- **R55** · The baseline and the mutants are measured on DIFFERENT RUNNERS, and a test that fails on the baseline's… · [R055.md](docs/roadmap/R055.md) · done 2026-07-28 — SUBSUMED by R58's rollout: with `coverageMode: "fenced"` the default, baseline and mutants…
- **R56** · A docs-only commit deleted a fixture test's body, and the tables gate kept passing for days against a stale… · [R056.md](docs/roadmap/R056.md) · done — `bun run compile:fixtures` (`scripts/compile-fixtures.ts`) compiles all five fixture projects offline…
- **R57** · WHY the hub and the fenced runner disagree is unmeasured · [R057.md](docs/roadmap/R057.md) · done (2026-07-27) — measured, and it is neither flakiness nor state.…
- **R58** · Collect coverage on the FENCED path, and delete the dual-runner asymmetry entirely · [R058.md](docs/roadmap/R058.md) · done 2026-07-28…
- **R59** · The unsafe direction of the runner disagreement is undetected · [R059.md](docs/roadmap/R059.md) · done 2026-07-31 — and the entry's PREMISE IS REFUTED: the false kill it fears cannot occur. Verified in code…
- **R60** · Every LethAL verdict describes the app's NON-GUI branch, and nothing says so · [R060.md](docs/roadmap/R060.md) · done — measured first, then stated. `scripts/measure-gui-guarded.ts`…
- **R61** · The hub's coverage map emits object-level entries for objects the target does not own, and nothing said so · [R061.md](docs/roadmap/R061.md) · done (0d10ded) — the declared-objects scope check shipped inside R63's fix…
- **R62** · `AppMethodIndex` never resolves an extension's method names, so every `tableextension`/`pageextension`… · [R062.md](docs/roadmap/R062.md) · done — `SYMBOL_ARRAYS` now includes both extension kinds (14/15, the numbers measured live under R40)…
- **R63** · The hub credited tests with coverage they could not have produced — measured on Document Output as 77 vacuous… · [R063.md](docs/roadmap/R063.md) · done (0d10ded) — mechanism measured (probe + symbol-file + call-tree proofs), fix shipped and gated
- **R64** · The bcdev backend could never run on a Linux or macOS host, and the publish tool was not pinnable · [R064.md](docs/roadmap/R064.md) · done (PR #1 + follow-ups) — live gate reproduced exactly on Linux…
- **R71** · `SwapRecXRec`, scoped to `OnValidate`/`OnRename` — the signal is measured, the operator is not built · [R071.md](docs/roadmap/R071.md) · done (this commit) — `packages/builtin-tier2/src/swap-rec-xrec.ts`, scoped to field `OnValidate` and table…
- **R72** · BC's "cannot run codeunit in a write transaction" is not distinguished from a genuine kill · [R072.md](docs/roadmap/R072.md) · done (detector shipped 2026-08-09, and the row's own candidate was measured WRONG twice over). A 2x2x2 on…
- **R73** · `RemoveCommit` ships proven on its refusals and unproven on its claims · [R073.md](docs/roadmap/R073.md) · done (this commit) — `codeunit 79312 "Data Commit Ops"` gives the fixture its first POSITIVE `remove-commit`…
- **R70** · The symbol table keys scope on the BARE object name, so a page named after its table silently overwrites the… · [R070.md](docs/roadmap/R070.md) · done (`5921e06` fix + this commit's fixture) — scope keyed by `objectScopeKey(kind, name)`, reproduced first…
- **R69** · `TestPage` is UNSUPPORTED on the fenced path, so every test that opens one fails there — and the original… · [R069.md](docs/roadmap/R069.md) · done (`c1da575`, measured unprofitable 2026-08-02) — routed path and control-app objects 71011-71014 DELETED…
- **R78** · The R69 router's gate 1 is DEAD in the only configuration where a real TestPage suite survives — so the… · [R078.md](docs/roadmap/R078.md) · closed 2026-08-14: MOOT, on the same verified grep as R74…
- **R79** · A COMMENT containing `codeunit <id> "Name"` silently truncates test discovery — every `[Test]` after it in… · [R079.md](docs/roadmap/R079.md) · done (`48b6dce`) — fix (a) as written, plus a narrowed form of the count assertion. `maskNonCode` blanks…
- **R74** · BLOCKS WIRING `routedTransport`: routed survivors discard the attestation, and the identity gate is vacuous… · [R074.md](docs/roadmap/R074.md) · closed 2026-08-14: MOOT, and VERIFIED moot rather than assumed. Re-checked 2026-08-14 by grep…
- **R75** · BLOCKS WIRING `routedTransport`: routed verdicts discard `coverageAttribution` · [R075.md](docs/roadmap/R075.md) · closed 2026-08-14: MOOT, on the same verified grep as R74…
- **R76** · A real `TestPage` test may make a fenced BASELINE unsurvivable — which would put the shipped R69 diagnosis… · [R076.md](docs/roadmap/R076.md) · done (`pending`) — MEASURED live on the 136-mutant tables fixture 2026-08-08, both coverage modes. A…
- **R77** · LethAL could supply its OWN test runner, which would move mutant activation and attestation INSIDE the test… · [R077.md](docs/roadmap/R077.md) · closed — REFUSED 2026-08-08, both surviving halves, with reasons. The hub half was already closed on platform…
- **R68** · A variable declared in a TRIGGER's own `var` section is never resolved, so Tier 2 refuses every site whose… · [R068.md](docs/roadmap/R068.md) · done (this commit) — `triggerScopeVar` (`packages/builtin-tier2/src/receiver.ts`) resolves the nearest…
- **R67** · A plain `page`'s implicit `Rec` is refused, though its `SourceTable` is declared in the page itself · [R067.md](docs/roadmap/R067.md) · done (this commit) — `sourceTableOf` reads the page's own `SourceTable = "X";` PROPERTY…
- **R82** · `SwapCallArguments` — swap two same-typed arguments at a call site · [R082.md](docs/roadmap/R082.md) · done (`f9e055c` mechanism, rate measured `pending`) — CLOSED 2026-08-08 together with R85, which was its only…
- **R83** · Is `InherentPermissions` a better `PermissionReduce` target than `Permissions`? Unknown, and worth one… · [R083.md](docs/roadmap/R083.md) · done (`pending`) — MEASURED live 2026-08-08 on Cronus281 (`scripts/r83-probe/`). ANSWER: NO, for two…
- **R119** · `scripts/` is outside `tsc --build`, so `bun run typecheck` type-checks none of the 21 scripts · [R119.md](docs/roadmap/R119.md) · done (this commit) — `scripts/tsconfig.json` admits the 11 files that compile clean and is referenced from…
- **R84** · `buildTypeTable` truncates every subtype, so "same type" is wrong for exactly the types where it matters · [R084.md](docs/roadmap/R084.md) · done (`f9e055c`) — `extractType` keeps the whole declaration, whitespace-collapsed. No special case was…
- **R120** · Eight `scripts/` files do not compile under the repo's own strict flags, and two of them produced numbers a roadmap decision was made on · [R120.md](docs/roadmap/R120.md) · done (`pending`) — reading (a) is WRONG and MEASURED wrong: the `ALNodeKind` comparisons DO fire at runtime…
- **R85** · The RATE at which a real suite notices a real argument swap — unmeasured, and it is the only R82 question… · [R085.md](docs/roadmap/R085.md) · done (`pending`) — MEASURED 2026-08-08 on the WHOLE swap population…
- **R87** · `resolveIdentifierType` answers from the WRONG OBJECT when two objects declare a same-named procedure, and… · [R087.md](docs/roadmap/R087.md) · done (`ac9ce94`) — scope is keyed from the identifier's OWN enclosing declaration…
- **R88** · A released `lethal` binary carries no evidence of which commit built it, and the filename cannot tell a stale… · [R088.md](docs/roadmap/R088.md) · done (`pending`) — `--version` now prints the build commit…
- **R89** · `--stop-hung-sessions` is only PARTIAL on a hosted environment, and a real DO codeunit still cannot complete… · [R089.md](docs/roadmap/R089.md) · open — NARROWED again 2026-08-08 and now GUARDED. The `--stop-hung-sessions` half is superseded by R91. The…
- **R90** · A single file whose instrumented form exceeds the publish budget cannot be measured at all, and… · [R090.md](docs/roadmap/R090.md) · done (`b95426b`) — the refusal exists and is MEASURED, not configured…
- **R91** · The 30 s mutant budget floor is wrong by default for real AL, and the failure it produces is a tier… · [R091.md](docs/roadmap/R091.md) · done (`bdc5b6b`) — the default was raised, not left as an option: `MIN_MUTANT_BUDGET_MS` is now 180_000…
- **R92** · Nothing lets a user predict the DEPLOYED mutant count, and the two available numbers differ by enough to… · [R092.md](docs/roadmap/R092.md) · done (`b95426b`) — `--dry-run` now reports both: the header carries `N mutant site(s), D deployed mutant(s)`…
- **R104** · `SessionConfig.emit` accepting a single subscriber, plus a separate `fanOutEmit` helper to combine several,… · [R104.md](docs/roadmap/R104.md) · done (`pending`) — `SessionConfig.emit` is `readonly EventSubscriber[]`, `fanOutEmit` and its test suite are…
- **R105** · Two session-level bookkeeping structures in `runSession` are write-only and confirmed dead, kept only to… · [R105.md](docs/roadmap/R105.md) · done (`6321843`) — the two write-only Sets are DELETED. `outcomes` is KEPT: this row lists it as equally dead…
- **R106** · `report-fold.ts`'s `untargetedTriggerCount` has no presence check of its own, unlike the three events the… · [R106.md](docs/roadmap/R106.md) · done (`78613ad`) — the fold throws when a batch finished baseline with a GREEN test under a coverage-claiming…
- **R86** · No kill records WHY it died, so a false kill and a real one are indistinguishable in the record · [R086.md](docs/roadmap/R086.md) · done (`4f3496b`) — the killing run's own failure text is recorded on every kill, in the report and the store…
- **R81** · `buildCallerIndex` keys call sites on the BARE owner name, so two same-named objects merge their callers —… · [R081.md](docs/roadmap/R081.md) · done (`2af5e23`) — measured first, and the measurement made the fix free. Severity today is ZERO, for a…
- **R80** · Two AL comment-strippers existed in two packages, and they disagreed about string literals · [R080.md](docs/roadmap/R080.md) · done (`bad29a1`) — measured first, then unified with the policy kept as a flag. The row's premise…
- **R93** · al-runner v2 landed upstream and every flag we send is gone — the gate is measured RED · [R093.md](docs/roadmap/R093.md) · done (`51e6415`) — `itest:alrunner` is GREEN at the frozen 3 killed / 13 survived / 0 no-coverage…
- **R123** · The al-runner canary measures two defects that are FIXED, and not the contract that moves several times a day · [R123.md](docs/roadmap/R123.md) · done (`930a0b6`) — five REFUSE-class facts measured per session against the binary that is about to produce…
- **R97** · v2's server protocol keeps our wire shape but ignores our second bundle — and returns GREEN EMPTY, not an… · [R097.md](docs/roadmap/R097.md) · done (`pending`) — RE-MEASURED against al-runner 2.1.0.0 on 2026-08-08 and the upstream defect this row is…
- **R98** · al-runner v2 does not run on Windows, and we are Windows · [R098.md](docs/roadmap/R098.md) · done — both blockers cleared 2026-08-05. #1657 merged 16:41 UTC (`e2555bd`), #1656 at 16:40 (`4eaf9d3`)…
- **R99** · v2 fixes R7 and R8 — measured — so the canary and `authoritative: false` both need re-deciding, not… · [R099.md](docs/roadmap/R099.md) · done (`51e6415`) — CLOSED 2026-08-08 after re-deciding. Both asks are complete: `authoritative` was…
- **R101** · v2 surfaces we should use, not just survive · [R101.md](docs/roadmap/R101.md) · closed 2026-08-09 — all five parts now DECIDED. (b), (d) and (f) refused with measurements as recorded below.…
- **R129** · A report does not record WHICH BC build produced its verdicts on the al-runner path, though the runner announces it · [R129.md](docs/roadmap/R129.md) · done (2026-08-09) — `parseAlRunnerBcBuild` reads `[bc] selected BC <version>`…
- **R65** · A failed tool spawn can report nothing at all — and it happened again on the PUBLISH path 2026-08-05 · [R065.md](docs/roadmap/R065.md) · done — `describeThrown` (`packages/runner/src/describe-error.ts`) backs the catches in `artifact.ts`…
- **R124** · al-runner v2 crashes with an unhandled exception when two bundle dirs share a basename — report upstream · [R124.md](docs/roadmap/R124.md) · done — filed upstream 2026-08-07 as StefanMaron/BusinessCentral.AL.Runner#1692. LethAL is not exposed…
- **R125** · al-runner 2.1.0.0 selects a BC build our fixture has no runtime packages for, so `itest:alrunner` goes 0/0/0 on an upstream release · [R125.md](docs/roadmap/R125.md) · done (`pending`) — `buildAlRunnerArgv` now sends `--auto-provision`, upstream's own named remedy. Gate is…
- **R128** · `--auto-provision` makes the FIRST mutant absorb a ~1 GB download inside its own timeout budget · [R128.md](docs/roadmap/R128.md) · done (2026-08-09) — `AlRunnerBackend.provisionOnce()` runs a real `--auto-provision` invocation with a…
- **R107** · An env-tool publish that EXITS 0 while reporting failure in its body is invisible to us — and that is R90's… · [R107.md](docs/roadmap/R107.md) · done (`pending`) — config-declared `successWhen` (positive evidence, fails closed) on any env-tool block; the…
- **R126** · al-runner server mode only pays if the backend can make ONE call per MUTANT instead of one per test · [R126.md](docs/roadmap/R126.md) · closed 2026-08-14: REFUSED on measured evidence, and the condition this row named does NOT hold. Measured…
- **R130** · al-runner's `--auto-provision` provisions the same platform apps TWICE inside one invocation · [R130.md](docs/roadmap/R130.md) · closed 2026-08-14: RE-MEASURED and it still reproduces, on al-runner 2.1.2.0, exactly as recorded against…
- **R131** · The al-runner artifact cache grows a new ~137 MB directory every time Microsoft publishes, and nothing cleans it · [R131.md](docs/roadmap/R131.md) · done (report landed 2026-08-14) — RE-MEASURED first: the cache is now 1.3 GB across 8 build directories…
- **R146** · `lethal doctor` refuses an al-runner-only project, so its al-runner checks reach nobody who only uses al-runner · [R146.md](docs/roadmap/R146.md) · done (2026-08-15) — an al-runner-only config now produces a report of TWO real checks…
- **R147** · Every al-runner CLI invocation re-downloads 230 MB of platform apps, and LethAL can stop paying for it with --package-cache · [R147.md](docs/roadmap/R147.md) · done (2026-08-15) — `provisionOnce` reads the platform-app directory off al-runner's own completion sentence…
- **R148** · The al-runner fixture's test bundle resolves its own target to a stale symbol-only .app, and al-runner warns that calls into it will fail · [R148.md](docs/roadmap/R148.md) · done — decision 2 built and covered offline (parser + emission, red-checked); decision 1 answered and acted…
- **R149** · After R147 the R123 contract probe measures an argv no mutant sends, and its own promise is that it does not · [R149.md](docs/roadmap/R149.md) · done — the contract is now MEASURED under the session's pin, after provisioning, without giving up the…
- **R108** · A batch whose TOTAL guard count exceeds the measured publish ceiling still pays for the failure — R90 refuses… · [R108.md](docs/roadmap/R108.md) · done (`pending`) — `batchCeilingWarning` warns (never refuses) when a batch TOTAL crosses the tier bracket…
- **R154** · CI and the release workflow existed but had never run — both are now verified on real runs · [R154.md](docs/roadmap/R154.md) · done (release run 32063692530, 2026-08-17) — both workflows are now verified on real runs: ci.yml green on…
- **R168** · al-runner keeps a SECOND cache tree (149 MB) that `doctor`'s cache accounting does not read · [R168.md](docs/roadmap/R168.md) · done (this commit) — `doctor` now reports both roots. The second is a plain total, deliberately: no…
- **R169** · LethAL's own object ids collided with a real product's, and two fixtures could not be co-installed · [R169.md](docs/roadmap/R169.md) · done (this commit) — `LethAL Control` moved 71000-71010 -> 91000-91010, and `sandbox-data`'s selector triple…
- **R170** · The selector-id convention was already implemented and enforced; only the docs never said it · [R170.md](docs/roadmap/R170.md) · done (this commit) — and the row's original premise was WRONG. `pickSelectorIds` has implemented one rule all…

---

**Recently closed** (delete these once a release has passed):

- Tier-2 Phase 0 — table triggers mutate, execute and kill on a live server; merged 2026-07-25 (`841069c`), frozen at `itest:tables` 3 killed / 2 survived / 2 no-coverage. Superseded by Phase 1's 63 / 10 / 2 over the same, larger fixture.
- Tier-2 Phase 1 (R10) and the live dedup-collision proof (R12) — see their rows above.
- Coverage keyed on `(objectType, objectId)` rather than the bare id (`6e89948`) — a table and a codeunit sharing an id sent a trigger mutant at the wrong object's tests.
- Per-mutant time budget floored at 30 s (`ab58469`) — an unfloored `2 × baseline` quarantined a cold start as in-flight-unknown.
